# Velora — Checklist

## Initialization (Definition of Done)

### Intake
- [x] Project goal, runtime, deliverables, target users, supported tools, validation workflow known.
- [x] Web architecture decisions captured (`docs/architecture.md`, `docs/routes.md`, `docs/data-flow.md`, `docs/api-contract.md`, `docs/design-system.md`).
- [x] Product/domain model captured from `docs/briefings/storyline-chat-briefing.md` (Storyline / Character / Setting / Scenario + Story Event + Stat system; five event types) and reflected across docs.
- [x] Visual design system locked to the `docs/CharacterFrontpage/` reference (Cinzel / EB Garamond / IBM Plex Mono; Parchment / Ember / Slate themes) in `docs/design-system.md`.

### Canonical docs
- [x] `docs/` exists with documentation, structure, workflow, checklist, architecture, routes, component-map, data-flow, deployment, design-system, api-contract.
- [x] `docs/plans/` exists.
- [x] `docs/skills/` exists with `global-project-rules` + all selected skills.
- [x] `docs/skills/global-project-rules/SKILL.md` names the required reading.
- [x] Supporting skill files preserved (`structures/`, `ui/`, `SETUP.md`, `planner.md`).

### Agent pointers
- [x] Claude Code (`.claude/skills/`), OpenAI Codex (`.agents/skills/`), Cursor (`.cursor/rules/`) pointer files created.
- [x] Each pointer references `docs/skills/global-project-rules/SKILL.md` + its canonical skill.
- [x] No agent folder holds the only copy of instructions.

### Project structure
- [x] Required top-level directories exist (`web/frontend`, `web/backend`, `web/shared`, `utils` (with `utils/tests` + `utils/scripts`), `libs`, `docs`).
- [x] Web code is under `web/`; root `app.py` is the backend entrypoint.
- [x] `pyproject.toml`, `.python-version`, `.env.example` exist.
- [x] `README.md` points to canonical docs.

### Cleanup
- [x] Selected starter skill dirs migrated into `docs/skills/` and removed.
- [x] Unselected starter material removed.
- [x] Initialization helpers (`initialize.md`, `read-yaml.py`) removed after migration.
- [x] No duplicate competing sources of truth.

### Verification
- [x] Final tree inspected after cleanup.
- [x] Canonical docs and representative pointer files checked.
- [x] Pointer targets verified to exist.
- [x] Validation commands run — frontend now runs `tsc` / `eslint` / `next build` / `vitest` (all green at scaffold). Backend `pytest` still N/A until backend code exists.

## Follow-up Work (next steps)

### Chat composer redesign — two-row composer, circular context dial, exact tokens (done; `worktree-chat-composer-redesign`; plan: `docs/plans/chat-composer-redesign.md`)
User ask: tone down the composer area. Rework it into **two rows** (top = the extendable textarea; bottom = a controls bar) with the per-scene **Config moved down** from the header into the bottom-left (room reserved for future options), a **"Send →" pill** on the bottom-right, and — left of Send — a **circular fillable context dial** replacing the flat context bar. Make the context readouts **exact** (the real tokens sent to the model, not char/4 approximations) — both the dial and the Config "Number of beats" readout. Five phases, commit-per-phase, in a worktree merged to `main`. **Decision (implementation):** the exact figure = the character-turn call's `usage.prompt_tokens`, carried on a **persisted `context` trace step** (no new event type / wire frame / Alembic migration — the player already streams trace frames live and reloads persisted traces on resume). Beats readout uses the real transcript (still char/4, no client tokenizer); a `/tokenize`-based exact readout is a deferred follow-up.
- **P1 — backend exact tokens [done]:** `llm.chat_complete_usage(...)→(text, prompt_tokens)` (reads `usage.prompt_tokens`, `None` when absent/non-positive; `chat_complete` wraps it); `character_turn_agent.generate_line_with_usage`; `turn_engine._generate_speaker` emits a persisted `context` trace step `{characterId, promptTokens}` when usage is present. Also fixed a pre-existing E402 (moved the module logger below imports). Tests (+7): chat_complete_usage (tokens/None/non-positive), generate_line_with_usage, play_turn context-trace present+persisted / absent-without-usage.
- **P2 — frontend token state [done]:** `turn-stream.latestContextTokens(traces)`; `useScenePlay` `liveContextTokens` (from the live `context` frame + seeded from history on resume), exposes `usedTokens = liveContextTokens ?? estimate` + `usedTokensExact`. `applyActivity`/`applyCharacterActivity` already ignore `context`. Tests (+5): latestContextTokens, hook estimate-pre-turn/adopts-live/seeds-on-resume.
- **P3 — circular dial [done]:** new `ContextUsageDial` (SVG ring, `role=progressbar`, `text-success`/`gold`/`danger` by the same <50/50–75/≥75 thresholds, centre K count, `"X of Y tokens (exact|estimated)"` title/valuetext, motion-reduce safe); deleted `ContextUsageBar` + test + its `StoryPlayerView` block. Tests (13).
- **P4 — two-row composer [done]:** `Composer` rebuilt (textarea row + `border-t` controls row: Config bottom-left `openUp`, blank gap, dial, "Send →" pill); `SceneConfigMenu` gains `beatTexts` (content-real readout via `beatsTokensFromTexts`) + `openUp`; `SceneHeader` drops Config; `StoryPlayerView` threads config + dial props + derived `beatTexts`; `contextBudget.beatsTokensFromTexts` (with a `slice(-0)` guard). Tests updated (Composer/SceneConfigMenu/SceneHeader/contextBudget).
- **P5 — docs + validation + merge [done]:** api-contract (`context` step + `promptTokens`), data-flow (exact-token path + content-real beats readout), design-system (two-row composer + dial + Send pill + Config relocation), component-map (`ContextUsageDial` in / `ContextUsageBar` out / Composer·SceneHeader·SceneConfigMenu rows), CLAUDE.md (component list), this entry.
- **Validation:** backend **682 pytest** (all green; ruff clean on touched files; mypy: only pre-existing `stat_render.py` errors, none in touched files); frontend **485 vitest** (+25) + typecheck clean + lint (0 errors) + `next build` clean.
- **Deferred — live in-browser pass:** the standing worktree shared-dir + backend-CORS-pinned-to-3346 + configured-LLM constraint blocks a clean `preview_start` from the worktree (an active dev server already holds 3346/3345 — likely the user's session — and can't be killed sight-unseen), matching every prior play-experience entry. Verified via the green suites + `next build`: the dial is a labelled `progressbar` with a text title (meaning never colour-only), the Send pill + Config button are keyboard-operable, the config popover opens upward to clear the screen edge. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Chat UX additions — context bar, header Config, flowing composer, Scene pulse, character activity, type-while-streaming (done; `worktree-chat-ux-additions`; plan: `docs/plans/chat-ux-additions.md`)
User ask: modernize the story-player chat experience in six ways: (1) a color-coded **context usage bar** showing estimated conversation tokens vs. the model's context window, hover showing `5K / 16K` counts; (2) the **Config button** moves from the composer into `SceneHeader`, immediately left of Export; (3) the **message input** becomes a flowing auto-grow textarea with an embedded circular Send button; (4) the right rail's static goal/tension/relationships display is replaced by a live animated **Scene pulse** feed; (5) characters show **typing indicators** and "Thinking"/"Speaking" status labels; (6) the player can **keep typing while characters respond** (only Send blocked). Seven phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — model context window [done]:** `llm_backend.get_context_window(base_url, api_key)` probes llama.cpp `/props` → `default_generation_settings.n_ctx` (top-level `n_ctx` fallback) and vLLM `/v1/models` → first model's `max_model_len`; cached per base URL (same TTL + `clear_cache()` seam). New `max_context_tokens` LLM setting (default 16384, floor-clamped 1024, persisted in `llm` namespace, on `LlmConfigRead`/`LlmConfigUpdate`). `GET /options/llm/context-window` → `{ maxContextTokens, source: "detected"|"configured" }`. Options → Language Models tab gained a "Max context (tokens)" fallback field.
- **P2 — context usage bar [done]:** `lib/contextBudget.ts` gained `estimateUsedTokens` + `fmtTokensK`. New `ContextUsageBar.tsx` — slim `role="progressbar"` bar above the composer (centered `max-w-[720px]` wrapper, only when context window fetched); fill: `bg-success` < 50 %, `bg-gold` 50–75 %, `bg-danger` ≥ 75 %; hover `title` + `aria-valuetext` show `"5.2K / 16K"` counts. `useScenePlay` fetches `getLlmContextWindow()` on mount (best-effort, bar hidden on failure) and derives `usedTokens` from the last `contextBeats` beats (char/4 heuristic over text+action+thought).
- **P3 — Config into the header [done]:** `SceneConfigMenu` restyled to the header-button idiom and rendered by `SceneHeader` immediately left of `ExportMenu`; popover opens downward (`top-[38px] right-0`). Props wired from `StoryPlayerView` through `SceneHeader`. Removed from `Composer`.
- **P4 — composer redesign + type-while-streaming [done]:** `Composer` rebuilt — transparent outer band, centered `max-w-[720px]` `rounded-t-[14px]` `border-field-bd bg-field` panel; `<textarea rows=1>` auto-grows to `scrollHeight` capped 240 px; right padding reserves Send column; embedded circular `38px rounded-full bg-accent` Send button absolute bottom-right with paper-plane SVG + `aria-label="Send"`; Enter sends / Shift+Enter newline. Prop renamed `disabled` → `sendDisabled` — textarea stays editable while streaming; only Send + Enter are blocked.
- **P5 — activity state layer [done]:** `turn-stream.applyActivity` builds an `ActivityEntry[]` feed (newest-first, cap 12) from trace steps + story events. `turn-stream.applyCharacterActivity` derives `Record<charId, "idle"|"thinking"|"speaking">` — `speaker`/`internal_thought` → thinking; first dialogue chunk → speaking; `done` + stream end → idle. Both held in `useScenePlay` state; `rehydrateFromHistory` seeds neither (live-only).
- **P6 — Scene pulse rail + character typing indicators [done]:** `DirectorRail` replaced its static goal/tension/relationships sections with a `role="log"` `aria-live="polite"` pulse feed; `TensionMeter`/`Relationships` components still exported for `CharacterDossier`; `StateChips` kept. `CastRail` rows show animated `embDots` typing indicator + "Thinking" label, or "Speaking" — static `"…"` fallback under reduced motion.
- **P7 — docs + validation [done]:** api-contract, data-flow, design-system, component-map, structure (no change needed), CLAUDE.md, this entry.
- **Validation:** backend **672 pytest** on the branch, **675** after merging main's concurrent Extraction Opt-In (all green); frontend **478 vitest** post-merge + typecheck clean + lint 0 errors / 5 warnings (all 5 pre-existing in untouched files, incl. api-mock `_id`) + `next build` clean pre- and post-merge.
- **Deferred — live in-browser pass:** the standing worktree shared-dir/CORS constraint + configured-LLM requirement blocks a clean `preview_start`, matching every prior play-experience entry. Verified via deterministic suites; the new surfaces are native controls/text (progressbar semantics on the bar, labeled textarea + 38 px circular Send, `role=log aria-live=polite` feed, text "Thinking"/"Speaking" labels so meaning is never motion-only; `embDots` static fallback under reduced motion).

### Document extraction — opt-in "Extract" check-off (done; `feat/extraction-opt-in`; plan: `docs/plans/document-extraction-opt-in.md`)
User report: **Build the whole world** auto-mined every attached reference doc for characters/settings "when we don't ask it to." Ask: make extraction a per-file check-off like the existing **RAG** and **Draft** toggles. Decision (with the user): **fully persisted flag** (not a frontend-only gate), **default OFF** everywhere so a new storyline never auto-extracts. Four phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — Persist `include_extract` [done]:** new `ContextDocument.include_extract` column (NOT NULL, `server_default false` so existing rows backfill and the additive-column reconciler self-heals it) + Alembic `f6a7b8c9d0e1` (chained after `prompt_overrides`); `includeExtract` on `ContextDocumentBase`/`Read` (`False`), `ContextDocumentUpdate` (`bool | None`), and `TriageItem` (`False`); `crud._new_context_document` carries it (update flows via the generic `model_dump` patch). Tests: default `includeExtract=false` on read; create-with-true + PATCH-toggle round-trip.
- **P2 — Server-side gate + conservative triage [done]:** `BuildDoc.extract: bool = False`; `build_agent._opted_in` filters the extraction work-list to `extract=True` docs before `_doc_sources` (bucket-kind policy + `other_docs`→grounding unchanged); `triage_agent` prompts (batch + per-file) gained a **conservative** `includeExtract` rule (true only for a clear, single, explicitly-named character/setting profile; default false), coerced in `_coerce_item`/`_fallback`. Tests: extract-off char/setting/uncategorized doc **not** mined (no cast/settings), mixed opt-in mines only the checked doc, classified fallback still fires for an Extract-checked unnamed doc, `other` doc still grounds; triage coerces `includeExtract` (omitted → false). Every existing build-test doc payload opted in (`extract: True`).
- **P3 — Frontend Extract chip + creator round-trip [done]:** `useExtract?` on `ReadDoc` (undefined = OFF) + `DocUse` widened; `includeExtract` on the `ContextDocument`/`TriageItem` types + `ContextDocumentInput` + each `BuildWorldBody` doc entry (`extract?`); `storylineCreator` (`UploadDefaults.useExtract`, `toCreatorDoc` default OFF, `fromContextDocument`/`docToContextInput` round-trip, `applyTriage` carries it); `useStorylineCreator` (`toggleDocUse` → `DocUse`, `toBuildDoc` emits `extract`); `TriagePanel` third **Extract** chip (per-row + upload default, generalized `uploadUses` record); `entityDocs` round-trip (no chip on `ContextFilesPanel` — extraction never applies to entity-scoped docs). Tests: creator mappers (default/round-trip/triage-carry), `TriagePanel` Extract chip (per-row toggle + upload default), `useStorylineCreator` build sends `extract` per doc (only checked → true).
- **P4 — docs + validation + merge [done]:** api-contract (`includeExtract` field + `BuildDoc.extract` + opt-in build policy + triage suggestion), data-flow (opt-in extract gate + triage item + persistence note), component-map (TriagePanel Extract chip), this entry.
- **Validation:** backend **661 pytest** (all green; ruff + mypy clean on touched files; single Alembic head `f6a7b8c9d0e1`); frontend **401 vitest** (+9) + typecheck clean + lint (0 errors; 5 pre-existing warnings in untouched logic) + `next build` clean.
- **Deferred — live in-browser pass:** the standing worktree shared-dir/CORS constraint blocks a clean `preview_start`, matching prior authoring entries. The Extract chip reuses the existing `aria-pressed` toggle idiom + AA `--field`/`--card` tokens in the same flex-wrap row (reachable at 320/375/768/1024); verified via the green component suites + typecheck + `next build`. Run the live keyboard + responsive pass once the dir + stack are free.

### Stat + per-band descriptions with `{Character}` substitution in the character prompt (done; `feat/stat-band-descriptions`; plan: `docs/plans/stat-band-descriptions.md`)
User ask: in the new/edit-storyline Statistics area, the agent should generate a **better** stat description (1–2 sentences) and give **each band its own description**, both written with a `{Character}` placeholder so the story engine can pass the character's name in and tell the LLM what the character's *current* stat means (e.g. Stamina 45 → "Capable: {Character} still has the fight to continue forward"). Ensure the new system is used in the prompts. Six phases, commit-per-phase, in a worktree merged to `main`. No Alembic migration — bands live in a `JSONColumn`, so the new per-band `description` key is purely additive.
- **P1 — `StatBand.description` [done]:** optional `description: str = ""` on the `StatBand` schema (label stays required); model band-column comment notes the new key. Test: band round-trips a description, defaults to `""`, blank label still rejected.
- **P2 — `services/stat_render.py` [done]:** `substitute_character` (replaces `{Character}`/`{character}`), `current_band` (first `[min,max]` containing the value; malformed rows skipped — mirrors FE `bandLabelFor`), `render_character_stats` (per-stat block: `Display value/max (BandLabel) — <stat desc> <current band desc>`, name-substituted, only the current band shown, empty descs degrade gracefully). Tests: substitution cases, band boundaries/no-match, multi-stat block, skip-unset/empty.
- **P3 — generation + sanitizer [done:]** `build_agent._BLUEPRINT_SYSTEM` rewritten (JSON example + rules) to author 1–2 sentence stat descriptions and per-band descriptions using the literal `{Character}` placeholder, explaining LOW vs HIGH; `_sanitize_bands` carries `description` (stripped, default `""`); `character_agent._bands_text` folds band descriptions into the stat-proposal context. Tests: sanitizer keeps/defaults the description; blueprint parse surfaces it; stat-proposal prompt includes the band description.
- **P4 — prompt injection [done]:** `character_turn_agent._compose` replaces the flat `k=v` HEAD state line with `stat_render.render_character_stats(ctx.stat_defs, speaker.stats, speaker.name)`, falling back to `k=v` only when there are no defs. Tests: current band chosen by value + name-substituted, non-current band text absent, no `{Character}` leak, `stat_defs=[]` keeps the `k=v` fallback. `test_stats.py::test_bands_roundtrip_and_validate` updated for the always-present `description` field on read.
- **P5 — StatsEditor [done]:** `StatBand.description?` on the FE type; new bands seed `description: ""`; the editor adds a per-band description input + a `{Character}` hint (intro copy + placeholders), stat description became a 2-row textarea. Tests: new-band seeds empty description; editing a band description fires `onChange`.
- **P6 — docs + validation + merge [done]:** api-contract (band `description` + `{Character}` play-time semantics), data-flow (current-band injection section), structure + CLAUDE.md (`stat_render.py`), this entry.
- **Validation:** backend **658 pytest** (all green); frontend **StatsEditor** suite (7 passed) + typecheck clean + lint (0 errors; pre-existing warnings only in untouched files). Full frontend suite + `next build` run at merge.
- **Deferred — live in-browser pass:** the standing worktree shared-dir/CORS constraint blocks a clean `preview_start`, matching every prior authoring entry; the band-description injection also needs a configured-LLM turn to see live. The StatsEditor change is native `aria-label`led inputs/textarea on the existing AA `--field`/`--card` tokens (band row reflowed to a controls-row + full-width description input, reachable at 320/375px); verified via the green component suite + typecheck. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Character stat display fixes — Library "beneath the name" stats + in-scene live values that actually update (done; `worktree-fix-character-stat-display`; plan: `docs/plans/character-stat-display-fixes.md`)
User report: "a lot of things wrong with the statistics" — (1) the storyline/Library page should show each character's stat values beneath their name, and doesn't at all; (2) in a scene, the Turn Inspector correctly traces every stat change, but no always-visible surface (cast rail, the Director rail's own "Character stats" section) ever reflects it — a player has to click into a character's Dossier, and even that fell back to the generic schema default until *that* stat happened to change in the current session. Two research passes traced the real defects (backend already had everything needed — `CharacterStat` model + `GET`/`PUT /characters/{id}/stats`, both clamped to the storyline's stat schema — this was pure frontend wiring): `useScenePlay`'s `statsByChar` started **empty**, only ever filling from live/resumed `state_update` events, so a character whose real starting value differed from the schema default read as "never changing"; `DirectorRail`'s "Character stats" section called `StatSchema` with **no `values` prop at all** — an always-static legend with no character context, despite its label; `CastRail` and the Library's `CharacterCard` rendered no stats whatsoever; the carousel's `CastStats` flyout existed but always showed the schema **default**, never a character's real value. Six phases, commit-per-phase, in a worktree merged to `main`. **Decision:** the Director rail's unwired "Character stats" legend was **removed** (not fixed in place) — duplicating a static, characterless legend alongside the now-correct live per-character surfaces (cast rail + dossier) would be more confusing, not less; its own doc comment already confirmed it was a legend by design, not a bug in isolation.
- **P1 — `getCharacterStats` API wrapper [done]:** `lib/api.getCharacterStats(id)` wraps the existing `GET /characters/{id}/stats` (+ `test/api-mock.ts` mock) — the one piece missing to read the backend's already-complete persisted-stat-values endpoint from the frontend.
- **P2 — baseline-seeded live scene stats [done]:** `turn-stream.rehydrateFromHistory` gained an optional `initialStatsByChar` param (starting accumulator instead of a hardcoded `{}`); new `turn-stream.baselineStatsByChar` folds each cast member's persisted starting stats into a `StatChip[]` map via the existing `applyStatByChar` upsert; `useScenePlay` now runs one sequential flow — fetch every cast member's `getCharacterStats` (best-effort per character) → seed `statsByChar` → *then* resume the most-recent session on top of that baseline (persisted deltas override by key, untouched entries survive) — replacing the old resume-only effect that started from empty. Tests (+7): `baselineStatsByChar` (2), `rehydrateFromHistory` initial-baseline layering (2), `useScenePlay` baseline-seed/resume-layers-on-top/failed-fetch-degrades (3).
- **P3 — live per-character stats in the cast rail; removed the Director-rail legend [done]:** `CastRail`'s `CastMemberRow` gained a `CastMemberStats` sub-component — each cast member's public stat values beneath their name/role, live via `statsByChar` else the schema default; `liveValueFor` exported from `DirectorRail` so both consumers share one matcher. `DirectorRail`'s "Character stats" section + its now-unused `statDefs` prop removed (Scene state + Relationships untouched). Moved the "omits non-public stats" + band-legend coverage from `DirectorRail.test.tsx` into `CharacterDossier.test.tsx` (the schema panel still renders there); new `CastRail.test.tsx` stat-row cases. Tests (+7 net).
- **P4 — Library loads per-character persisted stats [done]:** `useLibraryState` fetches every active-storyline character's persisted stats (`getCharacterStats`, best-effort per character) into a new `statsByCharId` map, keyed by character id; derived from `characters` itself so any create/edit/delete naturally refreshes it (no manual invalidation). Tests (+3): loads-keyed-by-id, failed-fetch-degrades-to-empty-entry, refreshes-after-save.
- **P5 — `CharacterCard` + carousel `CastStats` show real values [done]:** `CharacterCard` gained optional `statDefs`/`statValues` props rendering public stats beneath the name/role (real value, schema-default fallback), threaded through `CharacterColumn` from `useLibraryState.statsByCharId`; `ScenarioCarousel`'s `CastStats` now reads `statValues?.[d.key] ?? d.default` instead of always `d.default`, threaded down from `LibraryView` through `CastStrip`/`CastCard`. Tests (+8): `CharacterCard` stat rows (4), `ScenarioCarousel` real-value-over-default (1), existing suites unaffected.
- **P6 — docs + validation + merge [done]:** api-contract (`GET /characters/{id}/stats` now frontend-consumed, by whom), data-flow (baseline-seeding rationale + cast-rail/dossier live surfaces + Library real-value display, replacing the stale "falls back until a stat first moves" note), component-map (`CharacterCard`/`ScenarioCarousel`+`CastStats`/`useLibraryState`/`CastRail`/`DirectorRail` rows), this entry.
- **Validation:** backend **649 pytest** (unaffected by this plan — no backend code changes, pure frontend wiring; the raw total moved because of concurrently-merged work, see below); frontend **399 vitest** (+25 from this plan) + typecheck (clean) + lint (0 errors; same 4 pre-existing warnings in untouched files) + `next build` clean.
- **Rebased onto concurrently-merged main:** two unrelated worktrees landed on `main` while this one was in progress — **Per-storyline / per-scenario writing prompts** (below) and a **flaky story-player test fix** that stubbed `useScenePlay`'s on-mount async effects (`listPlaySessions`/`getScenarioRelationships`/`closePlaySession`) in `StoryPlayerView.test.tsx` so they don't race real `fetch` in jsdom. This plan's own P2 added a fourth on-mount call (`getCharacterStats`, once per cast member) that the flaky-test fix didn't know about — left un-stubbed it would have reintroduced the exact same race. Rebased this branch onto the new `main` and added `getCharacterStats` to that same `vi.mock` stub list; re-verified the previously-flaky test 5/5 stable.
- **Accessibility/responsive note:** every new stat row is non-interactive text reusing existing AA-compliant tokens (`text-ink-soft`/`text-accent`/`OVER_ART.body`/`OVER_ART.eyebrow`, already used by `StateChips`/`TensionMeter`/the carousel's over-art text); no new interactive elements or breakpoints were introduced, and the touched rails' existing responsive behavior (`hidden lg:block`, the 2/3-column Library grid) is unchanged. Verified via the updated component suites + typecheck + lint + `next build` rather than a live browser pass — the standing worktree shared-dir/CORS constraint blocks a clean `preview_start`, matching every prior scene-surface entry above.
- **Follow-up correction — the Scenario hero's click-to-open card was still stat-less [done]:** the Library rows/`CharacterCard` and the carousel's `CastStats` flyout were fixed above, but the card those surfaces actually **open on click** — `CharacterProfileModal` — took no `statDefs`/`statValues` at all, so a player clicking into a character from the Scenario hero still saw no stats. Fix: `CharacterProfileModal` gained optional `statDefs`/`statValues` props + a new `ProfileStats` section (same bordered-manuscript-box treatment as `ProfileSection`, public stats only, real persisted value else schema default) rendered beneath Background; `LibraryView` wires `lib.statDefs`/`lib.statsByCharId[lib.profileChar.id]` into it. The story player's own `CharacterProfileModal` call is untouched (its in-scene stats already live in `CharacterDossier`). Tests (+3): no-Stats-section-when-omitted, real-value-over-default + non-public-omitted, default-fallback. Validation: frontend **402 vitest** (+3) + typecheck clean + lint (0 errors, same pre-existing warnings) + `next build` clean; backend unaffected (no backend changes).
- **Follow-up fix #2 — the carousel's own "Statistics" flyout was still empty for real stat schemas [done]:** user report: the "Statistics" panel on the right side of a cast card (the hero's per-card arrow toggle, `CastStats` in `ScenarioCarousel.tsx`) showed nothing, while the Library's `CharacterCard` correctly showed stat rows — both read the same `lib.statDefs`/`lib.statsByCharId`, so the divergence pointed at `CastStats` itself. Confirmed via the live backend (`GET /api/storylines/embergate/stats`): every real stat def has `appliesTo: ["character"]` — a **node-type tag**, not a per-character id list (the backend model's own default, `applies_to` on `StatDefinition`). `CastStats` was the only stat consumer in the codebase that additionally filtered `d.appliesTo.includes(c.id)`, so a real character id (`"maerin"`) never matched the literal string `"character"` and every stat with a non-empty `appliesTo` — i.e. every stat created through the normal editor — was silently dropped, while `CharacterCard`/`CastRail`/`DirectorRail`/`CharacterProfileModal` (which only ever filtered on `visibility === "public"`) rendered fine. Fix: dropped the `appliesTo` check from `CastStats`, matching every other consumer. Regression test added: a def shaped exactly like the real backend default (`appliesTo: ["character"]`) now asserts it renders in the panel — this test fails without the fix. Validation: frontend **403 vitest** (+1) + typecheck clean + lint (0 errors, same pre-existing warnings); backend unaffected (no backend changes, this was a pure frontend filter bug). No live-browser pass — an existing dev server already occupies the fixed preview port and cannot be safely killed sight-unseen (may be the user's own session); verified instead via direct backend API inspection of the real storyline's stat defs (`appliesTo: ["character"]` confirmed) plus the new regression test.
- **Follow-up — narrowed scope: stats removed from `CharacterCard` and `CharacterProfileModal` at the user's request [done]:** the user decided per-character stats should live **only** in the Scenario hero's `CastStats` flyout, not on the Library's "Characters" column cards or the character-profile popup. Reverted: `CharacterCard` lost its `CardStats` sub-component + `statDefs`/`statValues` props (and the now-unused threading through `CharacterColumn`/`LibraryColumns`); `CharacterProfileModal` lost its `ProfileStats` section + `statDefs`/`statValues` props (and `LibraryView`'s wiring into it). `useLibraryState`'s `statDefs`/`statsByCharId` are untouched — still needed by `ScenarioCarousel`. Removed the now-obsolete stat-display tests from `CharacterCard.test.tsx`/`CharacterProfileModal.test.tsx` (added a small "renders no Stats section" regression test to the latter to lock in the removal). Validation: frontend **397 vitest** (−6, tests removed not broken) + typecheck clean + lint (0 errors, same pre-existing warnings) + `next build` clean.

### Per-storyline / per-scenario writing prompts + Options Prompts tab (done; `worktree-feat+per-story-prompts`; plan: `docs/plans/per-storyline-scenario-prompts.md`)
User ask: adjust the writing-agent system prompts that shape a scene's tone/phrasing + how the bot progresses the story, editable **per storyline** (an options button next to the Storyline selector) and **overridable per scenario**, plus an Options tab that shows/edits **all** prompts in a structured, sub-tabbed way. Decisions (with the user): overrides are **storyline-level, overridable per-scenario** (resolution chain `registry default → global (Options) → storyline → scenario`, last non-blank wins); coverage is the **four core writing agents** only (Character · Narrator · Director · Planner). Seven phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — prompt registry + resolver [done]:** new `agents/prompt_registry.py` — `PromptSpec` + `PROMPT_REGISTRY` (7 keys: `character.output_contract`, `narrator.system`/`system_long`, `director.who_is_up`/`rerank`/`branch`, `planner.system`) holds the default text (moved verbatim from the agent constants — no wording drift) + `default()`/`keys()`/`resolve_prompts(*layers)`. The four agents source their defaults from it. Tests (+7): registry completeness, no-drift, layering, blank/None/unknown-key handling.
- **P2 — `prompt_overrides` on Storyline + Scenario [done]:** nullable JSON column ({key→text}) on both tables/models (Alembic `e5f6a7b8c9d0`, JSONB on Postgres, NULL read as {}, reconciler-self-healing); `promptOverrides` on both Base/Update/Read schemas (Reads coerce NULL→{}); wired into both `create_*` (updates already generic). No new storyline/scenario endpoints — persisted via the existing PATCH. Tests (+4).
- **P3 — resolution threads through the turn loop [done]:** `settings_store` `prompts` namespace (`get/set_prompts_overrides`, registry-key gated); `assembler.assemble_context` folds `global → storyline → scenario` into `TurnContext.prompts`; the four writing agents read `ctx.prompts.get(key, default)`. Tests (+7): three-layer fold + per-agent threading + default fallback.
- **P4 — Options prompts endpoint [done]:** `PromptSpecRead`/`PromptsConfigRead{catalog,overrides}`/`PromptsConfigUpdate`; `prompts` on `SettingsRead`; `GET /options` includes the catalog+overrides; `PATCH /options/prompts` (blank clears a key, unknown keys ignored). Tests (+3).
- **P5 — shared editor + Options Prompts tab [done]:** FE types (`PromptSpec`/`PromptsConfig`/`PromptsConfigUpdate`; `prompts` on `AppSettings`; `promptOverrides` on Storyline/Scenario) + `updatePromptsConfig` + `savePrompts`. New `PromptOverridesEditor` (per-agent upper sub-tabs, prefill override-or-baseline, reset-to-default, overridden badge, contract-tag warning, emits only overridden keys). New `PromptsTab` (global; merge-patch clears removed keys), registered in `OptionsView`. Tests (+ editor/tab/mocks).
- **P6 — storyline gear modal + per-scenario override [done]:** `StorylineMenu` per-row **gear** (`onConfigurePrompts`); generic `PromptOverridesModal` (fetches catalog+global, baseline = global under any passed layer); `LibraryView`+`useLibraryState` wire the per-storyline modal (`updateStoryline({promptOverrides})` + local mirror); `EntityModal` scenario editor gains a "⚙ Writing prompts" button staging `_promptOverrides` into the draft (persisted on scene save). Tests (+ gear/modal/mock).
- **P7 — docs + validation + merge [done]:** api-contract, data-flow, design-system, component-map, structure, documentation, this entry.
- **Validation:** backend **649 pytest** (all green; ruff + mypy clean on touched files); frontend **vitest 379 passed** + typecheck clean + lint (0 errors; 4 pre-existing warnings in untouched files) + `next build` clean.
- **Flaky story-player test — fixed (was pre-existing):** `features/story-player/StoryPlayerView.test.tsx > "selecting a suggestion writes it into the composer"` failed intermittently (empty composer). Root cause: the test kept the **real** api for `useScenePlay`'s three on-mount async effects (`listPlaySessions`/`getScenarioRelationships` + `closePlaySession` on unmount), so they hit real `fetch` in jsdom and resolved/rejected at nondeterministic times, racing the click-driven `setComposer`. Fix: stub those three in the test's `vi.mock` with empty data (keeps the seed scene). Verified: file alone 8/8, the prior failing 3-file combo 3/3, full suite 4/4 (380 passed) — all green.
- **Deferred — live in-browser pass:** the standing worktree shared-dir/CORS constraint (+ a configured-LLM turn to see overridden prompts actually change generation) blocks a clean `preview_start`. Verified via the green suites + `next build` + structure: the editor is a native tablist + aria-labelled textareas (keyboard-operable, AA `--field`/`--card` tokens), the gear is a labelled icon-button, both modals reuse the accessible `Modal` focus-trap idiom. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Scene Presence & Director Actions — auto-remove a character who dies/leaves + manual control (done; `feat/scene-presence-actions`; plan: `docs/plans/scene-presence-actions.md`)
User report: a character who has **died or left keeps chatting**, because the planner picks speakers from the static cast with no notion of presence. Goal (settled with the user): **auto-detect** removal AND give the player **full manual control**, via a reusable **scene-action bus** (not a one-off). Decisions: **auto + undo** (engine acts, every change reversible); **general action bus** (`exit`/re-entry now, `end_scene`/`move_scene` later); **five distinct statuses** — `present` · `unconscious` (body present, dormant) · `departed` (soul gone, body remains) · `left` (walked out) · `dead` (terminal). Six phases, commit-per-phase, on a feature branch merged to `main`. **Architecture:** presence is the **fold of the session's `character_status_change` event log** (default `present`, latest-per-character) — no new table, no Redis, survives reload, rehydrates through the existing reducers.
- **P1 — event + durable presence resolution [done]:** new `character_status_change` story event (`base.EventType`, `PresenceStatus` literal, envelope data/event/union); `services/presence.py` (`STATUSES`, `SELECTABLE={present}`, `current_presence` fold, `vital_status_for`, `can_transition`); `CastMember.presence` (+`is_present`) stamped by the `assembler`. Tests (+11): envelope validate/reject, fold (default/latest-wins/per-session), selectability/terminal/normalize, health-floor trigger, assembler stamp.
- **P2 — planner present-only roster + director `exit` action [done]:** `planner_agent` builds its roster from **present** members only (a dead/departed one never appears) + an `exit` action (`status`) + prompt rule; fallback + empty-present guard; `turn_engine` handles the exit beat via the shared `_apply_presence_change` (emit + **mutate the in-memory cast** so it takes effect next beat), reflection targets present-only. Tests (+8).
- **P3 — deterministic vital trigger + self-declaration [done]:** `emission` recognizes a `presence_change` JSON block; `validator.validate_presence` (gated by `can_transition`); `turn_engine` applies it + auto-`unconscious` on health→floor; character output contract documents the optional `<type:presence_change>` block. Tests (+6).
- **P4 — manual presence endpoint + undo signal [done]:** `POST /play/{id}/presence` (`PresenceRequest`) persists a `character_status_change` (`auto=False`, not bound by `can_transition` — the player may resurrect), folds like an engine change. Tests (+5).
- **P5 — frontend presence state + cast rail + undo [done]:** `lib/events` event + `PresenceStatus`; `turn-stream.applyPresence` + rehydrate `presenceByChar`; `useScenePlay` state/routing (auto → Undo toast)/`setPresence`; `CastRail` present vs. out-of-scene groups + per-character presence `<select>`; `Toast` gained an optional action button. Tests (+9).
- **P6 — docs + validation + merge [done]:** api-contract (event + endpoint + presence semantics), data-flow (presence layer + detection paths), design-system (cast-rail states + toast action), component-map (CastRail/useScenePlay/Toast), structure (`presence.py`), documentation status, this entry.
- **Validation:** backend **628 pytest** (all green; ruff + mypy clean on touched files); frontend **368 vitest** (+9) + typecheck (clean) + lint (0 errors; 4 pre-existing warnings in untouched files) + `next build` clean. No Alembic migration — presence is event-log-derived (no schema change).
- **Deferred — live in-browser pass:** the cast-rail presence control + Undo toast need a configured-LLM turn (or a manual click) to exercise live, and the standing shared-dir/CORS constraint blocks a clean `preview_start`. Verified via the green suites + `next build` + structure: the presence `<select>` is a native `aria-label`led control (keyboard-operable, AA `--field`/`--card` tokens), the Toast action reuses the accessible dismiss-button idiom, non-present rows dim on existing tokens with no new motion. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free. `end_scene`/`move_scene` verbs are a deferred follow-up on the same bus.

### Persistent scene saving + conversation export (done; `feat/scene-saving-export`; plan: `docs/plans/scene-saving-and-export.md`)
User report: scenes weren't being saved (couldn't go back and review past conversations), and there was no way to export the full record of a conversation for debugging. Decisions (with the user): **persist the diagnostic trace server-side** (not client-only export) so a reopened scene's graph/RAG activity survives after the fact; reopening a scenario **resumes its most recent play-through** with EVERYTHING intact (turns, thoughts, graph/RAG, stats) and continues forward; export produces **both JSON and Markdown**. Five phases, commit-per-phase, on a feature branch merged to `main`.
- **P1 — persist the trace + session recency (backend) [done]:** new `TurnTrace` model/table (`turn_traces`, keyed by `(session_id, turn, n)`, `turn` = the turn's opening `user_turn` seq) — `turn_engine._Tracer` now writes each step **regardless of the opt-in streaming flag** (best-effort, rolled back on failure) and still streams only when requested; `PlaySession` gains `updated_at` (bumped each turn via `events_store.touch_session`) + `closed_at`. Alembic `c3d4e5f6a7b8` (chained after the Scene-Dialogue `b2c3d4e5f6a7`; backfills `updated_at = created_at`). Kept out of the `events` table so the `(session_id, seq)` invariant is untouched. Tests (+3): trace persistence + `touch_session` (events_store), always-persisted trace over a real turn (play_turn).
- **P2 — session history/list/close/export endpoints (backend) [done]:** `GET /play/{id}/sessions` (resume list w/ `turnCount`+`preview`), `GET …/sessions/{id}` (full history: events incl. hidden thoughts + `user_turn`, plus the persisted traces, in wire-envelope shape), `POST …/sessions/{id}/close` (save-on-close), `GET …/sessions/{id}/export?format=json|md` (attachment). New `services/session_export` renders JSON + Markdown from **one** turn grouping (player line → beats → the turn's graph/RAG trace). Schemas in `schemas/play.py`; query helpers in `events_store`. Tests (+7): list/history/close/export json+md/format-422/404s.
- **P3 — resume + save-on-close (frontend) [done]:** `turn-stream.rehydrateFromHistory` replays persisted rows through the **existing** reducers (`mergeFrame`/`applyStat*`/`foldTrace`; a persisted `user_turn` → a player beat; stale `branch_choices` skipped) so reload reads byte-identically to live play; `useScenePlay` loads the latest session on open (`listPlaySessions`→`getSessionHistory`), rehydrates transcript/thoughts/stats/trace, continues on that `sessionId`, and fires `closePlaySession` (`navigator.sendBeacon`) on unmount/`beforeunload`. Reactive `sessionId` exposed. API client + wire types (`SessionSummary`/`PersistedEvent`/`PersistedTrace`/`SessionHistory`). Tests (+5): rehydration reducer (fold + skip stale branch) and hook resume/no-session/close-on-unmount.
- **P4 — Export control (frontend) [done]:** new `ExportMenu` (keyboard-operable menu: Markdown / JSON; `aria-haspopup="menu"`, `role="menuitem"`, outside-click + Escape close; disabled until a session exists) placed **immediately left of `ThemeSwitcher`** in `SceneHeader`; `StoryPlayerView.onExport` downloads `exportSessionUrl(...)`. Tests (+6): ExportMenu (open/select/disabled/Escape), SceneHeader (left-of-theme placement, disabled-until-session, omitted-without-handler).
- **P5 — docs + validation + merge [done]:** api-contract (4 endpoints + trace-now-persisted), data-flow (persistence/resume/export section + stale-note fix), component-map (`ExportMenu` + resume note), structure (`turn_trace`/`session_export`/`turn_traces`), documentation status, this entry. Merged after `feat/scene-dialogue-updates` (resolved the `guidance`/`outcome` turn-request overlap and re-chained the Alembic revision that had collided on `b2c3d4e5f6a7`).
- **Validation:** backend **pytest** all green post-merge (ruff + mypy clean on touched files); frontend **vitest** + typecheck + lint (0 errors) + `next build` clean.
- **Deferred — live in-browser a11y/responsive pass:** the standing shared-dir/CORS + full-stack constraint (dev server on 3346; backend CORS allows only that origin; the screenshot tool is broken) blocks a clean `preview_start` from this environment, and exercising a real resumed scene needs the LLM turn loop. Verified via the green suites + `next build` + structure: `ExportMenu` reuses the header's AA-compliant Inspector-button tokens, is a labeled menu with Escape/outside-click close and a disabled state, and sits in the existing header flex row (no new breakpoint risk). Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.
- **Post-merge fix — DB drift on non-null columns [done]:** after the merge the running backend 500'd (`column scenarios.max_turns does not exist`). Root cause was pre-existing and systemic, not this feature: the live Postgres `alembic_version` was stuck at **baseline** (`b23834895e1c`) — every later migration had drifted in via `create_all` + `bootstrap._reconcile_additive_columns`, which only self-heals **nullable** columns. So the three NOT NULL columns added by the two concurrent features (`scenarios.max_turns`/`suggestions_count` from Scene-Dialogue, `play_sessions.updated_at` from this plan) were silently skipped, and `alembic upgrade head` couldn't recover (it replays old additive migrations that collide with reconciler-added columns and aborts best-effort). Fix: (1) **hardened the reconciler** to also add a non-null column that carries a `server_default` (`ADD COLUMN … NOT NULL DEFAULT <sd>`, backfilling existing rows); (2) gave `PlaySession.updated_at` a `server_default=func.now()` (model + migration parity) so it joins that self-healing set; (3) repaired the live DB (added the 3 columns, backfilled `updated_at` from `created_at`, **stamped Alembic to head** so future migrations apply cleanly from a real head). Tests: +1 bootstrap (`test_reconcile_adds_nonnullable_column_with_server_default`); backend **591 pytest** green.

### Scene Dialogue System — turn limit, follow-up suggestions, guided selection, deeper thinking, composer controls (done; `feat/scene-dialogue-updates`; plan: `docs/plans/scene-dialogue-system-updates.md`)
User report on five scene-dialogue behaviors: (1) exchanges run on indefinitely (want a configurable max, default 5); (2) follow-up scenario suggestions "used to work but don't" (want a configurable 0–4 count, from the most recent turn, 4 → 2×2 grid); (3) selecting a suggestion copy/pastes its text as-is (want it to *guide* open-endedly, not dictate); (4) the controls belong as dropdowns left of the composer input, persisted per scene; (5) the character "thinking" phase is one thin line (want a fuller in-voice paragraph of real deliberation before speaking). Six phases, commit-per-phase, in a worktree merged to `main`. Decisions (with the user): controls **persist per-scene** (`Scenario` fields + migration); **"a turn" = character replies per player message** (bounds the `run_turn` beat loop; planner may still end earlier); default suggestions **4** (2×2 grid).
- **P1 — per-scene `max_turns`(5) + `suggestions_count`(4) [done]:** new non-null `Scenario` columns (+ Alembic `b2c3d4e5f6a7`, server defaults), schema clamps (`max_turns ≥ 1`, `0 ≤ suggestions_count ≤ 4`), `crud.create_scenario` wiring (`update_scenario` already generic). `turn_engine.run_turn` tracks a **character-speak counter** (seeded with puppet performances) and ends the loop at `scenario.max_turns` — a hard ceiling, planner-`end` still fires earlier; `2*cast+6`/`turn_max_beats` stays a secondary runaway backstop; a "Reached the scene's turn limit" trace fires. Tests (+5): scenario defaults + roundtrip + clamp-reject; a `max_turns=2` scene stops after 2 character beats.
- **P2 — count-driven suggestions from the most recent turn [done]:** `director_agent.propose_branches(count=…)` returns `[]` for `0` (no LLM call), caps at `min(count, 4)`, and anchors its prompt on the **most recent character line** (`_latest_line`, fallback last non-empty beat) instead of the whole transcript. `turn_engine` replaces the `if needs_branch:` gate with `if scenario.suggestions_count > 0:` (always propose at end of turn) — `needsBranch` now only colors the trace copy. Tests (+5): count=0 no-call, count caps, latest-line anchor; suggestions emit regardless of `needsBranch`, none when count 0.
- **P3 — guided (not dictated) selection [done]:** `TurnRequest.guidance` + `TurnContext.guidance`; `turn_engine` sets `ctx.guidance` from the request and emits a "Steering toward your choice" trace; `planner_agent` + `character_turn_agent` fold the steer into their prompts as an **open-ended nudge** ("honor that general direction … original, unscripted dialogue … do not quote/restate"). The legacy `outcome` play-out stays for back-compat but the frontend no longer sends it. Tests (+4): guidance reaches planner + character prompts; a guidance turn injects the steer, traces it, and never reproduces it verbatim as a beat.
- **P4 — deeper in-voice thinking [done]:** `character_turn_agent.TURN_EFFORT` **LOW → MEDIUM** (256 → 512 thinking tokens); the `<thinking>` contract now asks for a short in-voice paragraph (~3–5 sentences) of genuine deliberation in the character's own terminology (not a clinical analysis), and the disposition tail no longer clips it to "a few words" (it builds on the stance). Parsing/emission already pass full multi-sentence thoughts through unchanged. Tests (+1 net, 2 updated): contract asks for a paragraph + `TURN_EFFORT == MEDIUM`; disposition builds-not-clips.
- **P5 — composer dropdowns + guided `choose` + 2×2 grid [done]:** new `components/ui/SceneControlSelect` (compact labeled native `<select>`, `aria-label`led); `Composer` gains "Max turns" (1–10) + "Suggestions" (0–4) dropdowns **left of the input** in a `flex-wrap` row; `useScenePlay` holds the two values (init from the scenario), setters **PATCH the scenario** (`updateScenario`), and `choose` sends `guidance` (the suggestion text also enters the chat as the player input); `TranscriptBeat.BranchChoices` lays 3–4 choices in a 2-column grid (4 → 2×2). Types/contract: `Scenario.maxTurns`/`suggestionsCount`, `TurnRequestBody.guidance`. Tests (+8): `SceneControlSelect` (3), `Composer` (3), `StoryPlayerView` guided-select + persist-PATCH + 2×2 grid (2).
- **P6 — docs + validation + merge [done]:** api-contract (per-scenario controls + `guidance` + count-driven `branch_choices` from the latest line), data-flow (bounded loop, suggestion stage, guided-selection path, thinking depth, composer controls), design-system (`SceneControlSelect` + composer controls + 2×2 grid), component-map (new primitive + Composer/BranchChoices rows), documentation status, this entry.
- **Validation:** backend **580 pytest** (all green; ruff + mypy clean on touched files); frontend **337 vitest** (+8) + typecheck (clean) + lint (0 errors; 4 pre-existing warnings in untouched files) + `next build` clean.
- **Deferred — live in-browser pass:** the standing shared-dir/CORS constraint (dev server on 3346; backend CORS allows only that origin) + the need for a configured-LLM turn to exercise suggestions/guidance/thinking blocks a clean `preview_start` from the worktree, matching every prior play-experience entry. Verified via the green suites + `next build` + structure: the controls are native `aria-label`led `<select>`s (keyboard-operable, AA field tokens) in a `flex-wrap` row that stays reachable at 320/375px; the 2×2 grid reuses the existing accessible choice-row buttons. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Scene Dialogue System — refinements: situation-based suggestions, select-to-composer, narration-aware cap, combined think/speak + bold quotes (done; `feat/scene-dialogue-refinements`; plan: `docs/plans/scene-dialogue-refinements.md`)
System feedback on the shipped scene-dialogue features, four requests: (1) the "choose a path" suggestions should be **situation-based** (what happens next from a general, story-wide perspective — not a character's next spoken line) and **match the tone/pace** the player has written; (2) selecting a suggestion should **write its text into the composer** for review/editing instead of auto-sending; (3) the `max_turns` ceiling must **count narration too**, not just dialogue; (4) **formatting** — merge a character's thinking + speaking into one bubble at the same text size, and **bold any quoted (`"…"`) text** while keeping the quotes. Five phases, commit-per-phase, in a worktree merged to `main`. Decision: request #2 supersedes the prior round's open-ended `guidance` steer (the chosen text now *is* the turn), so that plumbing is **retired end-to-end**; the legacy `outcome` field is orthogonal and left untouched.
- **P1 — situation-based, tone-matched suggestions [done]:** `director_agent._BRANCH_SYSTEM` rewritten to request situation-based moves from a general perspective (never a single character's voice); new `_player_voice(ctx, turn_beats)` samples the player's own recent lines (committed history + this turn, deduped, oldest→newest) and threads them into the prompt as the tone/pace to match; `_latest_line` stays as the "what just happened" anchor. Tests (+3, 1 refactored): situation-based framing in the system prompt, player-voice sampled (never character lines), anchor keeps non-latest character beats out; `test_play_turn` branch-routing marker updated to the new wording.
- **P2 — `max_turns` counts narration [done]:** `turn_engine.run_turn` renames `char_beats → scene_beats`, seeds it with the narrated open + puppet performances, and increments it on **every** beat (mid-turn narrator interstitials included) so a hard cap of N is never exceeded (the trace copy reads "N beat(s)"). Tests (+2, 1 replaced): a cold-open turn with `max_turns=2` runs one reply (the open ate a slot); a mid-turn narrate beat also consumes budget.
- **P3 — select-to-composer + retire the guidance steer [done]:** frontend `useScenePlay.choose` now `setComposer(text)` (no submit, no guidance) and `submit` drops the `guidance` param; `StoryPlayerView` moves focus to the composer after a pick (new `Composer` `inputRef`); `TurnRequestBody.guidance` removed. Backend guidance plumbing removed: `TurnRequest.guidance`, `TurnContext.guidance`, the "Steering toward your choice" trace, and the planner + character prompt tails. Tests: `StoryPlayerView` select-writes-to-composer (+ no-send + focus) replaces the old guided-submit test; obsolete backend guidance tests removed (play_turn, planner, character_turn).
- **P4 — single think+speak bubble + bold quotes [done]:** new `components/ui/QuotedText` primitive bolds any straight-/curly-double-quoted run while keeping the quotes; `TranscriptBeat.CharacterMessage` folds thought + speech into ONE bubble at the **same 15.5px size** (thought muted/italic on top, spoken line below a `--hair` rule), and `NarratorCard`/`PlayerMessage`/`CharacterMessage` all render prose through `QuotedText`. Tests (+6): `QuotedText` (4) + combined-bubble-same-size + bold-quote (2); the existing thought/speech-order tests still pass.
- **P5 — docs + validation + merge [done]:** api-contract (drop `guidance`; `maxTurns` counts narration; situation-based/tone-matched suggestions + select-to-composer), data-flow (same, + one-bubble/`QuotedText` rendering, guidance retired), design-system (event-render table: combined bubble same-size + bold quotes), component-map (`QuotedText` primitive + `TranscriptBeat`/`Composer` notes), this entry.
- **Validation:** backend **591 pytest** (all green); frontend **355 vitest** (+11) + typecheck (clean); `next build` after merge.
- **Deferred — live in-browser pass:** same standing worktree shared-dir/CORS + configured-LLM constraint as prior play-experience entries — a live streamed-thought turn (with an embedded quote) can't be driven cleanly from the worktree, and the screenshot tool is unreliable there. Verified instead via deterministic component tests that assert the exact DOM/classes (one bubble container, identical 15.5px size for thought + speech, `<strong>` around quoted spans with quotes preserved, focus-to-composer on select) + typecheck + `next build`. A11y: thought text grew 13→15.5px (better legibility) on the same `--ink-soft`/`--card` tokens, `--hair` divider, no new motion, unchanged reflow. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Narration-driven progression + scene configuration menu (done; `feat/scene-config-and-narration`; plan: `docs/plans/scene-config-and-narration.md`)
Two-part scene-play feedback: **(A) behavior** — the narrator lingered on setting and characters over-talked (spoke even when a beat called for action). The narrator should *progress the story to the next beat* (narrate what each character is doing, carry an action through its consequence), and dialogue should follow once the scene has moved. **(B) feature** — turn the two composer controls into a full **configuration menu** (Max turns · Max suggestions · **Number of beats** 5–100) with a **live approximate token estimate** for the chosen beats. "Number of beats" is the context-window depth the character conditions on (the old hardcoded `_TRANSCRIPT_MAX_BEATS = 14`). Four phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — narration drives progression; characters act/think over talk [done]:** rewrote the three turn-loop agent prompts. `narrator_agent` (both `_SYSTEM`/`_SYSTEM_LONG`) now PROGRESSES to the next beat — narrate what characters are DOING, carry an action to its consequence, lead with action, don't dwell on scenery. `planner_agent` makes narrate-to-progress the DEFAULT (esp. in action) and a character speaks only after the scene moves + has a real POV reaction (not every beat → over-talking). `character_turn_agent` keeps `<thinking>` always-required but makes `character_dialogue` OPTIONAL (act or think in action moments). Tests (+5): narrator/planner/character prompt intent; emission parses action-only + thinking-only beats with no synthesized dialogue.
- **P2 — per-scene `context_beats` (5–100) [done]:** mirrors the `max_turns`/`suggestions_count` plumbing — `Scenario.context_beats` column (default 14, server default) + alembic `d4e5f6a7b8c9`, schema clamps (`ge=5, le=100`), crud wiring. `assembler` fetches exactly `context_beats` recent beats (`buffer.recent_turns(limit=…)`), exposes it on `TurnContext`, and the character transcript slices to it (was a fixed 14). Bumped `turn_buffer_size` 12 → 100 so the buffer retains enough for a high setting. Tests: scenario defaults/roundtrip/clamp, buffer retention + fetch-limit, assembler depth + clamp, transcript window.
- **P3 — scene config menu + live token estimate [done]:** new `components/feature/SceneConfigMenu.tsx` (gear button → `role=dialog` popover, focus moved in, Esc + outside-click close) replaces the two inline `SceneControlSelect`s in `Composer`; holds Max turns + Suggestions dropdowns and a "Number of beats" 5–100 range slider with a live `≈ N tokens` readout (`lib/contextBudget.estimateBeatsTokens` = beats × `AVG_CHARS_PER_BEAT` ÷ `CHARS_PER_TOKEN`). `useScenePlay` holds `contextBeats` (persisted via `updateScenario`); `Scenario` type + `StoryPlayerView` thread it through. Tests (+11): SceneConfigMenu open/close/handlers/estimate, contextBudget beats estimate, Composer menu placement, StoryPlayerView beats-persist.
- **P4 — docs + validation + merge [done]:** api-contract (three per-scene controls + `contextBeats`), data-flow (config menu + context window + narration-progression/optional-dialogue behavior), design-system (config-menu popover), component-map (`SceneConfigMenu` + `estimateBeatsTokens`), this entry.
- **Validation:** backend **600 pytest** (all green); frontend **359 vitest** (+16) + typecheck (clean) + lint (0 errors; 4 pre-existing warnings in untouched files); `next build` after merge.
- **Deferred — live in-browser pass:** the behavior half is prompt-only (validated by prompt-intent assertions + the emission parser already supporting action-only/thinking-only beats; live model behavior can't be unit-tested). The menu is verified via deterministic DOM tests (popover `role=dialog`, combobox/slider roles + aria-labels, handlers firing with exact numbers, token text matching `estimateBeatsTokens`, Esc/outside-click close, closed-by-default) + typecheck + `next build`; the worktree shared-dir/CORS + broken-screenshot constraint blocks a clean live `preview_start`. A11y: native button (`aria-haspopup`/`aria-expanded`) + `role=dialog` with focus moved in, native `<select>`/`<input type=range>` with aria-labels, established `--field`/`--card`/`--ink-soft` contrast tokens, one compact button improves the narrow-viewport row over the prior two dropdowns. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Character Voice & Tone — single-response, situation-authentic fix (done; `worktree-voice-tone-single-response`; plan: `docs/plans/character-voice-tone-single-response-fix.md`)
User report: generated voice samples showed a full back-and-forth conversation instead of one response, and read as a generic voice reused across situations rather than reacting to what actually happened in each prompt. Root cause: the prior "dialogue-depth" rewrite (`docs/plans/character-voice-tone-dialogue-depth.md`, `[Voice Tone Dialogue Depth]`) had explicitly asked `_VOICE_SYSTEM` for a "full back-and-forth dialogue exchange... three to five lines, alternating speakers" — the opposite of what the user wanted. **Prompt-content-only fix** — no schema/storage/endpoint/UI-plumbing change (the `{situation, sample}` shape, `POST /characters/voice-samples`, and the turn-loop injection all stay as-is). Three phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — backend prompt + renderer/schema docstrings [done]:** `_VOICE_SYSTEM` (`character_agent.py`) keeps the "reason through the character's distinctive voice first" framing but rewrites the output contract: each `situation` is a previous situation the character was confronted with, each `sample` is the character's **single** in-voice response — explicitly forbidding a back-and-forth exchange or multiple alternating speakers — that reacts to the *specifics* of its situation rather than restating a generic personality blurb, with register (calm/pressured/challenged/warm) varying across the 2-3 pairs. Module/function docstrings, `_VOICE_SAMPLES_CAP` comment, `assembler._format_voice_samples`, `rag/entries._voice_samples_text`, and the `VoiceSample` schema docstring updated to describe a single response instead of a dialogue exchange (render format unchanged — existing single-line test fixtures needed no changes). Tests: existing 44 assembler/rag/character-agent/character-turn-agent tests pass unchanged (docstring/prompt-content-only).
- **P2 — editor copy [done]:** `VoiceSamplesEditor.tsx` (doc comment, empty-state hint, situation/response placeholders) and the `CharacterModal.tsx` Voice & tone description reverted from "real back-and-forth exchange" wording to "a past situation and this character's single response to it, in their own voice — not a back-and-forth."
- **P3 — docs + validation + merge [done]:** api-contract.md + data-flow.md corrected from "dialogue-exchange" to "single in-voice response, reacting to that situation's specifics"; this entry.
- **Validation:** backend pytest (targeted 44 + full suite), ruff + mypy clean on touched files. Frontend `npm test -- VoiceSamplesEditor CharacterModal` (12 passed) + typecheck (0 errors) + lint (0 errors; 4 pre-existing warnings in untouched files) + `next build` clean.
- **Supersedes** the prior "dialogue-depth" prompt rewrite (`character-voice-tone-dialogue-depth`): that plan's premise (samples should be a multi-line exchange) was itself the reported defect; this plan restores single-response output while keeping its "reason through the voice first" improvement and adding the situation-authenticity requirement the user asked for.

### Play-experience fixes — narrator parsing, combined thought+speech, live dossier stats, richer graph trace (done; `worktree-play-experience-fixes`; plan: `docs/plans/play-experience-narrator-thoughts-stats-graph.md`)
Four in-scene defects from user feedback, one plan, commit-per-phase, in a worktree merged to `main`. Scoping resolved with the user: Turn Inspector "Graph" → **enrich the trace detail** (no viz widget); stats bug → the **character dossier** surface.
- **P1 — narrator output sanitizer (backend) [done]:** the narrator is the one freeform-prose agent (no `<type:>`/`<thinking>` markers), so a reasoning model leaked its chain-of-thought + harmony channel tokens (`<channel|>`, `*Check:*`/`*Revised:*`) into the beat. New shared `_common.strip_reasoning` (drop `<think>` blocks → keep text after the last channel marker → scrub residual harmony control tokens; clean prose passes through) applied in `narrator_agent.interstitial`. Tests: 6 focused sanitizer cases (incl. the exact reported leak) + 1 end-to-end narrator case.
- **P2 — combine thinking + speech into one beat (frontend) [done]:** `internal_thought` no longer renders as its own bubble — `mergeFrame`/`isOpenCharBeat` fold a speaker's thought → action → dialogue into ONE `char` beat (`SceneMessage.thought`); `CharacterMessage` renders the muted "thinks" line between the name and the spoken bubble; `ThoughtBubble` + the `"thought"` kind removed. Tests: turn-stream fold cases + TranscriptBeat render (thought-before-speech order, thought-only, speech-only).
- **P3 — live per-character dossier stats (frontend) [done]:** root cause — stat state was a single global list that ignored each `state_update`'s `characterId`, and the dossier's sliders hardcoded `def.default`. Added `applyStatByChar` (per-`characterId` buckets) + `statsByChar` in `useScenePlay`; made `StatSchema`/`StatSlider`/`bandLabelFor` value-aware (live value → thumb/readout/band, fallback to default); `CharacterDossier` takes `stats` and `StoryPlayerView` threads `statsByChar[profileChar.id]`. Director rail's global Scene-state chips unchanged. Tests: `applyStatByChar` bucketing + `CharacterDossier` live band/readout.
- **P4 — enrich Graph trace detail (backend) [done]:** the green Graph steps reported only a count. `commit` now lists each durable `Consequence.summary` (`data.changes[]`); `relationships.ensure_seeded` returns one `"A <type> B — reason"` summary per seeded edge and the first-turn `relationships` step lists them (`data.edges[]`). Tests: `ensure_seeded` returns summaries; play-turn commit detail names the change.
- **P5 — docs + validation + merge [done]:** data-flow (sanitized narration, folded thought+speech, two-way stat state, Graph-trace detail), api-contract (narration sanitized, inline thinking, Graph step detail), design-system (folded beat, value-aware dossier sliders), documentation status, this entry.
- **Follow-up — sanitizer moved to the central choke point (backend) [done]:** the same reasoning/channel leak then surfaced on the **character** path (chain-of-thought + `<channel|>` bleeding into the spoken bubble), because character output is parsed by `emission.parse_emission`, not the narrator scrub. `strip_reasoning` now runs inside **`services.llm.chat_complete`** — the one generation call every agent shares — so narrator prose, character emission, and authoring JSON are all sanitized before return (the app's `<speaker:>`/`<type:>`/`<thinking>` markers are preserved). The narrator's now-redundant per-call scrub was removed. Docs (data-flow, api-contract) updated to describe central sanitization. Tests (+5): `test_llm.py` (channel leak, harmony final-channel, clean pass-through, app-marker preservation) + a play-turn character-emission leak-stripped case.
- **Validation:** backend **566 pytest** (+12 total; ruff + mypy clean on touched files); frontend **329 vitest** (+7) + typecheck + lint (0 errors; 4 pre-existing warnings in untouched files) + `next build` clean.
- **Deferred — live in-browser pass:** the standing shared-dir/CORS constraint (dev server on 3346; backend CORS allows only that origin) blocks a clean `preview_start` from the worktree, and the folded thought + live dossier stats need a configured-LLM turn to exercise. Verified via the green suites + `next build` + structure: the muted thinking line reuses the AA-compliant `text-ink-soft` token; the dossier sliders stay read-only labeled visuals; the narrator scrub is pure-string with unit coverage. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free.

### Build extraction respects the author's classification — stop inventing entities (done; `worktree-build-extract-classify`; plan: `docs/plans/build-extraction-respect-classification.md`)
User report: **Build the whole world** *invented* characters/settings by "taking context from documents and expanding them," and mined even the docs the author had explicitly classified. Desired: only **Uncategorized** docs get mined; classified Character/Setting docs are used as-is; and extraction must be strict — a **NAMED** character/setting must genuinely be present, else create nothing. Decisions (with the user): classified docs → **one entity, split only if clearly multiple** (never invent); **Other → lore/grounding only** (only Uncategorized is strictly mined). Three phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — strict, bucket-scoped extraction (backend) [done]:** `extract_agent` rewrote `_EXTRACT_SYSTEM` to **strict / named-only / no-invention** (explicitly named + genuinely profiled subjects only; forbids inventing/inferring/expanding from lore/history/rules/terms; a name mentioned in passing is not a subject; lore → empty) + a `kind` param (`character`/`setting`/`both`) that injects a bucket instruction and **clears the off-kind list**. `build_agent`: extraction now **respects the bucket** — character docs → named chars (fallback to **one** when unnamed/failed, so a classified doc is never lost), setting docs → named settings (same fallback), `uncategorized_docs` → strict both (**no** fallback — 0 is valid), `other_docs` → **lore/grounding only** (folded into `grounding`, never extracted). De-dup stays in document order. New `uncategorized_docs` request field + route passthrough. Tests (+6 net): kind scoping, strict lore→nothing, other-grounds-nothing, uncategorized-mined, uncategorized-lore→nothing, classified-fallback-to-one, unreadable-uncategorized-skipped.
- **P2 — frontend routes the four buckets [done]:** `useStorylineCreator.build` splits kept docs by `category` into `characterDocs`/`settingDocs`/`uncategorizedDocs`/`otherDocs` (previously `other` + `select` were bundled into `otherDocs`) and sends all four; `lib/api.BuildWorldBody.uncategorizedDocs`. Hook tests: categorized send asserts `uncategorizedDocs: []`, an 'other' doc → `otherDocs`, an Uncategorized doc → `uncategorizedDocs`.
- **P3 — docs + validation + merge [done]:** data-flow (per-bucket extraction policy), api-contract (`uncategorizedDocs` + bucket policy), documentation status, this entry.
- **Validation:** backend **554 pytest** (+6; ruff + mypy clean on touched files); frontend **322 vitest** + typecheck + lint (0 errors) + `next build` clean. (Two `CharacterModal`/`SettingModal` reveal tests intermittently time out under full-suite load — a pre-existing timer flake in the real-time-feedback tests, unrelated to this change; green on a clean re-run and in isolation.)
- **Supersedes** the prior "mine every doc for every subject" behavior (`world-build-multi-entity-extraction`): multi-subject mining is now scoped to Uncategorized docs only, and classified docs are one-each (split only when clearly multiple).

### Character Voice & Tone — dialogue-depth rewrite (done; `worktree-voice-tone-dialogue-depth`; plan: `docs/plans/character-voice-tone-dialogue-depth.md`)
User report: generated voice-sample pairs (`character_agent.propose_voice_samples`) felt "stilted" and "textbook" — the old `_VOICE_SYSTEM` prompt asked for a short situation label plus one or two flat sentences of reply, which read as a description of the voice rather than a demonstration of it. **Prompt-content-only rewrite** — no schema/storage/endpoint/UI-plumbing change (the `{situation, sample}` shape, `POST /characters/voice-samples`, and the turn-loop injection all stay as-is). Three phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — backend prompt + renderers [done]:** `_VOICE_SYSTEM` (`character_agent.py`) now instructs the model to first reason through the character's distinctive voice (diction, rhythm, tics, formality — grounded in background/personality/traits/speech), then write **2–3** (capped at 4) situation → **full back-and-forth dialogue-exchange** pairs — `situation` is a line/beat that prompts the character (often another character's dialogue), `sample` is 3–5 alternating lines in the character's voice, not one sentence. Reformatted the two render sites that assumed the old short-phrase shape (`assembler._format_voice_samples`, `rag/entries._voice_samples_text`) to a two-line "Prompt: ... / You: ..." block, and updated the `VoiceSample` schema docstring. Tests: updated `test_assembler.py`'s render-format assertion; confirmed parsing/capping/best-effort (`test_character_agent.py`) and turn-prompt pass-through (`test_character_turn_agent.py`) unaffected.
- **P2 — editor copy [done]:** `VoiceSamplesEditor.tsx` (doc comment, empty-state hint, situation/sample placeholders, textarea rows 2→4) and the `CharacterModal.tsx` section description updated so hand-authored samples follow the same exchange shape as generated ones.
- **P3 — docs + validation + merge [done]:** `api-contract.md` + `data-flow.md` updated to describe the dialogue-exchange shape; this entry.
- **Validation:** backend **548 pytest** (post-merge full suite; no new tests — one assertion updated for a prompt-content/render-format change), ruff + mypy clean on touched files. Frontend **321 vitest** green (incl. `VoiceSamplesEditor` + `CharacterModal` suites) + typecheck clean + lint (0 errors; 4 pre-existing warnings unrelated to this change) + `next build` clean.
- **Deferred — live in-browser check of the editor copy:** `CharacterModal` needs the backend to have an open storyline; the standing shared-dir/CORS constraint (dev server on 3346; backend CORS allows only that origin) blocks a clean `preview_start` from the worktree, matching the same deferral recorded for this component in the original Character Voice & Tone consistency plan. Verified via the passing component tests instead — purely static placeholder/description text, no new interactive behavior.

### Build "Reading Docs" hang + "invalid JSON" — parallel, fault-tolerant doc extraction (done; `worktree-build-extract-fix`; plan: `docs/plans/build-extract-stage-fix.md`)
User report: **Build the whole world** hangs on the "Reading Docs" step with no detailed status, then fails with only `"The model did not return valid JSON."`, suspected tied to 40+ uploaded files / parallelism. **Root cause (diagnosed):** the `extract` stage (`iter_build_world` → `_extract_roster`) ran one `extract_agent.extract_entities` LLM call **per doc in a sequential `for` loop**, emitted a **single** status for the whole loop, and let **any one** doc's malformed-JSON reply propagate and **abort the entire build**. So 40 docs = 40 serial reasoning-model calls with no per-doc output (the "stuck, no status"), and one bad reply out of 40 killed everything with a message naming no doc. It was the *absence* of parallelism + fault-tolerance in extraction (the drafting phase already had both), not the drafting parallelism. Two phases, commit-per-phase, in a worktree merged to `main`. **Decisions (with the user):** fix this first, ship on its own; the RAG-first redesign is a separate plan (`docs/plans/rag-first-ingestion.md`) with **ephemeral build-scoped namespace** + **native tool/function-calling** ReAct.
- **P1 — parallel, fault-tolerant, progress-reporting extraction [done]:** `extract_agent.extract_entities` takes an optional pre-resolved `conn` (`resolve_llm_or`) and runs at **`ReasoningEffort.LOW`** (segmentation — faster, far less JSON truncation). `build_agent`: the extract stage now runs per-doc extractions **concurrently** via `concurrency.imap_unordered` bounded by `authoringConcurrency` (conn pre-resolved once → workers never touch the Session), **failure-isolated** with a single retry then skip (`_extract_one`); results collected into index slots + de-duped in **document order** (`_dedup_roster`) so the roster is stable under out-of-order completion; a **per-doc `BuildStatusEvent(stage="extract", "Read k/N: <name>")`** streams progress and a closing status **names skipped docs** instead of aborting. Tests (+3): per-doc progress emitted, a never-parsing doc is skipped-not-fatal (+ named), a transient failure is retried once and salvaged; the budget test updated (extraction=LOW 256, drafts stay MEDIUM 512).
- **P2 — docs + follow-up plan + merge [done]:** data-flow (parallel + fault-tolerant + per-doc-progress extract stage), documentation status, this entry, and the new `docs/plans/rag-first-ingestion.md` capturing the RAG-first redesign decisions.
- **Validation:** backend **548 pytest** (+3; ruff + mypy clean on touched files). Frontend untouched (the per-doc messages ride the existing `buildStage` line; the `ProcessProgress` "extract" step is unchanged) → `npm test` N/A.
- **Deferred — the RAG-first ingestion + ReAct redesign** (`docs/plans/rag-first-ingestion.md`): ingest every uploaded doc into an **ephemeral build-scoped RAG namespace** *before* generation, then let the character/setting agents **query it on demand via native tool/function-calling** during their drafts (look up a location, species, or uncertain world-term instead of guessing), promoting the namespace to the real storyline on Create World. Not implemented here — the next plan.

### Real-time field editing feedback — live "which field now" across every agentic surface (done; `worktree-realtime-field-feedback`; plan: `docs/plans/realtime-field-editing-feedback.md`)
Whenever an agentic process authors a world/character/setting/scenario or generates voice/stats/prompts, the UI now shows **which field is being written at that exact moment**, where the process is, and any error. **Frontend-only** (no backend change: the world build already streams NDJSON with a `stage` vocabulary; the draft endpoints stay plain JSON). Decisions (with the user): **all agentic surfaces**; **choreographed reveal** for the all-at-once drafts; **top-right toast** for errors. Five phases, commit-per-phase, in a worktree merged to `main`.
- **P1 — shared primitives [done]:** `styles/themes.css` `veloraFieldPulse` keyframe + `.velora-field-active` utility (accent ring; reduced-motion leaves a static frame); `hooks/use-field-reveal.ts` (ordered field-by-field reveal — `activeKey`/`values`/`done`, reduced-motion/empty → at once); `components/ui/Toast.tsx` + `components/layout/ToastProvider.tsx` (`useToast`; portal, top-right, stacking, error=`alert`/info=`status`, `embMsg` entrance, no-op default) mounted in `AppShell`; `components/feature/ProcessProgress.tsx` (done/active/pending stepper + live "Now … · Next …"). Tests: `use-field-reveal` (6), `ToastProvider` (4), `ProcessProgress` (5).
- **P2 — world-build page [done]:** `useStorylineCreator` adds `activeField` (highlight-only sweep over `meta`/`primer` via `use-field-reveal`), `activeEntity` (drafting card), `buildStageKey`; build `error`/throw → error toast (values still set synchronously). `StorylineCreatorView` applies `.velora-field-active` to the field being written + a `ProcessProgress` stepper; `WorldBuildPanel` glows the active cast/setting card. Tests: WorldBuildPanel active card (+2), hook stage/highlight-clear + in-band error (+2), View error-toast integration (+1).
- **P3 — character create/edit [done]:** `useLibraryState.draftCharacter` choreographs the visible identity fields one at a time (goal/secret/color set at once), advancing `draftStage` identity → voice → stats; character-draft/voice/stats errors → toast. `CharacterModal` highlights the active field + a `ProcessProgress` stepper. Tests: hook `activeField`/`draftStage` lifecycle (+1), modal highlight+progress (+1); existing draft assertions now await the reveal.
- **P4 — setting + prompt/image [done]:** `draftSetting`/`draftScenario` choreograph their prose fields; a shared `revealPromptPair` reveals positive→negative for portrait/scene-art/scenario prompts; setting/scenario draft + prompt + render errors → toast. `SettingModal` highlight + `ProcessProgress`; `PortraitModal`/`SceneArtModal` take an `activeField` prop to highlight the prompt landing. Tests: `SceneArtModal` highlight (+3), setting draft lifecycle (+1), `SettingModal` highlight+progress (+1).
- **P5 — docs + validation + merge [done]:** design-system (field-active utility + toast + ProcessProgress), component-map (new components + `useFieldReveal`/`ToastProvider`), data-flow (presentational feedback note), documentation status, this entry.
- **Validation:** frontend **321 vitest** (+26) + typecheck + lint (0 errors; 1 pre-existing `loadInitial` exhaustive-deps warning) + `next build` clean. Backend **545 pytest** (untouched — confirmed no regression).
- **Deferred — live in-browser a11y/responsive pass:** the standing shared-dir/CORS + full-stack constraint (the user's dev server on 3346; backend CORS allows only that origin; the screenshot tool is broken) blocks a clean `preview_start` from the worktree, and the agentic flows need the LLM (and ComfyUI) stack to exercise live. Verified via the green suites + `next build` + structure: toasts are `role="alert"`/`status` live regions with a keyboard-operable dismiss; `ProcessProgress` is a labeled `group` with an `aria-live` status line; the `.velora-field-active` ring reuses the proven reduced-motion-safe `--glow-color` idiom (static frame under `prefers-reduced-motion`); the reveal collapses to instant under reduced motion; new fields reuse the AA-compliant `TextField`/`TextArea` primitives. Run the live keyboard + 320/375/768/1024 pass once the dir + stack are free. Scenario-draft field **highlight** (the glow ring) is not wired into `EntityModal`/`ScenarioForm` — only the sequential reveal + toast — an optional follow-up.

### Authoring parallelism — parallel world-build drafting + parallel RAG indexing (configurable) (done; `feat/authoring-parallelism`; plan: `docs/plans/authoring-parallelism.md`)
The "Build the whole world" flow drafted characters/settings one at a time and the RAG indexer embedded one at a time. Added **configurable** parallelism: individual characters/settings draft concurrently and RAG batch-embed concurrently, bounded by a new user setting; **images stay sequential** (single-GPU ComfyUI — untouched). Four phases, commit-per-phase, in a worktree merged to `main`. **Design (assumptions):** one knob (`authoringConcurrency`) governs both, homed on the **LLM** settings namespace (concurrency is an LLM-backend property; single-slot llama.cpp → 1, vLLM → higher); pre-resolve the LLM once so drafting workers never touch the request Session (`world_context`/`rag_block` are no-ops for the build's `None` storyline); concurrent embedding + lock-serialized Qdrant writes (safe with the in-memory test store); the frontend already places build events by `index` (`upsertAt`) so out-of-order emission needs no FE change.
- **P1 — the knob + `imap_unordered` [done]:** env `Settings.build_max_concurrency` (3) seeds a user-facing `authoringConcurrency` on the LLM config (schema Read/Update + `settings_store`, clamped ≥1), surfaced in Options › Language Models ("Max parallel authoring requests" + vLLM/single-slot hint). `concurrency.imap_unordered(thunks, max_workers)` — as-completed, index-tagged, bounded, failure-isolated; `≤1` runs inline in order. Tests: helper (index-tagged/inline/failure/empty/all-complete), options GET+PATCH+clamp, LanguageModelsTab hydrate/save.
- **P2 — parallel drafting [done]:** `draft_character`/`propose_voice_samples`/`draft_setting` take an optional pre-resolved `conn` (`_common.resolve_llm_or`); `iter_build_world` pre-resolves once, reads `authoringConcurrency`, and drafts characters then settings via `imap_unordered` — events stream out-of-order by `index`, final lists stay in extraction order, a per-entity failure is skipped best-effort, `1` → sequential. Tests: out-of-order tolerated, `1` sequential-in-order, failed draft skipped (still `done`), existing counts order-independent.
- **P3 — parallel RAG indexing [done]:** `indexer.index_many` (`_index_entry_locked`) embeds concurrently, serializes Qdrant writes under a lock; `iter_reindex_storyline` uses it (progress as each completes) with a `max_workers` the reindex route passes from the setting; `bulk_create_context_documents` embeds its corpus via `indexer.sync_context_documents`. Tests: `index_many` all-indexed + idempotent skip + no lost points; parallel reindex all-indexed with climbing counters; bulk context-doc commit indexes all (in-memory store).
- **P4 — docs + validation + merge [done]:** api-contract (LLM `authoringConcurrency` + parallel-reindex note), data-flow (parallel build + out-of-order events), rag.md (parallel batch indexing), workflow.md + `.env.example` (`BUILD_MAX_CONCURRENCY`), documentation status, this entry.
- **Validation:** backend **545 pytest** (+17; ruff + mypy clean on touched files); frontend **295 vitest** + typecheck + lint (0 errors) + `next build` clean.
- **Deferred:** the frontend commit still creates entities sequentially (avoids `_next_position` races / concurrent per-request graph+RAG writes); per-single-save inline `sync_*` stays inline (one embed each). Live in-browser a11y pass on the new Options field not run (standing shared-dir/CORS constraint) — it's a native numeric `TextField` mirroring the existing `ImageModelsTab` params pattern (labeled, keyboard-operable), verified via the component test + typecheck + build.

### Character Voice & Tone consistency (done; `feat/character-voice-tone`; plan: `docs/plans/character-voice-tone-consistency.md`)
Fixes character tone drift by attaching a **voice & tone profile** — a small list of **situation → sample-response** pairs — to every character, generated at creation (before stats), editable in the character menu, and injected into the live turn loop so both spoken lines and the hidden `<thinking>` step stay in voice. Six phases, commit-per-phase, in a worktree merged to `main`. **Design (assumptions):** store as a `JSONColumn` `list[dict]` mirroring `Setting.timeline`; a **dedicated** `propose_voice_samples` agent/endpoint (parallel to `propose_starting_stats`) rather than folding into `draft_character` (keeps the flat draft JSON stable + gives the editor a Redo button); target 3 (2–4) pairs; per-speaker samples injected in the **user-message HEAD** (not the byte-identical system prefix) so vLLM prefix caching holds.
- **P1 — Backend persistence [done]:** `Character.voice_samples` `JSONColumn` (nullable, `default=list`); `VoiceSample` schema + `CharacterBase/Update/Read` wiring (`None→[]` coercion, mirroring `SettingRead._coerce_timeline`); `create_character` serializes samples (`update_character` patches via `model_dump`); Alembic `a1b2c3d4e5f6` (chained on `f1a2b3c4d5e6`); RAG character entry body folds in the samples. Tests: schema shape + `None→[]`, CRUD roundtrip + default-empty, RAG body.
- **P2 — Turn-loop prompt injection [done]:** `CastMember.voice_samples` (assembler `_format_voice_samples` renders the pairs; empty when unauthored); `character_turn_agent._build_user_prompt` HEAD gains a "Voice samples — how you sound" block; the generic `<thinking>` contract rule nudges thinking into the same voice (character-agnostic → prefix stays cacheable). Tests: assembler render/omit; agent HEAD inject/omit; thinking-contract references voice. Play-turn pipeline unchanged.
- **P3 — Generation agent + endpoint + build wiring [done]:** `character_agent.propose_voice_samples` (2–4 pairs from the prose, best-effort → `[]`, capped at 4) + `VoiceSamplesRequest/Response` + `POST /characters/voice-samples`; `ProposedCharacter.voice_samples` generated per character in `build_agent.iter_build_world` **before** the schema-default stats. Tests: agent parse/cap/empty/best-effort-malformed; build proposal carries samples (mock-router branch added).
- **P4 — Frontend plumbing [done]:** `VoiceSample` type; `Character.voiceSamples` + `ProposedCharacter.voiceSamples`; `api.proposeVoiceSamples`; `Draft._voiceSamples` + `DEFAULT_DRAFTS`; `useLibraryState` (draftCharacter auto-generates voice **before** stats, best-effort; standalone `proposeVoiceSamples` + `generatingVoice`; `editCharacter` hydrates; submit sends trimmed `voiceSamples`); `storylineCreator.proposedToCharacterInput` maps them into the create body; shared `api-mock`. Tests: hook auto-gen/best-effort/on-demand/persist-trim/rehydrate; mapper; api-client shape.
- **P5 — Frontend UI [done]:** `VoiceSamplesEditor` (repeatable situation→response rows, `StatsEditor` pattern); a **Voice & tone** section in `CharacterModal` **above** Starting stats with a ❖ Propose/Redo button; a "N voice samples" indicator on proposed characters in `WorldBuildPanel`. Tests: editor add/edit/remove, modal section-above-stats + propose-fills-rows, panel indicator (existing stats-propose test scoped to the 2nd ❖ Propose).
- **P6 — Docs + validation + merge [done]:** api-contract (`voiceSamples` read/write + `/characters/voice-samples` + turn-injection note), data-flow (authoring flow + turn injection), documentation status, component-map (`CharacterModal` + new `VoiceSamplesEditor` row), this entry.
- **Validation:** backend **528 pytest** (+10; ruff + mypy clean on touched files); frontend **286 vitest** (+16) + typecheck + lint (0 errors) + `next build` clean.
- **Deferred — live in-browser a11y/responsive pass:** the Voice & tone editor lives inside `CharacterModal`, which needs the backend to open a storyline; the standing shared-dir/CORS constraint (the user's dev server on 3346; backend CORS allows only that origin) blocks a clean `preview_start` from the worktree, and the screenshot tool is broken in this env. Verified via the green component suites (which drive keyboard add/remove/edit + the section ordering + the propose interaction in jsdom) + `next build` + structural reasoning: the editor reuses `StatsEditor`'s accessible pattern (`role="group"` rows, per-field `aria-label`s, native labeled buttons, `focus:border-accent` focus indication, shared AA tokens) and inherits the modal's responsive `agentic && "hidden md:block"` rule. Run the live pass (keyboard operability + 320/375/768/1024) once the dir + backend origin are free.

### Scene Structure & Feedback — narrator-led openings, longer output, thought bubbles, Inspector polish (done; `worktree-turn-loop-runtime`; plan: `docs/plans/scene-structure-feedback.md`)
Six pieces of play-experience feedback. **Locked decisions:** only the narrator opens a scene (no character speaks unprompted at the start), but characters still respond once the player acts (narrator-led, not narrator-only); branch selection sends its `outcome` so the story extends over several beats; more + longer narration; speech and thought render as separate bubbles; the action label stays a short 5–10-word tag; narrator + label are upright (not italic); the Inspector gets larger text, auto-collapsing turns, per-entry dropdowns, and color-coded dots. Four phases, commit-per-phase; merged to `main`.
- **P1 — Backend turn flow [done]:** cold-open freeform turns emit a narrator opening first (`turn_engine`); `planner_agent` biased toward narration + gated so a cold open doesn't force a speaker; `narrator_agent` gains a longer `long`/`lead` progression paragraph; `TurnRequest.outcome` plays a branch out; `internal_thought` streams as `private_to_user` (kept out of `turn_beats`); `character_action` capped ≤10 words. Tests: `agents/test_planner_agent.py` (+2), `agents/test_narrator_agent.py` (+1), `api/test_play_turn.py` (thought-streams rewrite, +2 opening/branch). Backend **523 pytest**, ruff + mypy clean.
- **P2 — Frontend seed + bubbles [done]:** `genericScene` opens narrator-only; `mergeFrame` renders `internal_thought` as its own thought bubble; `TranscriptBeat` de-italicizes the narrator + action label and adds a distinct `ThoughtBubble`; branch `outcome` forwarded through `useScenePlay.submit`/`postTurn`. Tests: `turn-stream.test.ts` (+2), new `TranscriptBeat.test.tsx` (4). Frontend suite green, tsc + eslint clean.
- **P3 — Inspector rewrite [done]:** larger text; turns are an accordion (newest open, completed collapse); each step is a keyboard-operable dot+tag+title row whose detail expands in a dropdown (`aria-expanded`); per-type colored dots redundant with the text tag. Tests: `TurnInspectorPanel.test.tsx` rewritten (7). tsc + eslint clean.
- **P4 — Docs + validate + merge [done]:** `api-contract.md`, `data-flow.md`, `design-system.md` updated (thought bubble, narrator-led opening, `outcome`, upright text, Inspector). Full backend **523** + frontend suites green; merged to `main`.
- **A11y/responsive note:** the frontend changes were verified via deterministic render/reducer tests (roles, `aria-expanded`, absence of italics, thought/speech routing) rather than a live browser pass — the thought bubble requires a configured-LLM turn to appear and the worktree preview is unreliable for it. New UI reuses established AA-compliant tokens (`text-ink-soft`, `bg-card2`, `border-cardbd`) and keyboard-operable button patterns; the Inspector panel keeps its prior 340px docked width (responsive behavior unchanged). Color-coding is paired with text tags (WCAG 1.4.1). A live visual pass on a running stack is a recommended follow-up.

### Reactive Turn Director — natural multi-party flow + graph-aware characters (done; `worktree-turn-loop-runtime`; plan: `docs/plans/reactive-turn-director.md`)
Overhauls the turn loop's **Produce** band after a user report: the old director was a one-shot pick capped at 3, the player's line was an opaque blob (so a bystander answered instead of the directed character), the narrator never fired, and the graph was read-but-unused + empty of relationships. **Locked decisions:** puppeting → the directed character performs **in their own voice**; **no speaker limit** (runs until the direction is satisfied); **ReAct** re-planning after each beat; relationships **seeded from bios + evolved in play**. Six phases, commit-per-phase; merged to `main`.
- **P1 — Input intent + puppet performance [done]:** `agents/intent_agent.interpret` classifies the player's line (puppet / direct / broadcast / narration / freeform; roster-constrained, best-effort → freeform). A puppeted character now **performs the direction in-voice** (`character_turn_agent` `directive` block) and the addressed one reacts — fixing attribution. Inspector `intent` step. Tests: `agents/test_intent_agent.py` (7), `api/test_play_turn.py` (+1 puppet). Backend 484 pytest.
- **P2 — ReAct turn planner [done]:** `agents/planner_agent.next_beat` drives a reason→act→observe loop (speak / narrate / end), replacing `who_is_up` + the 3-cap + rerank/cascade in `run_turn`. A whole-group direction walks the **entire cast** (`TURN_MAX_BEATS` is a runaway backstop, floored above cast size); the narrator fills context between speakers in POV; `directed_at` folds into the intent. `who_is_up`/`rerank` kept as superseded utilities. Inspector `plan` steps. Tests: `agents/test_planner_agent.py` (8), rewrote the P5/P8/P10 director tests (planner-driven; incl. a 4-speaker "everyone" test past the old cap). Backend 492 pytest.
- **P3 — Seed relationships from bios [done]:** `agents/relationship_agent.extract` reads the cast bios and proposes initial directed edges using the registry's character↔character types; `services/relationships.ensure_seeded` writes them to Neo4j on a session's first turn (idempotent, best-effort). Inspector `relationships` step. Tests: `agents/test_relationship_agent.py` (6), `services/test_relationships.py` (5). Backend 503 pytest.
- **P4 — Graph-aware responses [done]:** `graph_reader.relationship_context` returns the speaker's direct edges (both directions + reason) and 2-hop shared links; the engine folds a plain-language note per beat (scoped to whom they address) into the character prompt. Inspector `relationship` step. Tests: `services/test_graph_reader.py` (+4), `agents/test_character_turn_agent.py` (+1). Backend 508 pytest.
- **P5 — Relationship evolution in play [done]:** a `<type:relationship_update>` block → `validator.validate_relationship` (target-in-cast + known type; unknown/self/malformed dropped) → a relational `Consequence` the cold-path turn-writer reifies as a directed edge. Inspector `relationship_change` step. Tests: `services/test_validator.py` (+6), `services/test_emission.py` (+1), `api/test_play_turn.py` (+1). Backend 516 pytest.
- **P6 — Frontend polish + live relationships [done]:** Inspector STEP_META for the new steps (`intent`/`plan`/`relationship`/`relationships`/`relationship_change`); `GET /play/{id}/relationships` (`graph_reader.scenario_relationships`) + `useScenePlay` fetches them and the Director rail shows **real** graph relationships (falling back to the seed when the graph is empty). Tests: `api/test_play_turn.py` (+2 route), FE `TurnInspectorPanel.test.tsx` (+1 tags) + `turn-stream.test.ts` (+2 mapper). **Validation:** backend **518 pytest**, ruff + mypy clean on touched files; frontend **270 vitest** + typecheck + lint (0 errors) + `next build` clean. Verified via the suites + build (not a live browser — the standing full-stack/CORS constraint); the user runs it live.

### Library selection glow — animated in-scene/active highlight (done; `feat/library-selection-glow`; plan: `docs/plans/library-selection-glow.md`)
User-requested polish: on the Story Library main screen, the Characters and Settings columns list every authored character/setting, and the subset belonging to the currently selected scenario used only a **static** highlight (a fixed generic-accent ring on `CharacterCard`, a fixed accent border + hardcoded shadow on `SettingCard`). Replaced both with an **animated pulsing glow** to draw the eye to who/where is in the scene, keyed to each entity's own existing outline color. Four phases, commit-per-phase, in a worktree merged to `main`.
- **Phase 1 — glow primitive:** `styles/themes.css` — new `@keyframes veloraGlowPulse` (oscillates a two-layer `box-shadow` ring between a dim and a bright frame, driven by an inline `--glow-color` custom property) + `.velora-glow` utility (`animation: veloraGlowPulse 2.2s ease-in-out infinite`, base box-shadow = the bright frame). No separate reduced-motion media query needed: the app's existing `.velora-themed *` reduced-motion rule already strips `animation` app-wide, so under reduced motion the base box-shadow survives as a static glow.
- **Phase 2 — `CharacterCard`:** `highlighted` cast members now get `.velora-glow` + `--glow-color: c.color` (the character's own accent color, matching its existing border) instead of the old static `boxShadow: "0 0 0 2px var(--accent), ..."` ring. `CharacterCard.test.tsx` (+1 — glow class/`--glow-color` present only when `highlighted`).
- **Phase 3 — `SettingCard`:** `active` setting now gets `.velora-glow` + `--glow-color: var(--accent)` alongside its existing `border-2 border-accent`; dropped the now-redundant hardcoded `shadow-[0_6px_18px_rgba(142,43,28,.18)]`. `SettingCard.test.tsx` (+1 — glow class/`--glow-color` present only when `active`, existing `aria-current`/label assertions unaffected).
- **Phase 4 — docs + validation + merge:** `design-system.md` (Library Layout section + Generated-image-orientation paragraph) + `component-map.md` (`CharacterCard`/`SettingCard` rows) updated; this entry.
- **Validation:** frontend **261 vitest** (+2), typecheck clean, lint clean (0 errors; 4 pre-existing warnings unrelated to this change), `next build` clean. Backend untouched → `pytest` N/A.
- **Decision (assumption, no blocker):** the hero `ScenarioCarousel`'s `CastCard` is **out of scope** — that section only ever renders the featured scenario's own cast (nothing to distinguish "selected" from "not"), unlike the library columns which list everyone. Extend to the hero later if the user wants full parity.
- **Live in-browser verification (done):** a worktree dev server on **port 3363** (own `.next`, node_modules hardlink-copied per the standing worktree-build gotcha) plus a throwaway `app/glowpreview` route (mounting `CharacterCard`/`SettingCard` directly — the real Library page redirects to "New Storyline" because CORS blocks the backend on 3345 from a non-3346 origin, the standing shared-dir constraint) confirmed via `preview_eval` computed styles: highlighted/active cards carry `animation-name: veloraGlowPulse`, `animation-iteration-count: infinite`, and a `box-shadow` colored by `--glow-color` (`#2F7D6B` for the character, `var(--accent)` resolving to the Parchment accent for the setting); non-highlighted/inactive siblings have no `.velora-glow` class and `animation-name: none`. Bounding-box checks at 320px confirmed the glow's blur doesn't clip within its column. The Electron **screenshot** tool timed out (known environment issue, not a regression) — verified via `preview_snapshot`/`preview_inspect`/`preview_eval` instead. **Not exercised live:** toggling the OS `prefers-reduced-motion` preference itself (no emulation hook in the preview tooling) — confirmed instead by reading the existing, already-proven `.velora-themed *` reduced-motion rule in `styles/themes.css`, which strips `animation` globally and leaves `.velora-glow`'s own (non-keyframe) `box-shadow` as the static fallback. The throwaway route + temporary `.claude/launch.json` entry were removed after verification; nothing preview-related was committed.

### Turn-loop runtime — the streamed story engine (in progress; `worktree-turn-loop-runtime`; plan: `docs/plans/turn-loop-runtime.md`)
Builds the runtime turn loop from `Documents/Plans/Velora/2.velora-turn-loop-plan.md` (v0.3): player message → assemble → per-character POV think→speak → validate/clamp → streamed NDJSON → cold-path consequences → read-time reflection. **P1–P8 built + reviewed;** the user approved continuing to **P9–P11** — all now **complete** (reflection interlude, multi-party queue/cascade/consistency, vLLM concurrency hardening). The full runtime turn loop (all four bands) is implemented, validated (backend **471 pytest**, ruff + mypy clean on touched files), and merged to `main`. **Locked decisions:** POST `/play/{id}/turn` streams NDJSON directly (separate `GET /stream` + pub/sub is a deferred seam); **no dice** (`check` dropped from `branch_choices`, D11); **parallel processing on** (vLLM available — `TURN_MAX_CONCURRENCY`, sequential speech preserved); simulated delta streaming (server-chunked) first; `seq` DB-authoritative with a `(session_id, seq)` unique constraint. Best-effort throughout (Redis/Neo4j/Qdrant down → degrade, never block; LLM offline-mocked in tests).
- **P1 — Transport + envelope (full-event echo) [done]:** `internal_thought` event (hidden, withheld from the stream) + `check` dropped from `BranchChoiceOption` (`events/envelope.py`, `schemas/base.py` EventType); `events/stream.py` (`build_event`/`to_ndjson_line`/`TurnErrorFrame`); `Event` `(session_id, seq)` unique constraint + Alembic `f1a2b3c4d5e6`; `services/events_store.py` (DB-authoritative `next_seq`, session resolve/create, persist); `memory/buffer.py` (best-effort Redis recent-turn buffer); `schemas/play.py` (`TurnRequest`); `services/turn_engine.py` (echo turn + reusable `_Emitter` — seq/persist/buffer/withhold-hidden); `routes/play.py` (`POST /play/{id}/turn`, NDJSON `StreamingResponse`, pre-flight); config `turn_buffer_size`/`turn_max_concurrency` + `redis_configured`; conftest `_redis_disabled_by_default`. **Validation:** backend **359 pytest** (+22; ruff + mypy clean on touched files). Tests: `api/test_play_turn.py` (9), `services/test_events_store.py` (8), `services/test_buffer.py` (4), `data/test_events.py` (+2 internal_thought/no-check). Docs: api-contract (Play implemented + Turn Stream + internal_thought + no-check), .env.example (`TURN_*`).
- **P2 — Band-1 context assembler [done]:** `services/assembler.py` (`assemble_context` → `TurnContext`: ordered `CastMember`s with a full clamped stat block + in-voice anchors from the buffer, the setting, the active stat schema + loaded guidance, recent beats, best-effort scenario subgraph, and the cacheable `stable_prefix` = World Primer + stat guidance). Reads share the request `Session` so assembly is sequential; dangling cast ids skipped, graph-down → empty subgraph, no-Redis → no beats. **Validation:** backend **364 pytest** (+5; ruff + mypy clean). Tests: `services/test_assembler.py` (cast/setting/stats/guidance/prefix; graph-down empty; dangling skip; in-voice anchors; no-stats clean).
- **P8 — Branch choices + stat-panel wiring + validator clamp (no dice) [done]:** Closes the interactive loop. **Backend:** `services/validator.py` (`validate_stat` → parse a proposed stat JSON, confirm the stat exists, resolve value/delta, **clamp to `[min,max]`**, keep `reason`; unknown/malformed → dropped); `emission.py` parses `<type:state_update>` blocks; the character contract gained an optional stat-change block; `turn_engine._apply_stat_change` validates → applies on the hot path (`set_character_stats`) → emits the `state_update` event → records a stat `Consequence` (now feeding the P7 cold-path writer); `director_agent.propose_branches` (2–4 `{label, outcome}` options, capped, best-effort) emitted as `branch_choices` when the Director flagged `needsBranch`. **Frontend (D11 — no dice):** retired the `CheckCard` renderer + the seeded check beat + `SceneMessage`/`SceneChoice` check/roll/result fields; `turn-stream.applyStatUpdate` (upsert a stat chip by key, value + `reason` title) + `branchOptionsToChoices`; `useScenePlay` routes `state_update` → stat panel and `branch_choices` → live choices, and **branch selection submits a real turn** (`choose` → `submit`); `StateChips` shows the change `reason` as a title. **Validation:** backend **417 pytest** (+11; ruff + mypy clean); frontend **259 vitest** (+3) + typecheck + lint (0 errors) + `next build` clean. Tests: `services/test_validator.py` (5), `agents/test_director_agent.py` (+3 branches), `api/test_play_turn.py` (+3 — clamped `state_update` applied, unknown stat dropped, `branch_choices` on a fork), FE `turn-stream.test.ts` (+3), `StoryPlayerView.test.tsx` (no-check render + branch→turn→stat-panel). Docs: api-contract (stat clamp + branches in the Turn Stream), design-system + component-map (CheckCard retired; chip reason title). **Deferred — live in-browser a11y/responsive pass:** standing shared-dir/CORS + full-stack constraint blocks `preview_start`; verified via the green suites + `next build` + structure (branch rows are `<button>`s with visible label/outcome + focus ring; stat chips are non-interactive with a `title` audit; composer locks while streaming).
- **P7 — Cold-path turn-writer [done]:** `services/turn_writer.py` (`write_turn` + `Consequence`): after a turn streams (called at the end of `turn_engine.run_turn`, once the last event is yielded — never blocks the player), on **consequence turns** it routes each consequence by the **"…toward whom?" rule** (relational `target_id` + `edge_type` → `graph_writer.upsert_edge` with a reified `:Consequence` node via `attach_consequence` as shared provenance; no target → a stat, already applied + clamped on the hot path, recorded for audit) and appends an `:Event` node (`occurred_at` the setting). **Best-effort + non-blocking:** no consequences, or Neo4j disabled/down → a clean no-op (Postgres stays canonical). The engine threads a `consequences` accumulator (empty until the branch/stat phase populates it from `state_update` events). **Validation:** backend **406 pytest** (+5; ruff + mypy clean). Tests: `services/test_turn_writer.py` (5 — no-op on empty/graph-down, stat-only reifies `:Consequence` + no relational edge, relational writes the `wary_of` edge via the consequence, `:Event` node `evt_{session}_{seq}` ties `occurred_at` the setting; recording fake Neo4j session). Backend-only. Docs: data-flow (turn-writer now implemented).
- **P6 — Retrieval gate + hybrid RAG [done]:** `services/retrieval_gate.py` (`gate(text, known_names)` → `GateDecision{fetch, reason, query}`: a cheap, model-free heuristic — fires on an **off-roster** capitalized entity (mid-sentence / multi-word, minus a roster+title+honorific stoplist) or a **wh-word + world-history cue**; conservative, skips by default). `assembler._gated_lore` runs the gate over the player's line and, only on fetch, calls `_common.rag_block` to retrieve + inject a fenced reference block (best-effort → "" when Qdrant is off/empty); `TurnContext.retrieved_lore` + `gate_reason` (logged) thread it into the character prompt's middle. `assemble_context(player_text=…)` + engine passthrough. **Validation:** backend **401 pytest** (+10; ruff + mypy clean). Tests: `services/test_retrieval_gate.py` (7 — skip/fetch triggers, stoplist, off-roster dedupe), `services/test_assembler.py` (+2 — skip injects nothing, fetch injects lore via patched `rag_block`), `agents/test_character_turn_agent.py` (+1 — fenced lore reaches the prompt). Backend-only. `memory/prefetch.py` (warm prefetch + top-up) recorded as a latency seam (the gate + synchronous fetch is the functional core). Docs: data-flow (gated RAG in the turn path).
- **P5 — Speaker selection + Narrator interstitials (one engine) [done]:** `agents/director_agent.py` (`who_is_up` → `DirectorDecision{speakers, needs_branch, beat}`: directed-addressee / solo = no-LLM fast path; else a reasoned, **structure-only** roster-constrained pick — numbers→ids, out-of-roster dropped, de-duped, capped at 3; **best-effort** → falls back to the first cast member, never raises). `agents/narrator_agent.py` (`interstitial` → short 3rd-person transition prose, best-effort `None`). `character_turn_agent.generate_line` refactored to take a chronological `turn_beats` list so a later speaker genuinely reacts to its predecessor (sequential). `turn_engine` drives the speaker queue, threads `turn_beats` (player line + each emitted line), and — in **Narrator Mode** — inserts a narration interstitial before each speaker (**POV Mode** omits it; one engine, D1). `TurnRequest.mode` (`pov`\|`narrator`, default `pov`). **Validation:** backend **391 pytest** (+13; ruff + mypy clean). Tests: `agents/test_director_agent.py` (8 — fast paths, reasoned resolve/drop/dedupe/cap, fallbacks), `agents/test_narrator_agent.py` (2), `api/test_play_turn.py` (+3 — two-speaker order, Narrator interstitial leads, POV no-narration). Backend-only (FE renders `narration` already; a mode toggle in the UI is a later nicety — the FE sends the `pov` default). Docs: api-contract (Play `mode` + Director/Narrator + sequential multi-speaker).
- **P4 — Think → speak + voice anchoring [done]:** `character_turn_agent` output contract now leads with a hidden `<thinking>` block (short, in-*character*-voice, conditioned on concrete priorities — never a trait label or narrator analysis); the parse→hidden-`internal_thought`→persist-but-withhold plumbing already existed from P3. Sampler tuning for voice (`_voice_params`: `top_p` 0.92 · `frequency_penalty` 0.4 · `presence_penalty` 0.3, keeping the operator's temperature + the floored max_tokens) reins in small-model drift; `LOW` hidden-thinking budget keeps it snappy. In-voice anchors + `speech` descriptor already injected (P3). Per-storyline/per-character voice settings recorded as a seam. **Validation:** backend **378 pytest** (+3; ruff + mypy clean). Tests: `agents/test_character_turn_agent.py` (+2 — thinking block requested, voice sampler applied), `api/test_play_turn.py` (+1 — internal_thought persisted `hidden` + withheld from the stream). Backend-only (internal_thought never reaches the client → no FE change); `internal_thought` already in the contract.
- **P3 — POV loop, single-pass + delta streaming [done]:** Real generation replaces the echo. **Backend:** `agents/character_turn_agent.py` (one isolated, **bookended** LLM call — identity/state front, transcript middle, act-now tail; cacheable stable prefix in the system message; `LOW` thinking budget); `services/emission.py` (thin `<speaker:N>`/`<type:…>` → typed segments; name→id; out-of-roster + untagged fallbacks); `services/turn_engine.py` rewritten (`assemble → _pick_speaker → generate → parse → emit`); `_Emitter.emit_streamed` delta-streams prose (same id+seq, incremental `text`, `done` flag; persists full text once); `chat_complete(extra_body=)` (vLLM guided-decoding seam); `events/stream.chunk_text`. **Frontend:** `lib/events.ts` (`PlayEvent`/`TurnStreamFrame` types); `hooks/use-event-stream.ts` (generic NDJSON consumer + lifecycle + abort); `lib/api.postTurn`; `features/story-player/turn-stream.ts` (`mergeFrame` reducer — delta accumulation by id + action⊕dialogue merge); `useScenePlay` send rewired to stream (optimistic player bubble, in-flight guard, session resume, error frame); `Composer` disabled-while-streaming; `CharacterMessage` skips the empty bubble. **Validation:** backend **375 pytest** (+11; ruff + mypy clean); frontend **256 vitest** (+9) + typecheck + lint (0 errors) + `next build` clean. Tests: `api/test_play_turn.py` (rewritten — mocked LLM, delta accumulation, seq, resume, error frame), `services/test_emission.py` (6), `agents/test_character_turn_agent.py` (2 — bookended prompt), `api/test_llm.py` (+1 extra_body), FE `turn-stream.test.ts` (6), `use-event-stream.test.ts` (3), `StoryPlayerView.test.tsx` (streaming send). Docs: api-contract (Streaming modes = same-id done-flag deltas), data-flow (turn write+stream path). **Deferred — live in-browser a11y/responsive pass:** the standing shared-dir/CORS constraint (user's dev server on 3346; backend CORS allows only that origin; screenshot tool broken) plus the need for the full backend+vLLM stack blocks a clean `preview_start`; verified via the green suites + `next build` + structural reasoning (transcript is an `aria-live` region; composer input/button disable + `aria-label`; error is `role=alert`). Run once the dir + stack are free.
- **Turn Inspector panel (understand the flow) [done; user request]:** The user found the turn flow confusing ("I guide it, then one character talks, or multiple — no rhyme or reason") and asked for a clickable, wide side-panel, right of "Narrator active", showing everything that happens after a message **in order** + the rationale. **Backend (opt-in trace):** `events/stream.TurnTraceFrame` (transport-only, like `TurnErrorFrame` — not a persisted story event, not in `story_event_adapter`); `TurnRequest.trace` (default **off**); `turn_engine._Tracer` interleaves ordered `trace` frames — `turn` (your message) · `assemble` (who's present + loaded dispositions + RAG gate) · `director` (**who speaks + a plain-language why**: addressed / solo / reasoned beat) · `speaker` · `thinking` (**surfaces the hidden `internal_thought`**) · `consistency` · `action` / `dialogue` · `stat` (clamp + reason) · `rerank` (order change) · `cascade` · `branch` · `reflection`. Off by default → the default stream + story-event contract (and every existing test) are unchanged. **Frontend:** `lib/events.ts` (`TurnTraceFrame` + `trace?` on the body), `turn-stream.foldTrace` (groups frames into per-turn timelines; `mergeFrame`/`sessionIdOf` ignore trace), `useScenePlay` (collects `traceTurns`, requests `trace: true`), `components/feature/TurnInspectorPanel.tsx` (**a docked right-side column — sits to the right of the Director rail so the chat stays visible**, not an overlay; `complementary` landmark; newest turn first, each step a tagged, ordered row; × close; empty-state hint), `SceneHeader` gains an **Inspector** button right of "Narrator active", wired in `StoryPlayerView` (rendered inside the layout row after the rail). Trace steps now include **`lore`** (RAG look-up — fetched/skipped + reason) and **`commit`** (story-graph write — # of consequences), and the `assemble` step reports graph availability, so the RAG + knowledge-graph phases are visible. **Validation:** backend **476 pytest** (+5 — trace off by default, present+ordered when requested with story events still validating, Director rationale, hidden thinking surfaced, `commit` reports graph changes on a stat turn; ruff + mypy clean); frontend **267 vitest** (`foldTrace` grouping, panel render/close as a docked column, trace ignored by transcript) + typecheck + lint (0 errors) + `next build` clean. Docs: api-contract (opt-in trace frames). Verified via the suites + build (not a live browser — the full-stack + shared-dir/CORS constraint stands); the user runs it live.
- **P11 — vLLM concurrency hardening [done]:** Turns the parallelism latent in P9/P10 into real latency wins + observability. **Async finalize:** the read-time reflection interlude is pre-resolved on the request thread (LLM conn, rendered transcript, plain target tuples) and handed to `concurrency.submit_background` (`reflection.dispatch_reflection`), so the NDJSON stream can close the instant the last visible event is yielded — the player never waits on the N reflection LLM calls. Gated by `TURN_ASYNC_FINALIZE` (**off by default** → inline, deterministic for the offline test/dev path; never used on SQLite, which has no independent connection to hand a worker). The `turn_engine` tail now calls `dispatch_reflection`; the cold-path turn-writer stays inline (no LLM, fast, best-effort). **Prefix-cache observability:** the character agent's **system message is byte-identical for every speaker in a turn** (World Primer + output contract + stat guidance), so vLLM's automatic prefix caching serves those tokens from KV instead of recomputing per speaker; `llm.prefix_cache_key` gives a short stable id logged per speaker so warm-prefix reuse is inspectable. Config `turn_ttft_slo_ms` (TTFT target) + `turn_async_finalize` knobs. The bounded pool (`concurrency.run_all`, `TURN_MAX_CONCURRENCY`) already drives the P9 universal reflection + P10 cascade concurrently; **token streaming** (`chat_complete(extra_body={"stream": …})` passthrough) and **Director-concurrent-with-assembly** are recorded seams (the latter deferred deliberately: the request SQLAlchemy `Session` is not thread-safe, so the safe overlap — running the db-free Director LLM call on a worker while the main thread does the db-bound RAG/subgraph fetch — is a targeted refactor, not free). **Validation:** backend **471 pytest** (+9; ruff + mypy clean on touched files). Tests: `services/test_prefix_cache.py` (4 — stable/prefix-sensitive/short/empty-sentinel), `services/test_concurrency.py` (+3 — `submit_background` inline by default, failure-isolated, on-a-thread when enabled), `services/test_reflection.py` (+2 — `dispatch_reflection` inline by default writes interior, unconfigured no-op). Backend-only.
- **P10 — Multi-party: live queue + cascade + consistency validator [done]:** Scales the one engine from a two-hander to a crowded scene. **Consistency guard** (`services/consistency.py`, `review(conn, …) → ConsistencyVerdict`): the streaming-safe realization of the design's post-sequence line-to-line check — once a character has spoken this turn, a later speaker's candidate beat is judged against the established beats *before* it goes on the wire; a clear contradiction (fact/who-did-what/physical state, not tone) triggers **one** regenerate with the reason folded into the act-now tail (`character_turn_agent.generate_line(correction=…)`), preserving sequence order. **Live queue** (`turn_engine.run_turn` → an index-based while-loop): each speaker's beat returns an **impact** (Σ|stat delta|); a high-impact beat **re-consults the Director** mid-turn (`director_agent.rerank` — roster-constrained re-rank of the not-yet-spoken, never silently dropping one) and **cascades a disposition refresh** (`reflection.refresh_dispositions` — reflect the affected members *now*, mutate their `CastMember.disposition` in place so their upcoming line reacts + persist interior), **width scaled to impact** (`_CASCADE_WIDE_THRESHOLD=10`: a big shift moves the whole remaining room, a small one only the next speaker). **Universal reflection:** in a crowd (N>2) the tail reflects the *whole cast* (silent watchers update interior too); a two-hander only reflects who spoke. `character_turn_agent._transcript` capped at 14 beats ("bookended prompt under scale"). All guards/queue extras are **best-effort** and gated on multi-party + real impact, so solo/2-hander turns (and every prior test) are unchanged. **Validation:** backend **462 pytest** (+15; ruff + mypy clean). Tests: `services/test_consistency.py` (4 — verdict parse, empty short-circuit, malformed→consistent), `agents/test_director_agent.py` (+6 — rerank reorder/dedupe/omit-preserve/single-skip/unconfigured/malformed), `services/test_reflection.py` (+2 — refresh mutates in place + persists, unconfigured no-op), `api/test_play_turn.py` (+3 — guard regenerates a contradicting later line, high-impact re-ranks the remainder [Mei→Jax→Kira] + stat still applied, universal reflection writes interior for the whole cast). Backend-only.
- **P9 — Read-time reflection interlude [done]:** After a turn streams, each speaker reflects *while the player reads* and writes per-character **interior state** Band-1 reads back next turn — so a character re-enters already carrying the shift instead of re-deriving it. **New:** `agents/reflection_agent.py` (`reflect(conn, …)` → `InteriorRecord{disposition, retrospective, branch_dispositions, seq}`: a mutable, overriding stance + one-line retrospective, plus **branch-keyed** anticipated stances when a fork was offered; structure-light JSON, best-effort → `None`; takes a **pre-resolved LLM connection**, not a `Session`, so the whole cast reflects on worker threads); `services/reflection.py` (`run_reflection` — request-thread entry: resolve LLM once, render the transcript, reflect concurrently, write each interior; `reflect_and_store` is the self-contained core reused by P11's background finalize); `memory/interior.py` (best-effort Redis get/set at `interior:{session}:{character}`, day TTL — mirrors `buffer.py`; **volatile**, never written to the graph); `services/concurrency.py` (`run_all` — bounded, order-preserving, failure-isolating thread pool capped by `TURN_MAX_CONCURRENCY`; `submit_background` for off-request finalize). **Wired:** `assembler._build_cast` reads interior → `CastMember.disposition`; `character_turn_agent` injects "Your current inner stance:" into the HEAD and, when present, nudges a shorter `<thinking>` (the stance is already computed); `turn_engine.run_turn` calls `reflection.run_reflection` at the tail (off the hot path, branch-keyed when a fork was offered). Config `turn_reflection_enabled` (+ `turn_ttft_slo_ms` for P11). **Best-effort:** Redis off → no interior (empty disposition), unconfigured/failed LLM → keep last state, disabled knob → skip — the turn always runs. **Validation:** backend **447 pytest** (+25; ruff + mypy clean). Tests: `services/test_concurrency.py` (5), `services/test_interior.py` (5), `agents/test_reflection_agent.py` (5 — disposition/retrospective parse, branch-keyed only when offered, malformed/empty → None), `services/test_reflection.py` (5 — writes interior per character, branch-keyed, unconfigured/disabled/no-target no-ops), `services/test_assembler.py` (+2 — interior read into cast, empty when off), `agents/test_character_turn_agent.py` (+2 — disposition injected + short-thinking nudge, omitted when absent), `api/test_play_turn.py` (+1 — full pipeline: interior written after the stream, stream unaffected). Backend-only (interior is prompt-side, never on the wire).
- **Fix — emission tolerates closing/XML-style tags (leaked `</type:…>` + JSON, no dialogue bubble) [done; live report]:** a model emitted `</type:character_dialogue>` / `</type:state_update>` (closing-style tags, some as the *delimiter* between blocks) plus a trailing stat JSON. The parser's `<type:…>` (open-only) regex didn't recognize them → every `</type:…>` and the raw `{"key":…}` leaked into the rendered prose, and all content lumped into one `character_action` (italic beside the name, **no dialogue bubble**). Fix: `emission._TYPE_RE`/`_SPEAKER_RE` now match an **optional slash** (`</?type:…>`) so `<type:X>` and `</type:X>` are equivalent block delimiters; a `_TAG_CLEAN` scrub strips any residual tag from each prose body (state_update keeps its raw JSON for the validator); the character contract nudges "opening tag only." Now action + dialogue + state_update segment cleanly → proper bubble + a real `state_update` event, nothing leaked. **Validation:** backend **422 pytest** (+5; ruff + mypy clean). Tests: `services/test_emission.py` (+4 — closing-style delimiters, paired XML, state_update-via-close, the reported Sylvarra beat), `api/test_play_turn.py` (+1 — full pipeline: no leaked tags/JSON, bubble + clamped state_update).

### World build — multi-entity extraction (stop losing characters/settings) (done; `worktree-world-build-extraction`; plan: `docs/plans/world-build-multi-entity-extraction.md`)
Bug fix from a user report: **Build the whole world** drafted exactly one card per `character`-doc and one per `setting`-doc, but triage routes any *multi-subject* file to the `other` bucket — and `other` docs were never turned into cards. Dropping a markdown file describing several characters silently lost all of them (or, force-bucketed as `character`, collapsed them into one). Now the build **mines every attached doc** for its distinct subjects. Four phases, commit-per-phase.
- **Phase 1 — extraction agent:** new `web/backend/app/agents/extract_agent.py` (`extract_entities` → `ExtractedEntities {characters, settings}`, each `{name, source}`); `ExtractedEntity`/`ExtractedEntities` schemas in `schemas/build.py`. One LLM call per doc lists each distinct character + place (focused per-subject brief; empty for pure lore; de-dupes within a doc). Tests: `utils/tests/backend/agents/test_extract_agent.py` (6).
- **Phase 2 — build wiring:** `build_agent.iter_build_world` runs `_extract_roster` over `character_docs + setting_docs + other_docs`, de-dupes by folded name (**uncapped** — capping would re-create the loss), emits a new `extract` status stage + `plan` labels = extracted subject names, then drafts one card per subject. `BuildDoc.category`, `BuildWorldRequest.other_docs`, route passthrough (`/build` + `/build/stream`). `test_build_agent.py`: multi-character doc → 3 cards; `other` doc → both kinds; cross-doc dedup; existing single-subject still one each.
- **Phase 3 — frontend:** `useStorylineCreator.build` sends **every** kept doc — `characterDocs`/`settingDocs` by bucket + `otherDocs` (other + uncategorized) — to `buildWorldStream`; `lib/api.BuildWorldBody.otherDocs`. Hook tests: `other`-bucket doc rides `otherDocs`; categorized docs unchanged + `otherDocs: []`.
- **Phase 4 — docs + validation + live walkthrough + merge:** `api-contract.md` (build request `otherDocs` + extraction note + `extract` stage + `plan` labels), `data-flow.md` (build flow → per-doc extraction), `documentation.md` status, this entry.
- **Validation:** backend **337 `pytest`** (ruff + mypy clean); frontend **247 vitest** + typecheck + lint (0 errors). **Live walkthrough** drove the real `iter_build_world` against the configured llama.cpp endpoint with a 3-character / 2-setting markdown — see the entry's commit message for the captured result.
- **Decisions (ratified with the user):** *extract every entity from all docs* (character/setting/other all mined for both kinds); cast/settings **uncapped** for doc-derived subjects (the affordability cap stays on invented stats only); verify with **automated tests + a real-LLM walkthrough**.
- **Deferred — live in-browser a11y/responsive pass:** the only UI change is the doc lists sent in the build request (no new layout/interactive surface); the standing shared-dir constraint (the user's dev server on 3346) blocks `preview_start`, and the full browser build needs the LLM+ComfyUI stack. Verified via the green suites + the backend live walkthrough.

### Portrait & image prompt persistence — Character + Setting editors (done; `feat/portrait-image-prompt-persistence`; plan: `docs/plans/portrait-image-prompt-persistence.md`)
Bug fix: in the Character and Setting edit menus the positive/negative ComfyUI prompts used to generate a portrait/establishing image were held in editor-only draft state (`_portraitPositive`/`_portraitNegative`, `_sceneArtPositive`/`_sceneArtNegative`) and **discarded on save** — re-opening the entity lost them. Replicated the proven Scenario `scene_art_*` pattern end-to-end. Five phases, commit-per-phase.
- **Phase 1 — backend Character:** nullable `portrait_positive` / `portrait_negative` columns on `Character` (model + `CharacterBase`/`Update`/`Read` schemas + `create_character` wiring; `update_character` patch loop already covers it); Alembic `d4f1a2b3c5e6` (`batch_alter_table('characters')`); roundtrip + null-default tests (+ schema-shape test updated).
- **Phase 2 — backend Setting:** nullable `scene_art_positive` / `scene_art_negative` columns on `Setting` (model + schemas + `create_setting` wiring); Alembic `e7a8b9c0d1f2` (chained after Phase 1); roundtrip + null-default tests.
- **Phase 3 — frontend Character:** `portraitPositive`/`portraitNegative` on the `Character` type; `editCharacter` hydrates the draft `_portrait*` keys; character submit body sends the prompts (empty → null); `DEFAULT_DRAFTS.character` seeds the keys. New hook tests (persist-on-save + rehydrate-on-edit).
- **Phase 4 — frontend Setting:** `sceneArtPositive`/`sceneArtNegative` on the `Setting` type; `editSetting` hydrates the draft `_sceneArt*` keys; setting submit body sends the prompts; `DEFAULT_DRAFTS.setting` seeds the keys. New `useLibraryState.setting.test.ts` (3: generate→render, persist-on-save, rehydrate-on-edit).
- **Phase 5 — docs + validation + merge:** api-contract (Character/Setting read-write shapes + portrait endpoint note), data-flow (both authoring-flow diagrams), documentation status, this entry.
- **Validation:** backend **326 pytest** (+2; ruff clean); frontend **244 vitest** (+5) + typecheck + lint (0 errors) + `next build` clean.
- **Decisions (autonomous):** Character columns named `portrait_*`, Setting columns `scene_art_*` (match each entity's existing draft keys + the shared `SceneArt*` schemas); all columns nullable so the additive reconciler self-heals dev DBs, Alembic covers Postgres; no new endpoints/agents/UI-layout — pure persistence wiring.
- **Deferred — live ComfyUI render verification:** the generate→edit→save→reopen loop needs a running ComfyUI server (not assumed available); the persistence path is covered by the pytest roundtrips + vitest submit/edit-hydration assertions + typecheck/build. No new layout/interactive surface (data-only field additions), so the a11y/responsive surface is unchanged. Run the live render pass once a Comfy server + free dev dir are available.

### Draft a Character / Setting from an uploaded context file alone (done; `worktree-fix-draft-context-docs`; plan: `docs/plans/draft-from-context-docs.md`)
Bug fix: dropping a `.txt`/`.md` reference file into the Character/Setting editor's Context-files panel with its **Draft** toggle on, then clicking **"❖ Draft with Velora"** without typing a seed sentence, did nothing — drafting was gated on a seed in three independent places (the modal `canDraft`, the `useLibraryState` handlers' `if (!seed) return;`, and the backend agents' `if not seed: raise 400`). Now a Draft-tagged context file is a valid grounding source on its own. Three phases, commit-per-phase.
- **Phase 1 — backend:** `character_agent.draft_character` / `setting_agent.draft_setting` reject only when **both** `seed` and `docs_overview` are empty; when the seed is empty they open the prompt with a docs-only instruction (no dangling `"seed: "`). Tests: `test_draft_requires_a_seed_or_docs` (both-empty → 400) + new `test_draft_from_docs_without_a_seed` (empty seed + `docsOverview` → 200, doc text reaches the prompt), for both agents.
- **Phase 2 — frontend:** `CharacterModal`/`SettingModal` `canDraft = Boolean(seedText) || docsForDraft(d._docFiles ?? []).length > 0`; `useLibraryState.draftCharacter`/`draftSetting` compute `docsOverview` first and bail only when both seed and docs are empty (the seed, possibly `""`, is still forwarded). New doc-only draft tests in `CharacterModal.test.tsx` / `SettingModal.test.tsx` (button disabled with no seed, enabled after a Draft-tagged drop, calls the API with `""` + the doc text).
- **Phase 3 — docs + validation + merge:** `api-contract.md` (seed now optional when `docsOverview` present, for both draft endpoints), `data-flow.md` (Character + Setting authoring-flow notes), this entry.
- **Validation:** backend agent suites green (`uv run pytest` for the two agent test files; ruff + mypy clean on both agents); frontend **241 vitest** + typecheck + lint (0 errors) green.
- **Scope:** Character + Setting only (the user's report). The **scenario** draft (`draftScenario` / `scenario_agent.draft_scenario`) keeps the same seed-required gate — bringing it to parity is an optional follow-up.
- **Deferred — live in-browser a11y/responsive pass:** the only UI change is the `disabled` predicate of an existing `<Button>` (no new interactive surface, contrast/focus/layout unchanged) — the disabled→enabled transition is covered by the new component tests; the standing shared-dir constraint (dev server on 3346) blocks `preview_start`.

### Hybrid RAG — implemented end-to-end (done; `worktree-rag-implementation`; plan: `docs/plans/velora-rag-implementation.md`, doc: `docs/rag.md`)
The RAG layer is now real and **used**, not theory. Eight phases, commit-per-phase, all best-effort (CRUD + `pytest` run with no Qdrant / no model — mirrors the Neo4j substrate). See `docs/rag.md` for the full design.
- **Phase 1 — core:** `web/backend/app/rag/` — `Frontmatter`/`LoreEntry` schema, prefix-fusion serializer (dense `build_embed_text` + sparse `build_bm25_text`), 512-token guard, and entity→entry adapters for storyline/character/setting/scenario/context-doc.
- **Phase 2 — embedder seam:** `embedder.py` — `FastEmbedEmbedder` (**fastembed** ONNX `BAAI/bge-large-en-v1.5`, 1024-dim, CPU-default, optional CUDA/ROCm, no torch) + deterministic `HashEmbedder` fallback (feature-hashing → real lexical-overlap cosine; the test default via conftest so CI never downloads a model). `fastembed` dep; `EMBED_*` config.
- **Phase 3 — Qdrant store:** a Qdrant container (docker-compose REST 3351/gRPC 3352, started by `app.py`), lazy best-effort `core/qdrant.py` (mirrors `core/neo4j.py`), `store.py` (one `velora_lore` collection, named dense+sparse vectors, storyline-scoped payload filter, uuid5 point ids, content-hash idempotency). Tests use `QdrantClient(":memory:")`. `qdrant-client` dep. Preflight pings Qdrant + ensures the collection (advisory).
- **Phase 4 — ingest-on-save + progress:** `indexer.py` content-hash-idempotent `index_entry` + best-effort `sync_*`/`remove` CRUD hooks (storyline/character/setting/scenario/context-doc embed on create+update, prune on delete; storyline delete drops the world's corpus). `POST /storylines/{id}/rag/reindex/stream` (NDJSON "embedding i / N" → done) + `GET …/rag/status`.
- **Phase 5 — retrieval + utilization:** `retriever.py` dense+sparse → hand-rolled RRF (k=60) + conservative metadata pre-filter (graceful fallback). `_common.rag_block` wired into the **character/setting/scenario** draft agents (storyline genesis has no corpus → unwired). `POST …/rag/query` debug endpoint.
- **Phase 6 — entity-scoped persistent docs + the "lost track" fix:** `ContextDocument` gained nullable `entity_type`/`entity_id` (Alembic `c0a7d0c5e1f2`); scoped list/create; entity delete purges scoped docs + embeddings. Frontend: storyline editor re-hydrates its panel on edit (fixes the vanishing-files bug); the four editors persist their context files (`features/library/entityDocs.ts` reconcile-by-name, re-hydrate on edit); `ContextFilesPanel` footer → "Saved with this entry · embedded for retrieval".
- **Phase 7 — delete-from-index + status surface:** delete paths verified to prune Qdrant points; `commitWorld` deletes removed storyline-level docs on edit (entity-scoped untouched); the storyline editor footer shows "✦ N embedded · Re-embed" with live "Embedding i / N" progress (`aria-live`).
- **Phase 8 — docs + validation + merge:** new `docs/rag.md` + updates across documentation/structure/workflow/deployment/architecture/data-flow/api-contract/routes/.env.example.
- **Validation:** backend **324 `pytest`** (ruff + mypy clean); frontend **239 vitest** + typecheck + lint (0 errors) + `next build` clean.
- **Decisions (ratified with the user):** fastembed/bge-large (CPU-default, no torch); **Qdrant** container; **hybrid baseline now** (defer cross-encoder rerank + Neo4j `related` KG edges — both pinned as seams); **all four menus** persist entity-scoped docs.
- **Deferred:** cross-encoder rerank (`bge-reranker-v2-m3`), Neo4j `related` KG-edge expansion, RAGAS eval. **Live a11y/responsive pass not run** — the frontend change adds no new layout/interactive surface beyond one `<button>` (the Re-embed control: native button, visible label, disabled states, `aria-live` progress) and a footer copy swap (same token classes → unchanged contrast); the standing shared-dir constraint (dev server on 3346) blocks `preview_start`. Run it once the dir is free: open a storyline editor, confirm the "✦ N embedded · Re-embed" control operates by keyboard and the file panel re-hydrates on edit.

### Story player — backend data wiring (done; plan: `docs/plans/story-player-backend-data.md`)
The story player now loads the **real scene from the database** instead of in-memory seed. New `features/story-player/useSceneData.ts` (`Promise.all` of `listScenarios`/`listCharacters`/`listSettings`/`listStatDefinitions`/`getStoryline`, resolves the scenario → `ResolvedScenario` with real cast + portraits and the setting + scene art, returns `loading`/`error`/`ready` + `reload`, **seed fallback** when the scenario is missing or the backend is unreachable) + `StoryPlayerRoute.tsx` (client wrapper: ❖ loading curtain, error card with Try again + ‹ Library, else `StoryPlayerView`). `app/[storylineId]/[scenarioId]/page.tsx` is now a thin server component passing the ids. Portrait/scene-art `/media/...` paths resolve via `mediaUrl()` at the `SceneLoader`/`SceneIntro`/`CastRail`/`CharacterMessage` sites; the loader kicker shows the real storyline name; `genericScene` is null-safe for real (nullable) character fields. **The transcript itself stays locally scripted** — the streaming/agent turn loop is the next phase. Tests: `useSceneData.test.ts` (resolves cast/setting/stats; errors when absent from backend+seed) + `StoryPlayerRoute.test.tsx` (loading→ready, error+retry).
- **Validation:** typecheck + lint clean (the new hook is lint-clean; the `set-state-in-effect` reset is an explicit, scoped disable), `next build` clean, full Vitest suite green (+4). **Live-validated against the running backend** (Terra Vaelun `/60c77310/4e5b` — 9-cast survival scene with real portraits + scene image + a 4-stat schema).
- **Decision (from the user):** load the **real DB scene** (unique characters, portrait images, scene art, real names/titles/stats) with the chat still showing fake/scripted messages — to see the populated interface before the agent processing lands.

### Scene loader + chat-interface overhaul (done; `claude/hungry-gould-dc57c3`; plan: `docs/plans/scene-loader-and-chat-overhaul.md`)
Frontend-only overhaul of the story player — the loading screen from the main page into the scenario chat, and the chat interface itself — using **pseudo-seeded** data (the real Embergate cast/setting/scenario + a real stat schema, with scripted messages) so the populated experience is visible **before** any agent/streaming wiring. Five phases, commit-per-phase.
- **Phase 1 — pseudo-seed enrichment:** `lib/seed-data.ts` gained `appearance`/`background`/`personality` on the 6 characters, `atmosphere`/`features`/`currentState` on the 5 settings, and a new `SEED_STAT_DEFS` stat schema (Health/Suspicion/Trust/Patience with labeled bands, mirroring `web/backend/app/core/seed.py` + `content/stats/*.md`). Data-only.
- **Phase 2 — establishing loader:** `SceneLoader` rebuilt from a `{title}` spinner into a scenario-aware "curtain" (scene-art backdrop behind `CARD_SCRIM` w/ gradient fallback, storyline/genre kicker, title, setting + genre/tone, cast portrait row, goal, ❖ conjuring-progress; `role=status`, reduced-motion safe). New `SceneLoader.test.tsx` (3).
- **Phase 3 — chat structure:** `SceneHeader` carries **genre · tone**; new `SceneIntro` "scene is set" band at the top of the transcript (setting + current state, genre/tone tags, the player's aim, clickable dramatis personae) so the reading column carries the full context (esp. on mobile where rails collapse); `CharacterMessage` dialogue avatars render the character **portrait** via `Monogram src`. New `SceneIntro.test.tsx` (2).
- **Phase 4 — rails:** `CastRail` avatars use portraits; `DirectorRail` gained a **`StatSchema`** section surfacing the storyline's real public stat definitions (default value · min–max · the labeled band the default sits in; non-public omitted), kept distinct from the live `StateChips`. `statDefs` threaded `page (SEED_STAT_DEFS) → StoryPlayerView → DirectorRail`. New `DirectorRail.test.tsx` (3) + a `StoryPlayerView` wiring assertion.
- **Phase 5 — docs + validation + merge:** this entry + component-map/design-system/routes/documentation.
- **Validation:** frontend **226 tests (48 files; +11)**, typecheck + lint + `next build` clean. **Backend untouched → pytest N/A.**
- **Live in-browser verification (this session):** an isolated worktree dev server on **port 3362** (own `.next` + hardlink-copied `node_modules`) loaded `/embergate/embergate`. Confirmed via `preview_eval` DOM/structure (the Electron preview can't visually screenshot or advance CSS transitions — the standing constraint): the establishing loader renders all context (title/setting/genre/tone/cast/goal); the chat shows the header genre·tone, the `SceneIntro` band, narrator/dialogue/check/choices, the cast rail (speaking marker), the director rail (goal · tension · **Character stats → Health → "Very healthy" band** · Scene state · Relationships), and the composer. **Bug found + fixed in-browser:** the loader was wrapped in `AnimatePresence`, whose exit animation never completes in the frozen-transition preview, leaving the curtain stuck; replaced with a plain conditional render on `scene.loading` (the transcript's own opacity/transform transition already provides the reveal). React state verified correct (`reveal` flips true; the residual computed-opacity-0 is only the frozen preview transition).
- **Decisions (from the user):** keep the player on **seed data** (pseudo-seeding — real character/title/setting info, fake messages); a **rich establishing** loader (not a bare spinner); surface the **real stat schema** *and* keep the live scene-state.
- **Deferred:** wiring the player to the **backend** + the streaming story engine (the next, agent-processing phase); a full live **responsive a11y pass at 320/375/768/1024** across all three themes (the preview viewport reports 0×0 here — the standing shared-dir/Electron constraint; verified structurally: rails are `hidden lg:block` with the `SceneIntro` band carrying context on mobile, decorative glyphs `aria-hidden`, `role=status` loader, theme-token contrast).

### Cast stats panel → slide-out extension of the card (done; `feat/cast-stats-extension`; plan: inline refinement)
Follow-up to the cast-card reorg: the hero cast card's statistics should read as **the character card extending**, not a detached box, and should **slide out**. `CastCard` is now a **2-column CSS grid** (`200px` portrait + a stats column); the `❯` arrow animates the stats column `0 → 208px` via **`transition-[grid-template-columns]`** (`motion-reduce` aware) so the stats slide out **inside the same 2px character-color border** and the wider card pushes the following cards over (one open at a time). The old separate `CastStatsPanel` sibling was replaced by an inline `CastStats` content block; `CastStrip` re-measures overflow after the ~300ms slide. **Validation:** `npm run typecheck` clean; full Vitest **217 passed (45 files)** incl. a new assertion that the stats `region` lives inside the same `.group` card as its arrow. In-browser: confirmed the end-state structure (card grid `200px 208px`, extension is the 2nd column within the single card border, stats Resolve 7 / Health 18). **The slide motion could not be visually confirmed** — CSS transitions don't advance in this Electron preview (a trivial control transition also froze at its start value; same root cause as the broken screenshot tool); the `grid-template-columns` transition is the standard reliable slide and will animate in a real browser.

### Cast-card reorg — profile, 3-up grid, transparent hero cards + stats carousel (done; `feat/cast-cards-reorg`; plan: `docs/plans/cast-cards-reorg.md`)
Frontend-only round of refinements. Four phases, commit-per-phase, merged to `main`. No backend change (the stat-defs endpoint already existed).
- **Phase 1 — profile pop-up + grid:** `CharacterProfileModal` now shows **only Background** beside the portrait; **Appearance** moved into the 2×2 (now Appearance/Personality/Voice/Goal); **Secret removed** from the view (field stays on the type); **seal medallion removed**. `ProfileSection` lost its unused `tone`/danger branch. `CharacterColumn` grid → **3 per row** (`grid-cols-2 sm:grid-cols-3`). Tests updated (no Secret; Appearance present).
- **Phase 2 — stat defs in state:** `useLibraryState` loads the active storyline's `StatDefinition[]` best-effort (`listStatDefinitions`, keyed on the active storyline; failure → empty) and exposes `lib.statDefs`. Also fixed two LibraryView profile tests that asserted the removed Secret.
- **Phase 3 — transparent hero cast cards + stats carousel:** `CastCard` rebuilt to mirror the Library card (full-bleed portrait + `PORTRAIT_SCRIM` + 2px character-color border; no solid footer/baked stats/corner badge). A `❯`/`❮` arrow toggles an inline `CastStatsPanel` (flex sibling, one open at a time) that **pushes the following cards over** and lists the storyline's public stat names + defaults (fallback empty state). **Carousel cut-off fixed:** the slide lacked `w-full min-w-0` so it grew to content and clipped cast cards past the visible edge — now the strip is bounded and pages/scrolls (Next/Previous arrows appear on overflow). `statDefs` threaded `useLibraryState → LibraryView → ScenarioCarousel → CastStrip`.
- **Phase 4 — tests + docs:** carousel tests rewritten (arrow toggles inline panel, one-open-at-a-time, public stat names/defaults shown, hidden omitted, transparent tiles `rounded-[6px]`); `component-map.md` + `design-system.md` + this entry.
- **Validation:** `npm run typecheck` clean; full Vitest **217 passed (45 files)**. In-browser pass on a worktree dev server: profile (Background-only right, 2×2 Appearance/Personality/Voice/Goal, no Secret, no medallion); 3-up column grid (3 tracks @1100px, 2-up @375); transparent cast cards (character-color border, transparent footer); arrow opens a 208px inline stats panel showing Resolve 7 / Health 18 (hidden stat omitted) and pushes siblings (scrollW 1528→1752); cast strip bounded to 345px with a working Next arrow. Screenshots unavailable in this env (Electron renderer timeout) — verified via a11y snapshot + computed styles.

### Hero cast cards — gaps, color glow, solid Statistics, cast carousel (done; `feat/hero-cast-cards-polish`; plan: `docs/plans/hero-cast-cards-polish.md`)
Frontend-only fix to the recent-scenario hero's cast strip (`components/feature/ScenarioCarousel.tsx`), from a user report (with reference image). Four phases, commit-per-phase, merged to `main`. No backend/data/API/route change.
- **Phase 1 — gaps + glow:** the cast cards became **discrete rounded tiles** (`rounded-[8px]`, `gap-[16px]` + `p-[16px]` track) with space on all four edges instead of flush `border-r` dividers; each card carries a **per-character outer glow + hairline border keyed to `c.color`** (multi-layer `box-shadow`: accent ring + accent glow + neutral drop shadow). Trailing spacer dropped.
- **Phase 2 — solid Statistics/footer:** the footer is now a **SOLID accent-tinted plate** (`color-mix(c.color 16%, #0e0a04)` + accent top hairline) so name/role/Statistics no longer show the portrait through them; the **Statistics block** sits in its own **non-transparent accent-tinted inset panel** (`color-mix(c.color 22%, #0a0703)`, own radius/padding/top-hairline). The bottom portrait scrim was softened to a gentle blend since the footer is opaque.
- **Phase 3 — cast carousel:** the strip was extracted into a `CastStrip` (+ `CastCard`) sub-component (`"use client"`) that tracks DOM overflow (mount `useLayoutEffect` + `scroll` + window `resize` + `ResizeObserver` when present — guarded for jsdom) and overlays gold **left/right arrow buttons** (`Previous`/`Next characters`) paging via `scrollBy({ behavior: "smooth" })`. Each arrow hides at its end; both hide when the cast fits; native keyboard/trackpad scroll preserved.
- **Phase 4 — tests + docs + merge:** `ScenarioCarousel.test.tsx` 7 → **10** (discrete-tile + Statistics-inset-panel structure; arrows hidden when fits; arrows appear + `scrollBy` paging when overflow is forced via mocked metrics + the end-state arrow swap). `design-system.md` (hero paragraph) + `component-map.md` (`ScenarioCarousel` row) refreshed.
- **Validation:** frontend **211 tests (44 files; +3)**, typecheck clean, lint 0 errors (only the pre-existing `onSelect`/`_onEdit` carousel compat-prop warnings), `next build` clean. **Backend untouched → pytest N/A.**
- **Decisions (assumptions, no blocker):** glow/border derive from the existing per-character `c.color` (hex-alpha, matching the repo's `${color}NN` pattern); "colored background" for Statistics = an opaque accent-tinted inset panel (whole footer also made solid, fixing name/role legibility too); arrows are **overflow-gated** (`scrollWidth > clientWidth`) and additive over native scroll.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** standing shared-working-dir constraint (a dev server already runs on **3346** under the user's `python app.py`; a second Turbopack server clashes on `.next` and `preview_start` can't reuse the external one). Verified via the green suites + `next build` + structure reasoning: arrows are labeled `<button>`s with `aria-label` + visible `:focus-visible` ring, keyboard-reachable, and additive over native horizontal scroll; the solid footer raises (not lowers) name/role/Statistics contrast vs. the prior see-through scrim (dark opaque plate). Run the in-browser pass once the dir is free: load a storyline with a wide cast and confirm the gapped glowing tiles, the solid Statistics panels, and the arrows appearing/paging on overflow.
### Portrait orientation + profile/card relayout (done; `feat/portrait-orientation-relayout`; plan: `docs/plans/character-portrait-orientation-relayout.md`)
Character images go **vertical** and the character surfaces are relaid out for them. Four phases, commit-per-phase, merged to `main`.
- **Phase 1 — portraits → 832×1216 (2:3):** `services/portraits.py` `_PORTRAIT_W/H` 1024→832×1216 (both ÷8); docstring/comment updated; `test_portraits.py` asserts 832×1216. Scene art unchanged (1024×576). Frontend sends no dimensions, so the constant is the only lever.
- **Phase 2 — profile modal two-column hero:** widened to `md:w-[860px] lg:w-[900px]`; hero = **2:3 framed portrait left** + (name, role, trait pills, **Appearance**, **Background**) **right** on `md+` (stacks below); **Personality / Voice / Goal / Secret** moved to a **2×2 grid** below (1 col below `sm`). Portrait frame uses `aspect-[2/3]` + `object-cover` (monogram fallback). Removed the now-unused `spanFull` from `ProfileSection`.
- **Phase 3 — portrait-dominant character cards + 2:3 preview:** `CharacterCard` rebuilt as a **2:3 full-bleed tile** framed in the character color — portrait fills the card behind a new vertical **`PORTRAIT_SCRIM`** (`lib/cardArt.ts`) with name/role footer (large monogram fallback on `bg-card2`); single `View {name}` button (matches ScenarioCard/Carousel/TranscriptBeat convention) + sibling edit pencil; cast highlight = accent ring + `◆ In this scene`. `PortraitModal` preview → `aspect-[2/3]`. `LibraryView` test scoped to the Characters panel.
- **Phase 4 — tests + docs:** new `CharacterCard.test.tsx` (portrait/monogram fallback, name/role, cast badge, onPreview/onEdit); `component-map.md` + `design-system.md` (dossier note rewritten + "Generated-image orientation") + this entry.
- **Validation:** `uv run pytest` portrait service (7) green; `npm run typecheck` clean; full Vitest **212 passed (45 files)**. In-browser pass on a worktree dev server: profile hero 2-col at 1024 / stacks at 375 (no horizontal overflow); card 2:3 ratio + character-color border + accent highlight ring + correct ink/character/accent text colors in `theme-light`. Over-art card text reuses the contrast-hardened `OVER_ART` constants. Screenshots unavailable in this env (Electron renderer timeout) — verified via a11y snapshot + computed styles.
- **Note — existing square portraits:** old 1024×1024 renders are centered `object-cover`-cropped into the 2:3 frames until regenerated; no backfill.

### Library column cards — full-bleed art + left→right gradient (done; `feat/library-card-images`; plan: `docs/plans/library-card-images.md`)
Frontend-only follow-up to the hero work: the **Scenario** and **Setting** cards in the Library columns now use the same full-bleed-art + left-dark→right-bright gradient "filter" as the carousel hero (the user's explicit ask), instead of a 96px image banner over a solid body. Three phases, commit-per-phase, merged to `main`. No backend/data/API change.
- **Shared:** new `web/frontend/lib/cardArt.ts` exports `CARD_SCRIM` (the two-layer gradient: dark left → near-clear right + light bottom reinforcement) and `OVER_ART` (light over-art text colors). Theme-independent for image cards (the art is the same in every theme); no-image cards keep the prior theme-aware treatment.
- **Phase 1 — `ScenarioCard`:** image-backed cards render the scene art `absolute inset-0 object-cover` behind `CARD_SCRIM`; title/genre·tone/goal are light + width-capped (`max-w-[60%]`, goal `line-clamp-3`) on the dark left; a cast + `◆ setting` footer (light hairline) spans full width; the Recent badge + edit pencil are grouped into one top-right cluster (no overlap). `min-h-[176px]`. No-image → solid card. `ScenarioCard.test.tsx` expanded 2 → 6 (full-bleed img, title/genre/goal/setting, Recent only when featured, select-on-click, cast profile without select).
- **Phase 2 — `SettingCard`:** image-backed cards render the establishing image full-bleed behind `CARD_SCRIM`; name (light, `pr` for pencil), type eyebrow (gold), `◆ In this scene` under the type when active (light-ember over art), description light + `max-w-[58%] line-clamp-2`. Active = 2px accent border (over the page bg) + the label + `aria-current`. `min-h-[118px]`. No-image → striped "setting plate" fallback. New `SettingCard.test.tsx` (5: plate fallback, full-bleed img, name/type/desc, active label+aria-current, edit).
- **Phase 3 — docs + validation + merge:** `component-map.md`, `design-system.md` (Library Layout → "Card art"), this entry.
- **Validation:** 208 frontend tests (44 files; +9), typecheck clean, lint clean (0 errors; 4 pre-existing warnings), `next build` clean. **Backend untouched → pytest N/A.**
- **Live visual verification (done):** isolated worktree dev server on **port 3361** + a throwaway `app/cardpreview` route mounting both card types with **real `/media` scene art + portraits** (10 images loaded cross-origin). Screenshot path still env-broken → verified via `preview_eval` computed-style/geometry/contrast: scenario card title/goal **16.7:1**, `◆ setting` **14:1**, goal capped 60%, scrim present; active setting card image covers the card (inside the 2px border), name **16.7:1**, `◆ In this scene` **9.4:1**, desc **16.7:1** capped 58%. Route + temporary `launch.json` config removed; nothing preview-related committed.
- **AA caveat (documented in design-system):** text rides the strong dark-left scrim + is width-capped, so over typical watercolor art it clears AA; near-white art directly behind the text band is the known edge — accepted as the deliberate, user-requested look (mirrors the approved hero scrim).
- **Decisions (assumptions):** "scroll-down menu" in the request read as the scrollable Scenarios/Settings columns (these same cards); badge/pencil grouped to avoid overlap; active marker moved under the type so it stays on the dark-left.

### Library hero — portrait cards & contrast refresh (done; `feat/library-hero-portrait-cards`; plan: `docs/plans/library-hero-portrait-cards.md`)
Frontend-only restyle of the recent-scenario hero (`components/feature/ScenarioCarousel.tsx`) to match the reference target: cleaner, higher-contrast, real imagery. Three phases, commit-per-phase, merged to `main`. No backend, data-shape, or API change.
- **Phase 1 — portrait cast cards:** the centered avatar-over-text card became a **full-bleed portrait card** — portrait fills the card (`object-cover object-top`), a bottom scrim carries the **name** (lightened via `color-mix(in srgb, accent, parchment)` so every palette color clears AA over the scrim), **role**, a hairline, and the `Statistics` empty state; a **wax-seal monogram badge** sits top-right; **monogram fallback** (tinted panel + large faint initial) when no portrait. Whole card is the `View {name}` profile button with hover/`focus-visible`. New co-located `ScenarioCarousel.test.tsx` (7 cases: portrait img, fallback, name/role, stats empty state, Begin/Profile/Prev/Next callbacks, empty-state).
- **Phase 2 — scenario panel contrast:** the flat `rgba(20,14,6,0.68)` overlay became a **graduated scrim** (two layered gradients — strong on the left under the title/Begin column + along the bottom, fading right) so the scene art reads boldly while the text column stays at AA.
- **Phase 3 — a11y hardening + docs + validation:** during the contrast pass the card scrim was strengthened to reach ~0.9 alpha by the footer line (≈62% down) so the colored name (top of footer, ~64% down) clears AA even over a bright portrait. Docs: `design-system.md` (hero card description + corner-badge deviation), `component-map.md` (ScenarioCarousel), this entry.
- **Validation:** 197 frontend tests (43 files; +7 new), typecheck clean, lint clean (0 errors; 2 pre-existing unused-prop warnings on the carousel's compat props), `next build` clean. **Backend untouched → pytest N/A.**
- **Live visual verification (done this session):** unlike the standing deferral, this change *was* verified live — a dedicated worktree gave an isolated `.next`, so a second dev server ran on **port 3360** (the user's runs on 3346). The real backend's data was unreachable (CORS allows only `:3346`; restarting the backend to add `:3360` would disrupt the running stack), so a **throwaway `app/heropreview` route** mounted `ScenarioCarousel` with mock scenarios + **real media portraits/scene-art** (served at `/media`, displayable cross-origin). The Electron **screenshot** path timed out repeatedly (no JS errors — environment tooling), so verification used **`preview_eval` computed-style + geometry + contrast** (the tooling's recommended path for colors/sizes): 5 uniform 282×325 cards, 4 full-bleed portraits covering the card + 1 monogram fallback, footer pinned to the bottom, footer names at **~7.5–9.5:1** over the scrim base (role 14:1, stats 17:1), scene-art panel full-bleed with the two-layer scrim and 16.6:1 title. The route + the temporary `launch.json` config were removed; nothing preview-related was committed. **Responsive (320/375/768) not exercised live** (the hero strip is horizontally scrollable and width-stable; revisit if a mobile a11y pass is scheduled).
- **Decisions (assumptions, no blocker):** corner badge = character monogram (no role→icon data); no per-card "selected" highlight (the carousel has no selected-character concept — hover/focus only); statistics stay the empty state (per-character stat values still not wired to the resolved `Character`).

### Character profile — bolder, more structured dossier (done; `feat/character-profile-bolder`; plan: `docs/plans/character-profile-bolder-structure.md`)
Frontend-only restyle of `CharacterProfileModal` to match a bolder/structured reference (scope: profile modal only — `CharacterCard`/`CastRail` unchanged). Three phases, commit-per-phase, merged to `main`.
- **Phase 1 — restructure:** flat 2-col grid replaced by a framed-portrait hero (2px character-color frame + inner hairline + `❖` seal medallion), large uppercase Cinzel name, role eyebrow on a rule, and `◆`-led **trait pills** (free-text `traits` split on `·`/`,`). Body is bordered manuscript section boxes (`ProfileSection` replaces `ProfileCell`): Background full-width, Appearance|Personality + Voice|Goal in a 2-col grid, full-width danger-tinted Secret box; widened to `sm:w-[640px]`. `CloseButton` gained an optional `className`.
- **Phase 2 — theme/a11y/responsive (verified, no code delta needed):** all surfaces use theme tokens — confirmed in-browser across **Parchment / Ember / Slate** (section boxes resolve `--card-bg2`/`--card-bd`; Secret keeps its `--accent` border + rust tint; name uses the theme ink). Responsive verified at **320 / 375** (single-column grid, stacked hero, no horizontal overflow) and **desktop** (2-col). Decorative glyphs are `aria-hidden`; modal focus-trap/Escape inherited from `Modal`.
- **Phase 3 — tests + docs:** `CharacterProfileModal.test.tsx` updated for the pill split (one `listitem` per trait), empty-traits, and the Secret section → **8 tests**; `component-map.md` + `design-system.md` (Meaningful Imagery → "Character dossier") + this entry.
- **Validation:** typecheck clean; full Vitest suite **192 passed (42 files)**. No backend changes → `pytest` N/A. In-browser a11y/responsive pass done in a worktree dev server (screenshots unavailable in this env — verified via a11y snapshot + computed styles).

### Character Card Preview Modal — click opens profile popup, 2-column layout (done; `feat/character-card-preview-modal`; plan: `docs/plans/character-card-preview-modal.md`)
Frontend-only. Three phases, commit-per-phase, merged to `main`.
- **Phase 1 — `CharacterProfileModal` 2-column layout:** widened to `sm:w-[560px]`; body replaced by a 2-column CSS grid with row pairs: Appearance|Background · Personality|Voice · Goal|Secret · Edit button (full span). Empty cells omitted; if only one in a pair is present it spans both columns. Traits line moved into the header. 6 tests (existing + new row/column assertions + edit callback + omit-empty-row).
- **Phase 2 — wire CharacterCard to open modal:** `CharacterCard` `expanded`/`onToggle` props replaced by `onPreview`; inline detail section removed; card click calls `onPreview()`. `CharacterColumn` swapped `expandedId`/`onToggle` for `onPreview`. `LibraryColumns` wires `onPreview={lib.openProfile}`. `useLibraryState` removes `expandedCharId`/`toggleExpand` state + the `setExpandedCharId(created.id)` post-create call (replaced by nothing — new character just appears in the list). Updated `LibraryView.test.tsx` ("expands character" → "opens profile modal" assertion).
- **Phase 3 — docs + validation:** `component-map.md` (CharacterCard behavior + CharacterProfileModal layout); `checklist.md` this entry.
- **Validation:** 184 frontend tests (typecheck + lint + build clean; 4 net-new tests in CharacterProfileModal). No backend changes.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** same standing constraint (shared working dir, `preview_start` can't reuse the external dev server on 3346). Verified structurally: `CharacterProfileModal` is already focus-trapped via `Modal`; the 2-column grid adds no new interactive elements; `CharacterCard` still has a single `<button>` as its clickable element; `Edit Character` is a standard `<Button>` with visible label. Run the in-browser pass once the dir is free.

### Scenario Editor — three-column layout (done; `feat/scenario-editor-three-column`; plan: `docs/plans/scenario-editor-three-column.md`)
Restructured `EntityModal` (scenario editor) to match `CharacterModal`/`SettingModal`'s three-column pattern. Two phases, commit-per-phase, merged to `main`.
- **Phase 1 — UI restructure:** `EntityModal.tsx` now uses `Modal` `externalClose` + `splitScroll`; outer `lg:flex` wrapper; main column is `lg:flex-1 lg:overflow-y-auto`; inner two-column `md:flex` (form `md:flex-1 md:pr-[26px]` + aside `md:w-[300px]`); `ContextFilesPanel` added as the third `lg:w-[330px]` column. Footer moved outside the inner flex to be full-width. Removed unused `CloseButton` import.
- **Phase 2 — tests + docs:** `EntityModal.test.tsx` gained `_docFiles: []` in the draft baseline + two new tests (ContextFilesPanel visible in agentic mode; hidden class present in manual mode). `docs/component-map.md` updated for `EntityModal` (three-column entry) and `ContextFilesPanel` (now also used by EntityModal).
- **Validation:** 182 frontend tests (typecheck + lint + build clean). Backend untouched — `draft._docFiles → draftScenario` wire already existed.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** same standing constraint (shared working dir, `preview_start` can't reuse the external dev server on 3346). Verified structurally: `EntityModal` now uses `Modal` `splitScroll`/`externalClose` (same as Setting/CharacterModal); `ContextFilesPanel` is a native drag-drop zone + `Browse files` label; aside hidden via `hidden md:flex` when not in agentic mode. Run the in-browser pass once the dir is free: open a scenario, confirm the three-column layout + context-files drop zone at lg+, then drop a `.txt` file and verify it grounds "Draft with Velora."

### Scenario Scene Art — watercolor image generation for Scenarios (done; `feat/scenario-scene-art`; plan: `docs/plans/scenario-scene-art.md`)
Brought **Scenario** scene-art generation to parity with the Setting Creator. Five phases, commit-per-phase, merged to `main`.
- **Phase 1 — data layer:** nullable `image`, `scene_art_positive`, `scene_art_negative` (all String) columns on `Scenario` (model/schemas/CRUD); Alembic migration `20260628_121750_scenario_scene_art_columns.py` (`revision e13f0f3faea0`); `services/media_cleanup.py` extended to track `Scenario.image` alongside `Character.portrait` and `Setting.image`. Roundtrip + null-default tests.
- **Phase 2 — agent + routes:** `agents/scenario_agent.generate_scene_art_prompts` (adapted from `setting_agent`, scenario-specific system prompt; depicts the setting shaped by the scene's tone, no people); `ScenarioSceneArtPromptRequest` schema; `POST /scenarios/scene-art-prompts` + `POST /scenarios/scene-art` routes declared **above** `/scenarios/{id}`; reuses `SceneArtPromptResponse` and `services/scene_art.generate_scene_art` unchanged. 5 new offline-mocked tests.
- **Phase 3 — frontend data path:** `Scenario` type gains `image?`, `sceneArtPositive?`, `sceneArtNegative?`; `api.ts` adds `generateScenarioSceneArtPrompts` / `generateScenarioSceneArt`; `useLibraryState` handlers (grounding on active setting from `draft.settingId`; `editScenario` prefill; submit persists all three fields). 2 new API tests.
- **Phase 4 — UI:** `EntityModal` gains a compact 16:9 Scene-art preview box + "✎ Edit image" button in the agentic aside + a wired `SceneArtModal`; modal widened to `sm:w-[520px] md:w-[920px] lg:w-[1120px]`. `ScenarioCard` shows a scene-art banner (`h-[96px]`, full bleed via negative margins) when `image` is set. 7 new component tests (EntityModal × 5, ScenarioCard × 2).
- **Phase 5 — docs + validation:** `api-contract` (Scenario endpoints table + Scenario Authoring Shapes section; ScenarioRead shape); `data-flow` (Scenario Scene Art Flow + cleanup referenced update); `documentation.md` current status; `checklist.md` this entry.
- **Validation:** 273 backend tests (ruff + mypy clean) + 180 frontend tests (typecheck + lint + build clean).
- **Decisions (autonomous):** reused `SceneArtPromptResponse` and `scene_art.generate_scene_art` unchanged (same shape, id-agnostic); the system prompt is scenario-specific (no characters, emphasizes setting shaped by tone); active setting name/desc passed as grounding context via `draft.settingId` lookup.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** same standing constraint (shared working dir, `preview_start` can't reuse the external dev server on 3346). Verified via the green suites + `next build` + structure reasoning: `EntityModal` reuses the focus-trapped `Modal`; `SceneArtModal` is the same component as Settings; the scene-art preview box is `aspect-[16/9]`; `ScenarioCard` banner uses `pointer-events-none` + `z-[1]`. Run the in-browser pass once the dir is free.

### Typography Scale + Font-Size Settings (done; `feat/typography-scale`; plan: `docs/plans/typography-scale-font-size-settings.md`)
Frontend-only. Five phases, commit-per-phase. Added a six-token CSS font-size scale (`--fs-eyebrow / --fs-label / --fs-ui / --fs-body-sm / --fs-body / --fs-tag`) with four user-selectable presets (Compact / Default / Comfortable / Large) persisted to `localStorage`. The AppearanceTab in Options gained a "Text size" preset picker. `Eyebrow`, `Tag`, and `Button` primitives now read from CSS variables; all `size={8.5}` / `size={9}` Eyebrow overrides and `text-[9.5px]` / `text-[8px]` hardcoded sizes across cards, modals, and layout components were replaced with scale-aware utilities (`text-eyebrow`, `text-tag`, `text-body-sm`, etc.). Design-system.md updated with the Typography Scale section.
- **Validation:** 175 frontend tests (typecheck + lint clean). Backend untouched.
- **Decisions:** Display/heading Cinzel sizes (16–26px) excluded from the scale — they are already readable. The "Default" preset bumps the old 8.5–9px Eyebrow text to 11px. `velora-font-size` localStorage key, `.fs-*` class on `<html>`.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** same standing constraint (shared working dir, `preview_start` can't reuse the external dev server on 3346). Verified structurally: `AppearanceTab` text-size section is a `role="radiogroup"` with `aria-checked` buttons; the Eyebrow, Tag, Button primitives are non-interactive elements whose font size changes have no a11y surface impact; contrast is unchanged (colors untouched). Run the in-browser pass once the dir is free: open Options → Appearance and cycle through all four presets to confirm card/label text scales visually.

### Finish Scene Creation — Cast/Setting dropdowns + agentic Scenario Creator (done; `feat/finish-scene-creation`; plan: `docs/plans/finish-scene-creation.md`)
Brought Scene Creation to completion: the scenario editor's chip grids became dropdowns, and the scenario "Draft with Velora" became a real backend agent. Six phases, commit-per-phase, merged to `main`. **No DB migration** (no new columns — every drafted field already lives on `Scenario`).
- **Phase 1 — `MultiSelect` primitive:** new `components/ui/MultiSelect.tsx` — a button-trigger `listbox` popover with a `multiple` prop (checkbox-style multi-select vs single-choice replacement), outside-click + Esc close, roving focus (Arrow/Home/End, Enter/Space toggle), `Monogram`/`◆`-seal row affordances, chip/name trigger display. 5 Vitest cases.
- **Phase 2 — `ScenarioForm` dropdowns:** the two `ToggleChip` grids → side-by-side `grid grid-cols-1 sm:grid-cols-2` pair of `MultiSelect`s — **Cast** (`multiple`, wired to `draft.cast` via `setDraft`) + **Setting** (single, `draft.settingId`, replace-never-append), portraits via `mediaUrl`, empty-state copy for new worlds. Dropped the now-unused `toggleCast` prop from the form. 4 Vitest cases.
- **Phase 3-4 — backend agent + route + tests:** `ScenarioDraftRequest`/`ScenarioDraftResponse` (camelCase `castIds`/`settingId`); `agents/scenario_agent.draft_scenario` — requires a seed, `resolve_llm`, grounds via shared `_common.world_context`/`docs_block`, hands the model a **numbered roster** of the storyline's real characters + settings (capped 40 each) and resolves the returned **names → ids** (case/whitespace-folded), **dropping** unknown cast + falling back to `""` for an unmatched setting. `POST /scenarios/draft` declared **above** `/scenarios/{id}`. 7 offline-mocked tests (seed-required 400, LLM-unconfigured 400, world grounding, roster reaches prompt, cast names resolve, unknown cast dropped, unknown setting → "").
- **Phase 5 — frontend wiring:** `lib/api.draftScenario` + `ScenarioDraftResult`; `useLibraryState.draftScenario()` (mirrors `draftSetting`) merges title/genre/tone/goal/opening + the roster-resolved cast/settingId + `_ai`; **retired the client-only `generate()` stub** (and its `AI_*` fake-data imports + `generateTimer`); `Draft` gained `opening` and `submit()` now persists the drafted opening; `EntityModal`'s "❖ Draft with Velora" → `lib.draftScenario`. 3 hook tests + `api-mock` extended.
- **Phase 6 — docs + validation + merge:** component-map (`MultiSelect`; `ScenarioForm` dropdowns; `EntityModal` draft now live), api-contract (`POST /scenarios/draft` + Scenario Authoring Shapes), data-flow (Scenario Authoring Flow), documentation status; merged.
- **Validation:** 266 backend tests (ruff + mypy clean; 7 new) + 171 frontend tests (typecheck + lint + `next build` clean; 12 new across MultiSelect/ScenarioForm/hook).
- **Decisions (from the plan, ratified with the user):** Setting stays **single-select** (one per scenario — no schema/cardinality change); the agent **never trusts model ids** — it returns roster names and resolves+drops server-side; grounding is **shared** with the character/setting agents (`_common`); the first cut grounds on **seed + world roster** only (no `ContextFilesPanel` for scenarios yet) and does **not** draft branches.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (3346) runs under the user's `python app.py`; `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified via the green suites + `next build` + structure reasoning: `MultiSelect` trigger is `aria-haspopup="listbox"`/`aria-expanded`; the popover is `role="listbox"` (+ `aria-multiselectable` when `multiple`) with `role="option"`/`aria-selected` rows; roving `tabIndex` + Arrow/Home/End/Enter/Space; Esc/outside-click close return focus to the trigger; visible `:focus-visible` ring; the Cast/Setting pair is `grid grid-cols-1 sm:grid-cols-2` so it collapses to one column on mobile. Run the in-browser pass once the dir is free: open a scenario, operate both dropdowns by keyboard, and confirm a drafted scene's cast/setting are real world members.

### New Storyline context UX — upload-as-category + 32K budget + contained scroll (done; `feat/storyline-context-upload-ux`; plan: `docs/plans/storyline-context-upload-ux.md`)
Three New Storyline page improvements + a budget bump. Mostly frontend (the budget touches a few backend caps). Five phases, commit-per-phase, merged to `main`.
- **Phase 1 — budget 8K → 32K:** `_common.DOCS_CAP` + `triage._TOTAL_CAP` 8000 → 32000; `readDocs.DOCS_CHAR_CAP` 8000 → 32000; `contextBudget.DRAFT_DOCS_CAP_TOKENS` 2000 → 8000 (= 32000 chars). Tests reference the caps symbolically (stayed green).
- **Phase 2 — upload-as-category data layer:** `toCreatorDoc(doc, opts)` applies an upload target (category + Draft/RAG); a real category marks the doc triaged. `useStorylineCreator.addFiles(files, opts)` threads the target to new (and re-dropped) docs; `triage()` now sweeps only `category === "select"` (Uncategorized) docs, preserving manual/bulk buckets.
- **Phase 3 — TriagePanel upload UI:** an "Add as" row (category `<select>` + Draft/RAG default chips) in the sticky top; the drop zone + Browse pass the chosen target so a whole batch lands pre-categorized. Triage button scoped to Uncategorized (`⚖ Triage Uncategorized (N)`, disabled when none). Per-row + upload selects share `CATEGORY_OPTIONS`.
- **Phase 4 — self-contained scroll:** `StorylineCreatorView` `<main>` is `flex h-dvh overflow-hidden flex-col md:flex-row` at **every** width (was gated at `lg`) — the page no longer scrolls as one long body. `TriagePanel`/`WorldBuildPanel` roots are contained (`min-h-0 max-h-[42dvh] md:max-h-none md:w-[360px] md:self-stretch`) with `min-h-0` inner scroll bodies: two columns at md+, a bounded scrollable context strip below md.
- **Phase 5 — docs + validation + merge:** data-flow (upload-as-category + Triage-Uncategorized + 32K), api-contract (grounding cap), documentation status; merged.
- **Validation:** full backend suite (ruff + mypy clean) + frontend (typecheck + lint + `next build` clean); new/updated tests for `toCreatorDoc` opts, bulk add, triage-skips-categorized, and the upload-target controls.
- **Decisions (from the user):** upload picker lets a batch be dropped straight into Character/Setting/Other (or left Uncategorized) with Draft/RAG defaults; **Triage handles the Uncategorized leftovers**; the budget is **32K chars** (up from 8K); the page must be **self-contained on one screen** with separate Main/Context scroll areas.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (3346) runs under the user's `python app.py`; `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified via the green suites + `next build` + structure reasoning (page is exactly `h-dvh`/no page or horizontal overflow; both panes own `overflow-y-auto` with `min-h-0`; the upload `<select>` is `aria-labelledby` a visible label; Draft/RAG chips are `aria-pressed`; sticky left-pane footer stays reachable). Run the in-browser pass once the dir is free: pick a bucket, drop a batch, confirm pre-categorization + the contained two-pane scroll at each width.

### Backend-controlled reasoning budget (done; `feat/reasoning-budget`; plan: `docs/plans/reasoning-budget-control.md`)
Caps local reasoning models' hidden *thinking* tokens **per authoring operation** so triage finishes fast (the user's 10–20s/doc → ~Low) instead of the model over-thinking a 3-way classification. **Backend-only**, no frontend change, no user-facing toggle. Five phases, commit-per-phase, merged to `main`.
- **Phase 1 — vocabulary + detector:** `schemas/reasoning.py` (`ReasoningEffort` Low/Medium/High/VeryHigh/Max → 256/512/1024/2048/4096); `services/llm_backend.py` — probe `GET /version` (vLLM) / `GET /props` (llama.cpp), TTL cache keyed by base URL, `apply_reasoning` injects the engine-specific key (no-op for unknown). Config + `.env.example`: `LLM_BACKEND_POLL_SECONDS` (30) / `LLM_BACKEND_CACHE_TTL_SECONDS` (60).
- **Phase 2 — `chat_complete` wiring:** keyword-only `reasoning=` on `services/llm.chat_complete`; when set it detects the engine (cached) and adds `thinking_token_budget` (vLLM) / `thinking_budget_tokens` (llama.cpp); unchanged on OpenAI/unknown. Local import breaks the `llm ↔ llm_backend` cycle.
- **Phase 3 — per-call efforts:** `_common.DEFAULT_AUTHORING_EFFORT = Medium`; **triage = Low**; storyline/character/setting agents forward a `reasoning` param (default Medium); build runs blueprint + per-entity drafts at Medium. None of it user-controllable.
- **Phase 4 — poller + diagnostics:** lifespan `asyncio` poller re-probes the configured endpoint every `POLL_SECONDS` (adapts to engine swaps; cancelled on shutdown; skipped under SQLite); `GET /options/llm/backend` returns the detected engine + budget map (`LlmBackendResponse`).
- **Phase 5 — docs + validation + merge:** api-contract (Reasoning budget + endpoint), data-flow (Reasoning-Budget Flow), workflow/deployment/.env (env vars), documentation status; merged.
- **Validation:** 224 backend tests (ruff + mypy clean) — 10 new across detection, injection, per-call effort, the endpoint, and the poller. **Frontend untouched** (no run needed).
- **Decisions (from the user):** the budget ladder is Low 256 / Medium 512 / High 1024 / Very High 2048 / Max 4096; effort is **backend-set, not user-facing** for Storyline/Character/Setting creation; **Triage = Low**, **Build the whole world = Low/Medium** (Medium chosen for the generative calls); only **vLLM + llama.cpp** supported for now; auto-detect the engine and re-check periodically so the server adapts to changes.
- **Operator note:** the budget only takes effect when reasoning is enabled server-side — vLLM `--reasoning-parser <model>`; llama.cpp `--jinja --reasoning on` with **no** CLI `--reasoning-budget` (a CLI budget overrides the per-request one). An OpenAI / undetected endpoint behaves exactly as before (no key sent).
- **Deferred:** the vLLM native `reasoning_effort` enum (we use the numeric `thinking_token_budget` for exact, uniform control across all five levels); a "none" (fully-disabled-thinking) tier; surfacing the detected engine/budget in the Options UI (the diagnostic endpoint exists, but per "backend only" no UI was added).

### Build from attached docs only + ComfyUI down-guard (done; `fix/build-from-attached-docs`)
Two bug fixes to **Build the whole world**, from user reports.
- **(1) Cast/settings come ONLY from attached docs.** The build was inventing a cast/settings via the blueprint, ignoring the author's attached character/setting docs. Now: `BuildWorldRequest` carries `character_docs` / `setting_docs` (`BuildDoc{name,text}`); `build_agent.iter_build_world` creates **exactly one character per character-doc and one setting per setting-doc**, drafted from that doc, and **invents nothing** — no character docs → `characters == []` (likewise settings). The blueprint is now used only for the **stat schema** (its invented concepts are discarded). Attached docs alone are valid context (build with no seed/lore). The frontend (`useStorylineCreator.build()`) sends the docs categorized `character`/`setting`; the `plan` event's skeleton labels are the doc names.
- **(2) Stop hammering a down ComfyUI.** `imagesAvailable` only means a URL is *configured*; a stopped server 502'd on every entity. Now the build runs a **status preflight** (`checkComfyStatus`) before rendering and skips images entirely if unreachable, and both `renderProposalImages` (build) and `commitWorld` (commit) have a **circuit breaker** — the first failed render stops all further image calls (entities are still created, just without art).
- **Validation:** 202 backend tests (ruff + mypy clean; build tests rewritten for the no-invention behavior + per-doc cast) + 143 frontend tests (typecheck + lint + build clean; new tests for docs-as-cast, the circuit breaker, and the reachability skip).
- **Decisions (from the user):** the build must create **only** the attached characters/settings — never invent; if none are attached, none are created.
- **Follow-up — cast/settings are uncapped (`fix/uncap-attached-cast`, merged):** the old `MAX_CHARACTERS=6` / `MAX_SETTINGS=5` caps truncated large imports (a user attached 40+/50+ and got 6/5). Since the cast now comes only from attached docs (nothing invented), `_doc_sources` no longer caps — **every attached character/setting doc becomes an entity**. Only the invented stat schema stays bounded (`MAX_STATS=8`). (Practical limit is just time/cost — N sequential draft calls + N image renders.)

### Self-Triage — default "Select" category for dropped context docs (done; `main`; plan: `docs/plans/self-triage-category-select.md`)
Frontend-only. Two phases, commit-per-phase, merged directly to `main`.
- Adds `"select"` to the frontend `DocCategory` union as a UI-only placeholder for uncategorized docs (`lib/types.ts`).
- `toCreatorDoc` now defaults new docs to `category: "select"` instead of `"other"`; `docToContextInput` maps `"select"` → `"other"` at the API boundary so the backend never sees the new value.
- `TriagePanel` dropdown now starts with "Select" and offers Character / Other / Setting in that order (per user spec).
- Grouping trigger changed from `anyTriaged` → `anyGrouped` (fires on any manual *or* AI categorization); an **"Uncategorized"** bucket at the top of the grouped view holds docs still at `"select"`. GROUPS reordered: Character / Other / Setting.
- 3 new frontend tests (139 total); typecheck + lint + build clean.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** same standing constraint (shared working dir, `preview_start` can't reuse the external dev server on 3346). Verified structurally: `<select>` is a native focusable with an associated `<label>`; the "Uncategorized" bucket uses the same heading/list markup as the other groups; the panel layout is unchanged. Run the in-browser pass once the dir is free.

### Live Agentic Streaming — watch worlds build in real time (done; `feat/live-agentic-streaming`; plan: `docs/plans/live-agentic-streaming.md`)
Turned the New Storyline page's blocking build + triage into **live, progressively-rendered** experiences over **NDJSON streaming POSTs**. Seven phases, commit-per-phase, merged to `main`.
- **Phase 1 — streaming build:** `BuildEvent` discriminated union (`status`/`meta`/`primer`/`plan`/`character`/`setting`/`done`/`error`) in `schemas/build.py`; `build_agent.iter_build_world` generator + `validate_build_inputs` pre-flight; `build_world` rewritten as a thin collector (existing behaviour/tests unchanged); `POST /storylines/build/stream` (`StreamingResponse`, `application/x-ndjson`, `Cache-Control: no-cache` + `X-Accel-Buffering: no`) with up-front `400`/`502` validation and an in-band terminal `error` event for mid-stream failures.
- **Phase 2 — streaming triage:** `TriageEvent` union; `triage_agent.classify_document` (single-doc) + `iter_triage_documents` (**per-file**, one LLM call each, per-doc fallback to Other/RAG-on, never aborts) + `validate_triage_inputs`; `POST /storylines/triage/stream`. Batched `triage_documents` kept for back-compat.
- **Phase 3 — FE data path:** `lib/api.postNdjson` (fetch + `ReadableStream` line-buffered reader, error-envelope decode) + `buildWorldStream`/`triageDocumentsStream`; `BuildEvent`/`TriageEvent` types + optional `portrait`/`image` on the proposed types; `useStorylineCreator.build()`/`triage()` consume the streams (incremental `proposed` via `upsertAt`, `planConcepts`, `buildStage`, `triageActive`, `AbortController` cancel-on-unmount).
- **Phase 4 — live world panel:** new `WorldBuildPanel` (right column; skeleton "drafting…" cards from `planConcepts`, filled per `character`/`setting` event, then editable review) **absorbing + retiring `ProposedWorldReview`**; `StorylineCreatorView` right pane is state-driven (`building || proposed` → `WorldBuildPanel`, else `TriagePanel`); `TriagePanel` gained an `aria-label` landmark.
- **Phase 5 — live images at commit:** `commitWorld` gained an `onEntity` callback emitting `{portrait}`/`{image}` patches as each ComfyUI render finishes; the hook patches the displayed `proposed`, so previews pop into the right column live during *Create World* (only kept entities rendered — per the user's choice).
- **Phase 6 — live triage UI:** `TriagePanel` shows the in-flight file's "classifying…" badge + `Triaging i/total…` + an `aria-live` status as each row fills in.
- **Phase 7 — docs + validation + merge:** full sweep; merged.
- **Validation:** 199 backend tests (ruff + mypy clean) + 134 frontend tests (typecheck + lint + build clean — both `/stream` routes + the streaming consumer covered).
- **Decisions (from the user):** triage is **per-file/genuinely live** (N LLM calls, accepted); images **initially** rendered during the *Create World* commit — **superseded** by the follow-up below (now rendered during the build).
- **Follow-up — images render during the build (`feat/build-generates-images`, merged):** per the user, **Build the whole world** now renders portraits + scene art **live during the build** whenever ComfyUI is available, not only at commit. `storylineCreator.renderProposalImages` generates an image per proposed entity (id-agnostic endpoints → no persistence; best-effort, skips entities that already have one); `useStorylineCreator.build()` runs it after the text stream (gated on `imagesAvailable && generateImages`) under the build `AbortController`; `commitWorld` now **attaches** already-rendered images (and only renders fresh for any still missing). `WorldBuildPanel`'s `committing` prop became `renderingImages` (= `committing || buildingImages`); Create World is disabled during the text build and aborts the image loop if clicked mid-render (no double-render/race). +2 frontend tests (136 total; typecheck + lint + build clean).
- **Transport choice:** **NDJSON over a streaming POST** (not SSE/WebSocket) — authoring is request/response with a body, so `EventSource` (GET-only) doesn't fit and no session/pub-sub channel is needed. Aligns with the documented "NDJSON event stream" direction.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (3346) runs under the user's `python app.py`; a second Turbopack server clashes on `.next` and `preview_start` can't reuse the external one (standing constraint, every prior entry). Verified via the green suites + `next build` + structure reasoning (`WorldBuildPanel`/`TriagePanel` are labelled landmark regions; `aria-live="polite"` on stage/triage status; reduced-motion-safe shimmer via `motion-reduce:animate-none`; cards keyboard-reachable; portrait/scene-art `<img>` `alt`). Run the in-browser pass once the dir is free: build a world and watch the fields + right-column cast/settings + image previews fill in live, and triage a few dropped files one-by-one.
- **Deferred follow-ups:** the **standalone Character/Setting modal drafts** stay on their working-spinner (single JSON-returning call — field-by-field streaming needs fragile partial-JSON parsing); **token-level prose streaming** (the World Primer "typing out" char-by-char) would need a `stream=true` primitive in `services/llm.py` — not required for staged liveness.

### New Storyline Page — agentic, triage-driven world build (done; `feat/new-storyline-page`; plan: `docs/plans/new-storyline-page.md`)
Replaced the storyline create/edit **modal** with a dedicated full **page** and turned the context-files area into a real **Triage → Build** pipeline. Seven phases, commit-per-phase, merged to `main`.
- **Phase 1 — context corpus:** `ContextDocument` model (`category` character/setting/other + `includeDraft`/`includeRag` + `charCount`), schemas (incl. Triage shapes), CRUD (`list/create/bulk/update/delete`), routes (`/storylines/{id}/context-docs[/bulk]`, `/context-docs/{id}`). The **durable RAG-corpus seam** — text persisted per storyline; chunking/embeddings/retrieval still deferred (nothing reads `content` at runtime).
- **Phase 2 — triage agent:** `agents/triage_agent.py` + `POST /storylines/triage` — one LLM call classifies each dropped doc (single character → character; single place → setting; multiple/mixed/general → other) with **Draft** (world-setting docs only) + **RAG** (default on); per-doc fallback; empty docs skip the LLM.
- **Phase 3 — build orchestrator:** `agents/build_agent.py` + `POST /storylines/build` — drafts a reviewable `ProposedWorld` (metadata → World Primer → a **blueprint** call for the stat schema + cast/setting concepts → one draft per character → one per setting). Builds from seed, docs, or both (seed optional with context); bounded counts (≤6 chars / ≤5 settings / ≤8 stats); proposed stats sanitized to valid ranges. **Persists nothing** (review-then-commit).
- **Phase 4 — frontend data path:** `lib/api` (triage/build/context-docs/getStoryline), `lib/types`, `lib/contextBudget` (char/4 estimate + primer/draft caps), `useStorylineCreator` + `storylineCreator` (field model, proposed→CRUD mappers, `applyTriage`, `persistStatsDiff`, `commitWorld`). **KG toggle removed** from `readDocs` + `ContextFilesPanel`.
- **Phase 5 — the page:** `StorylineCreatorView` (`/storylines/new` + `/storylines/[id]/edit`) + `TriagePanel` + `ContextBudgetMeter` + `ProposedWorldReview`; **`StorylineModal` retired** (menu/Library navigate via `useRouter`); **empty-state** `/` → `/storylines/new`. Literal `storylines/` resolves before `/[storylineId]` (clean `next build`).
- **Phase 6 — commit + images:** `commitWorld` renders **opt-in** ComfyUI portraits/scene-art best-effort per entity (gated on `getSettings` `comfy.baseUrl`; a failed render never aborts); grounding capped via `concatDocs` (budget enforcement).
- **Phase 7 — docs + validation + merge:** full sweep; merged.
- **Validation:** 190 backend tests (ruff + mypy clean) + 124 frontend tests (typecheck + lint + build clean — both new routes register).
- **Decisions (from the user):** triaged docs **persist to Postgres** (durable seam; retrieval deferred); the build is **end-to-end, review-then-commit** (images opt-in, only if ComfyUI configured); **both create + edit moved to the page** (modal retired).
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** a `next-server` already runs on **3346** in the shared working dir (the user's `python app.py`); `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified structurally (`main`/`h1` landmarks; section `aria-label`s; `FieldLabel`/`aria-label`-associated inputs + the `sr-only`-labelled category `<select>`; `aria-pressed` Draft/RAG toggles; `role=alert`/`status`, labelled `role=progressbar`; fluid `lg:grid-cols-[1fr_360px]` collapsing below `lg`, `sm:grid-cols-2` title+genre, `flex-wrap` hero/footer) + the green suites + `next build`. Run the in-browser pass once the dir is free: drop files → Triage groups them; Build → review → Create World persists + navigates; budget meter levels; edit-mode prefill.
- **Deferred — RAG retrieval:** the corpus is stored but **nothing reads `content` yet** — chunking, embeddings, hybrid search, and runtime retrieval remain the next plan. Also deferred: editing/removing **already-persisted** context docs from edit mode (edit only appends new uploads); the build's per-character starting stats default to the schema (no per-character LLM proposal). (Orphaned-image cleanup is now **resolved** — see the Maintenance entry below.)

### UUID routing — storyline-scoped URLs + short ids (done; `feat/uuid-routing`; plan: `docs/plans/uuid-routing.md`)
Storyline-scoped, shareable routing plus tidier ids. Three phases, commit-per-phase, merged to `main`.
- **Phase 1 — backend short ids:** `app/core/ids.py` gains `new_hex_id(length)` (bare, prefix-free hex; `new_id(prefix)` unchanged for every other entity). Storyline default → **8-hex**, scenario default → **4-hex**. `services/crud.py` `_gen_hex_id(db, model, length)` generates **collision-checked** ids (retries, widens by a char if saturated) for the no-client-id create path. `docs/api-contract.md` gained an **Identifiers** note. Seed slugs (`embergate`) and client-supplied ids still pass through (string PKs). 5 new id tests; 171 backend tests green (ruff + mypy clean).
- **Phase 2 — frontend routes + URL sync:** new `app/[storylineId]/page.tsx` (`/{storylineId}` → Library, `initialStorylineId`) and `app/[storylineId]/[scenarioId]/page.tsx` (`/{storylineId}/{scenarioId}` → story player, `backHref`); `app/play/[scenarioId]/page.tsx` converted to a seed-resolving `redirect()` for old links. `useLibraryState(initialStorylineId?)` prefers the deep-linked id on load and mirrors the active storyline into the URL via `history.replaceState` (load + switch + create + delete); `LibraryView`/`BeginSceneModal` thread `storylineId` (Enter Scene → `/{storylineId}/{scenarioId}`); `StoryPlayerView` threads `backHref`. 5 new frontend tests (route render, redirect, URL sync) + the updated Enter-Scene link assertion; 103 frontend tests green (typecheck + build clean). `docs/routes.md` + `docs/component-map.md` updated.
- **Phase 3 — validation + docs + merge:** full suites green; `docs/documentation.md` status + this entry; merged to `main`.
- **Decisions (from the user):** storyline ids = **pure 8-hex**, scenario ids = **pure 4-hex** (no prefix); URL shape dropped the literal `/storyline/` segment in favor of root-level **`/{storylineId}/{scenarioId}`**. Other entity ids unchanged.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (port 3346) + backend (3345) run under the user's `python app.py`; `preview_start` can't reuse them and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). The new routes are thin wrappers around already-audited views (`LibraryView`, `StoryPlayerView`) — no new interactive surface beyond the existing keyboard-reachable `SceneHeader` back-link. Verified via the green suites + `next build` (routes registered) + structure reasoning. Run the in-browser pass once the dir is free: deep-link `/{storylineId}`, confirm `/` rewrites, Enter Scene → `/{storylineId}/{scenarioId}`, and legacy `/play/{id}` redirects.
- **Known caveat:** root-level `/{storylineId}` coexists with static routes (`/options`, future `/sign-in`) — Next.js resolves static segments first (verified by a clean `next build`), but any *new* top-level static route must keep that precedence in mind.

### The Story Graph — Neo4j substrate (done; `feat/story-graph-neo4j`; plan: `docs/plans/story-graph-neo4j-substrate.md`)
Committed the Story Graph to **Neo4j** — the substrate realization (§6), Type Registry (§1.4), authoring write path (§6.5), and template read path (§7.2/§7.4) of `Documents/Plans/5.4_story-graph-structure-prep.md`. The prior prep phases produced node *properties* only; this phase builds the graph itself. Five phases, commit-per-phase, merged to `main`. See `docs/story-graph-neo4j.md`.
- **Phase 1 — substrate & lifecycle:** `neo4j` driver dep; `neo4j_uri/user/password` config + `neo4j_configured`; `.env.example` activated (Bolt **3349** / Browser **3350**); **custom container** `web/backend/docker/neo4j/Dockerfile` (`neo4j:5.26-community` + APOC + dynamic-label config) + a `neo4j` compose service (TCP healthcheck, named volume); `app.py` builds it (`up -d --build --wait`, `pull --ignore-buildable`); `app/core/neo4j.py` — lazy graceful driver singleton, `ping`, READ/WRITE session context managers (§7.4), injectable `build_driver`; optional non-gating preflight check.
- **Phase 2 — Type Registry (§1.4):** `GraphTypeDefinition` model (global built-ins via NULL `storyline_id`; per-storyline user types; unique scope); schemas (edge-requires-valence validator); `content/graph_registry.py` (§5 seed catalogue — Character/Setting/Event/Secret/Faction/Consequence nodes + 17 edges with valence + feeling decay); `services/type_registry.py` (idempotent seed, CRUD, `validation_rules` + `schema_blob`, hot-path staging §10); `routes/graph.py`; preflight seeds built-ins.
- **Phase 3 — write path (§6):** `services/graph_writer.py` — dynamic-label `upsert_node` (§6.2), `upsert_edge`, `delete_node`, `attach_consequence` (reified `:Consequence`, §6.4), `ensure_constraints` (§6.6), registry validation (§6.5); **best-effort** `sync_character`/`sync_setting`/`remove_node` hooked into CRUD after commit (never blocks).
- **Phase 4 — read path (§7.2/§7.4):** `services/graph_reader.py` — `ensure_scenario_materialized` (idempotent upsert of cast+setting + `present_at` edges, so the seed world appears on first load), read-only Cypher templates, graceful `scenario_graph` orchestrator; `GET /api/scenarios/{id}/graph`.
- **Phase 5 — frontend seam + docs:** `lib/api.ts` (`getScenarioGraph` + graph-types CRUD) + `lib/types.ts` (`GraphNode`/`GraphEdge`/`ScenarioGraph`/`GraphTypeDefinition`) + `api-mock` + `lib/api.test.ts`; full docs sweep + `docs/story-graph-neo4j.md`.
- **Validation:** 165 backend tests (ruff + mypy clean) + 98 frontend tests (typecheck + lint + build clean). **Live-validated against the running container** (Phases 1/3/4): custom image builds + Healthy; driver connects; dynamic labels (§6.2) → `[Character, Node]`; APOC 5.26.27; READ-session writes rejected server-side (§7.4); node upsert/clear/remove; scenario materialize → 3 nodes + 2 `present_at` edges, idempotent on reload.
- **Decisions (from the user):** Neo4j is **graceful/best-effort** (never blocks CRUD; optional preflight; tests run with no Neo4j); scope = **substrate + sync + template read** now; user-defined types **staged** until promoted (§10).
- **Deferred seams (by design — prerequisites absent):** the **§8 async turn-writer** (needs a turn loop / story engine), the **§7.1 vector entry-point** (needs an embedding stack + native vector index), **§7.3 Text2Cypher** (the `schema_blob` is compiled + ready; live generation awaits a hot-path consumer, staged per §10), and a **graph-visualization UI** (the API-client + types seam exists; no component yet).
- **Deferred — live in-browser a11y/responsive pass:** no new UI surface (API-client + types only), so nothing to pass; the standing shared-dev-server (3346) constraint is unchanged. Re-confirm when a graph-viz component lands.

### Agentic Setting Creator — Setting-node prep (done; `feat/setting-creator-agentic`; plan: `docs/plans/setting-creator-agentic.md`)
Brought the **Setting** creator to parity with the Character/Storyline creators and enriched the Setting to carry the **Setting Node** metadata of `Documents/Plans/4.story-graph-structure-prep.md` §4.1. The **preparation phase for the Setting node**: §4.1 *node properties* only — **no graph** (no edges, no Event/Faction nodes, no Neo4j). Five phases, commit-per-phase, merged to `main`.
- **Phase 1 — data layer:** nullable `atmosphere`/`features`/`current_state` (Text), `image` (String), and `timeline` (JSON, default `[]`) columns on `Setting` (model/schemas/create-CRUD/seed); self-heal via the additive-column reconcile. `SettingRead.timeline` coerces `None → []` for reconciled rows. Embergate's 5 settings gained atmosphere/features/current_state. Roundtrip + null-default + node-metadata tests.
- **Phase 2 — agent + render:** `agents/setting_agent.py` — `draft_setting` (seed → name/type/desc + §4.1 metadata, optionally grounded in the world + dropped docs) and `generate_scene_art_prompts` (watercolor **establishing-shot**, no people). Shared `agents/_common.world_context` (character agent refactored onto it). `services/media.py` (extracted `to_webp`/`save_webp`; portraits refactored onto it) + `services/scene_art.py` (`generate_scene_art`, landscape 16:9, `/media/scenes`). Routes `POST /settings/{draft,scene-art-prompts,scene-art}` on fixed sub-paths. 11 new offline-mocked tests.
- **Phase 3 — frontend data path:** `Setting`/`Draft` gain the fields + `SettingTimelineEntry`; `api.ts` adds `draftSetting`/`generateSceneArtPrompts`/`generateSceneArt`; `useLibraryState` handlers (reusing the draft/prompt/render spinners) + `editSetting` prefill + `submit` setting branch; api-mock extended.
- **Phase 4 — UI:** new `SettingModal` (mirrors `CharacterModal`: name + Type chips + Description/Atmosphere/Features/Current state, agentic Draft seed, Scene-art preview + Edit-image → `SceneArtModal`, read-only **Event timeline** seam, shared `ContextFilesPanel`) + `SceneArtModal` (the `PortraitModal` analog, 16:9). `EntityModal` → **scenario-only** (`SettingForm` deleted as orphaned); `SettingCard` shows the scene-art banner when set; `SettingModal` mounted in `LibraryView`. 4 new frontend tests.
- **Validation:** 117 backend tests (ruff + mypy clean) + 96 frontend tests (typecheck + lint + build clean).
- **Decisions (autonomous):** authored §4.1 buckets = base description + current state; the **event timeline** is play-accrued and ships **empty** (seam only — no async writer yet); **edges out of scope** (graph deferred, like the Character prep); an opt-in **establishing image** was added for true Character-creator parity (reuses the ComfyUI pipeline); **no setting stats** (§3/§4 scope stats to the Character node).
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (port 3346) runs under the user's `python app.py`; `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified via the green suites + build + structure reasoning (`SettingModal`/`SceneArtModal` reuse the focus-trapped `Modal`; labelled `TextField`/`TextArea`; `ToggleChip` type picker; `splitScroll` gated to `lg+`). Run the in-browser pass once the dir is free.
- **Deferred follow-ups:** scene-art generation needs a configured + running ComfyUI server; the §4.3 edges (`from`/`present_at`/`controls`/`occurred_at`/`connected_to`) and the async timeline writer are the graph build, **out of scope** here by design.

### Seal pop-up + Storyline statistics (done; `feat/seal-popup-and-stats`; plan: `docs/plans/seal-popup-and-storyline-stats.md`)
Two editor changes, 6 phases, commit-per-phase, merged to `main`. **(1) Seal** — the storyline seal moved out of the cramped inline picker into a dedicated **`SealModal`** pop-up (mirrors `PortraitModal`): a large live preview, the shape set expanded **8 → ~24** glyphs, a curated color palette, and a native **color wheel** (`<input type="color">`) for any custom hex; `StorylineModal` now shows a compact **Seal row + Edit** button. The per-character accent **`PALETTE` grew 8 → 12** colors. **(2) Statistics** — the backend stat seam already existed end-to-end; this added the missing pieces: a `bands` JSON column on `StatDefinition` (labeled value **"tickers"**, e.g. Health `0–20` = "Nearly dead"), **freely editable** definitions (range edits **re-clamp** existing character values), a **DELETE** route that **prunes** the stat's values from every character, and the band meanings folded into the Character creator's **band-aware** `propose_starting_stats`. Frontend: `StatDefinition`/`StatBand` types, the `listStatDefinitions`/`create`/`update`/`delete` API client, a **`StatsEditor`** section under World Primer in `StorylineModal`, and a load→diff→persist path in `useLibraryState` (create/update/delete diff on save; new-storyline stats persisted against the returned id). Seed: Embergate's Health/Suspicion gained sample bands.
- **Validation:** 104 backend tests (ruff + mypy clean) + 92 frontend tests (typecheck + lint + build clean).
- **Decisions (from the user):** stats live **inline under World Primer** (not a pop-up); each stat carries labeled **bands** for future state-extraction; stats are **freely add/edit/removable**; characters keep the **12-color** cap (4 added). Seal is its own pop-up with 2–3× shapes + a color wheel.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (port 3346) runs under the user's `python app.py`; `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified via the green suites + build + structure reasoning (`StatsEditor`/`SealModal` reuse the focus-trapped `Modal`; labelled number/text inputs; `role=group` per stat + on the seal symbol/color grids; the color wheel is a labelled `<input type=color>`; the Seal/Statistics rows wrap and are gated like World Primer below `lg`). Run the in-browser pass once the dir is free.
- **Known caveat (carried):** the universal-stat re-clamp on range narrowing fixes stored *character* values, but bands themselves are not auto-clamped to a narrowed range — an out-of-range band is allowed (authoring is iterative); surfaces only as a stale label until edited.

### Character & Storyline editor overhaul (done; `feat/editor-overhaul`; plan: `docs/plans/character-storyline-editor-overhaul.md`)
Frontend-only restructure of the create/edit modals (5 phases, commit-per-phase). **(1) Shared chrome** — `FieldLabel` is now **bold + slightly larger** (legible editor headers across every form); `Modal` gained an opt-in **`splitScroll`** prop so a two-column panel's edit column and Context-Files column **scroll independently at `lg+`** (whole-dialog scroll below `lg`); `ContextFilesPanel` gained a paired `scroll` prop. **(2) Character form** — **Goal/Secret removed from the editor** (kept on the `Character`/`Draft` wire shape, in `submit()` defaults, the agent draft, and the read-only profile — only the form fields are gone); the **Voice / speech style** field now sits beside a **non-functional voice-sample upload** placeholder (disabled, "coming soon") for the planned text-to-speech feature. **(3) Portrait → pop-up** — the portrait flow moved into a new **`PortraitModal`** (nested `Modal`, raised z): preview + editable positive/negative prompts + **Generate prompts** (from context) + **Generate portrait**; the right column now shows a **compact portrait preview + "Edit image"** button above Draft with Velora. **(4) Single titles** — the redundant double titles collapsed to one promoted title in `CharacterModal` ("Edit/New Character"), `StorylineModal` ("Edit/New Storyline"), and `EntityModal` ("Edit/New Setting|Scenario"); the storyline **seal picker** relocated from the header to the right column **above Draft with Velora** (under a "Seal" label). **Validation:** 80 frontend tests (typecheck + lint + build clean); `CharacterModal.test.tsx` updated to open the portrait pop-up before exercising prompts/render.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (port 3346) runs under the user's `python app.py`; `preview_start` can't reuse it and a second Turbopack server clashes on `.next` (standing constraint, every prior entry). Verified via the green suite + build + structure reasoning (`splitScroll` gated to `lg+` with whole-dialog fallback below; native focusable controls; `FieldLabel`-associated inputs; the voice upload is a disabled `aria-disabled` button; portrait pop-up reuses the focus-trapped `Modal`). Run the in-browser pass once the dir is free.
- **Known caveat:** the character portrait preview + Edit-image button live in the right aside, which is hidden on **mobile manual mode** (`< md`, non-agentic tab) — the portrait is reachable on `md+` and in mobile agentic mode. Acceptable under the deferred-mobile pattern; revisit if mobile portrait editing is needed.
- **Follow-up — portraits square + re-render fix (same branch):** portraits now render **1:1 at 1024×1024** (`services/portraits.py` `_PORTRAIT_W/H`; the `PortraitModal` preview box is square to match). **Re-rendering** now works: `generate_portrait` picks a **fresh random seed each call** (overridable). With the old fixed seed, an identical prompt was served from ComfyUI's cache with no GPU execution, and since the completion WebSocket connects only *after* the prompt is queued, that instant cached-completion signal was missed and `wait_for_completion` blocked to its 300s deadline — the stall. A real seed forces execution (WS connects in time) and yields a genuinely new image. 102 backend tests (ruff + mypy clean) + 80 frontend tests. **Deeper race (deferred):** `comfyui.generate` queues *then* opens the WS; opening it before queueing would make even all-cached runs robust — larger refactor, not needed for the reported symptom.

### Agentic Character Creator — prep phase (done; `feat/character-creator-agent`; plan: `docs/plans/character-creator-agent.md`)
An agentic Character Creator mirroring the storyline authoring flow, plus optional watercolor portraits — the **preparation phase** for the character knowledge graph (`Documents/Plans/3.character-graph-structure-prep.md`). It produces **§1 node properties only — no graph** (no edges, secret nodes, acquired traits, or Neo4j). Six phases, commit-per-phase, merged to `main`.
- **Phase 1 — data layer:** nullable `appearance`/`background`/`personality`/`portrait` columns on `Character` (model/schemas/create-CRUD/seed); these self-heal on the dev DB via the bootstrap additive-column reconcile. Roundtrip/default/PATCH tests; `api-contract` Character shape.
- **Phase 2 — agent:** `agents/character_agent.py` — `draft_character` (seed → full base identity, optionally grounded in the world + dropped docs), `generate_portrait_prompts` (short comma-separated **watercolor** prompts, species/race-accurate), `propose_starting_stats` (keyed/clamped to the storyline stat schema, proposal-only). Shared LLM helpers extracted to `agents/_common.py` (storyline agent refactored onto it). Fixed-path routes `POST /characters/{draft,portrait-prompts,starting-stats}`; 9 offline-mocked tests.
- **Phase 3 — portrait:** `services/portraits.py` wraps `comfyui.generate`, converts PNG→**WebP** (Pillow), saves under `MEDIA_DIR` (default `<repo>/media`, `MEDIA_DIR` env, gitignored), served read-only at `/media` (StaticFiles mount). `POST /characters/portrait` is id-agnostic (attach during creation). New dep: `pillow`. 5 offline tests.
- **Phase 4 — frontend data path:** `Character`/`Draft` gain the new fields; `api.ts` adds `draftCharacter`/`generatePortraitPrompts`/`generatePortrait`/`proposeStartingStats`/`setCharacterStats` + a `mediaUrl` helper; `useLibraryState` handlers (independent spinners) apply proposed stats with the character on save. 4 renderHook tests.
- **Phase 5 — UI:** `CharacterModal.tsx` (form + base-identity prose, Draft-with-Velora seed, Portrait section, Starting-stats section) + the extracted shared `ContextFilesPanel` (StorylineModal refactored onto it). `EntityModal` now owns setting+scenario only; `CharacterModal` mounted in `LibraryView`. 4 integration tests.
- **Phase 6 — display:** `Monogram` gained an optional portrait `src` (colored-ring image, monogram fallback); `CharacterCard` + `CharacterProfileModal` show the portrait, and the profile shows appearance/background/personality. Removed the now-orphaned `CharacterForm.tsx`.
- **Validation:** 98 backend tests (ruff + mypy clean) + 80 frontend tests (typecheck + lint + build clean).
- **Decisions (from the user):** portraits are **generated + persisted as the avatar**, encoded **WebP** (not PNG); starting stats are **propose-only** with review/adjust, applied on save (explicit "Save stats now" in edit mode).
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the shared working dir's dev server (port 3346) runs under the user's `python app.py`; a second Turbopack server clashes on `.next` and `preview_start` can't reuse an external one (standing constraint, every prior entry). Verified structurally (focus-trapped `Modal`, `FieldLabel`-associated inputs, `role=group` accent picker, labelled number inputs, `role=alert` errors, `img` alt) + the green suite/build; layout mirrors `StorylineModal`'s verified-responsive structure. Run the in-browser pass once the dir is free.
- **Deferred follow-ups:** RAG/KG context toggles remain forward-looking seams; portrait generation needs a configured + running ComfyUI server. The knowledge graph itself (edges, secret/event/faction nodes, Neo4j, acquired traits) is **out of scope** here by design — this only produced the base-identity material it will later connect.

### Maintenance — orphaned-media cleanup (done; plan: `docs/plans/eod-four-features.md`)
Resolves the standing orphaned-portrait/scene-art follow-up. `services/media_cleanup.py` (`scan_orphans`/`delete_orphans`) cross-references on-disk `*.webp` under `MEDIA_DIR/portraits` + `MEDIA_DIR/scenes` against every `Character.portrait` / `Setting.image` basename. `GET /api/options/media/orphans` is a dry-run report (per-dir + total orphan/eligible counts + bytes); `POST /api/options/media/cleanup` deletes **only** files that are both unreferenced **and** older than a grace period (`minAgeHours`, default 24) — so a just-generated image from an in-flight draft is reported but never deleted. Safe by construction: never touches referenced files, non-`.webp` files, or anything outside the two media subdirs; each unlink is fault-isolated. Surfaced in the Options **About** tab's Maintenance section (scan → confirm → delete, with an `aria-live` result). 14 backend tests (referenced-survives, expired-deleted, recent-protected, non-webp-untouched, missing-dir, both routes) + 4 frontend tests; full suites green (backend 259, frontend 156), ruff + mypy + typecheck + lint clean.

### Storyline modal — wider layout + per-context selection (done; `feat/storyline-modal-layout-context`; plan: `docs/plans/storyline-modal-layout-context.md`)
Restructured the create/edit-world modal (`StorylineModal.tsx`) and enriched the context model. **(1) Layout** — the modal is **wider** (`md:w-[860px] lg:w-[1120px]`). The **main column** holds a by-hand form (**Title + Genre on one row**, full-width **Tagline**, then **Premise**) beside the agentic **Draft with Velora** seed (its action button now spans the **full width** of the seed box), over a **full-width World Primer row**; the dropped/uploaded contexts live in a **detached, full-height column down the right side** (`lg`), separate from the main (own `bg-card` panel + border). The **seal + color picker** moved to the header, right-justified on the title row (preview **left** of the symbol/color grids, **no "Seal" label** — shorter), with × pinned to the corner. **(2) Context selection** — `ReadDoc` gained `useDraft`/`useRag`/`useKg` flags + a `docsForDraft` filter (`lib/readDocs.ts`); each dropped file is now **showcased as a selectable item** with **Draft / RAG / KG** toggle chips (`aria-pressed`). Only **Draft** is wired — `draftStoryline`/`generatePrimer` ground generation with `concatDocs(docsForDraft(...))`, so opting a file out of Draft drops its text from the grounding block. **RAG/KG are forward-looking seams** (those systems deferred — not persisted/sent). **(3) Scale + chrome** — the context column shows a **file count** and **per-category bulk All / None** controls (`setAllDocUse`) so hundreds of dropped files are manageable; the **whole modal scrolls** (no nested scroll); and the × close moved **outside** the panel (a reddish button hovering at the top-right corner) via a new `Modal` `externalClose` prop (kept inside the focus trap; default-off so other modals are unchanged). **Frontend-only.** 69 frontend tests (2 new readDocs + 2 new editors tests) + typecheck + lint + build green.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the working dir's dev server (port 3346, hardcoded `-p 3346`) was already running under the user's `python app.py`; `preview_start` can't reuse an externally-launched server and a second Turbopack server in the same dir clashes on `.next` (same standing shared-dir constraint as every prior entry). Verified via the green suite + build + width reasoning (modal `max-w-[92vw]`; header/seal cluster `flex-wrap`; Title+Genre `grid-cols-1 sm:grid-cols-2`; rail `lg:flex` drop-zone+list, stacked below `lg`; no horizontal overflow). Run the in-browser pass once the dir is free: open the switcher → New Storyline, confirm the two-column head, header seal, full-width World Primer, and the per-file Draft/RAG/KG toggles at each width.

### Storyline UI tweaks + draft-502 fix (done; `feat/storyline-ui-tweaks-draft-fix`; plan: `docs/plans/storyline-ui-tweaks-and-draft-fix.md`)
Three targeted fixes. **(1) Draft 502** — `POST /api/storylines/draft` returned `502 "empty response"` because the configured model (`gemma-4-26B`, a **reasoning model**) spent the whole `max_tokens=512` budget on `reasoning_content`, returning empty `content` (`finish_reason: length`). Fixes in `agents/storyline_agent.py` (`_gen_params` floors per-call `max_tokens` to **≥8192** for draft + primer, without touching the operator's saved Options value) and `services/llm.py` (`_send` accepts a per-call timeout; `chat_complete` now uses a **300s** read window vs. 20s for list/test so slow local/reasoning models can finish; an empty reply with `finish_reason: length` raises an actionable "raise Max tokens" error). 2 new agent tests; 82 backend tests green (ruff + mypy clean). **Validated live** against the running backend + LLM — drafts now return 200. **(2) Smooth scroll** — selecting a scenario glides the active Setting into view (`SettingColumn.tsx`: `scrollIntoView` `behavior:'smooth'`, falling back to instant under `prefers-reduced-motion`). **(3) 16:9 scene art** — the hero carousel's art panel went from a fixed 200px portrait placeholder to a true `aspect-[16/9] h-full` (~437px) frame shown at `lg+`, with overlay/dots offset `lg:right-[449px]`. 65 frontend tests + typecheck + lint + build green.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** the working dir's dev server (port 3346, hardcoded `-p 3346` in `npm run dev`) was already running with a browser attached via the user's `python app.py`; a second Next/Turbopack dev server in the same dir would clash on `.next`. Verified via the green suite + build + computed geometry instead (same shared-dir constraint as prior entries). Run the in-browser pass once the dir is free: confirm the smooth scroll glides (and is instant under reduced-motion), and the 16:9 art frame renders without overflow at each width.



### ComfyUI image generation (done; `feat/comfyui-image-generation`; plan: `docs/plans/comfyui-image-generation.md`)
A server-side **ComfyUI** client + an Options surface to configure it. **Backend:** `services/comfyui.py` implements the full 7-step pipeline (status → load workflow → build_prompt patch → queue → WebSocket wait → history → download → `generate` orchestrator) with injectable `get_http_client`/`open_ws` factories (offline-tested); `comfyui_base_url` config + `COMFYUI_BASE_URL` env; `comfy` namespace in `settings_store` (baseUrl/workflow/params); routes `PATCH /options/comfy`, `GET /options/comfy/workflows`, `POST /options/comfy/status`, and `comfy` added to `GET /options`. New dep: `websocket-client`. **Frontend:** `ImageModelsTab` (base URL + Check status, workflow dropdown via `GET /options/comfy/workflows`, default params + negative prompt, Save) added as a dedicated **Image Generation** tab in `OptionsView`; `api.ts` comfy types/calls; `useOptionsSettings.saveComfy`. **Validation:** 80 backend tests (ruff + mypy clean) + 65 frontend tests (typecheck + lint + build clean). **Live end-to-end proven** against the running ComfyUI 0.25.0 — `generate(...)` rendered a real 1.7 MB PNG from `ZiT-Workflow.json` in ~19s (4-step watercolor).
- **Decision:** the Options tab's "test" is a **status check only** (no GPU spend in the UI); the full `generate` pipeline lives in the service for the story engine. HTTP reuses `httpx` (the guide's `requests` was intentionally not added — `httpx` already covers it).
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** verified structurally via the suite (native focusable controls, `aria-live` status, `role="alert"` errors, labelled section, global `:focus-visible`, existing AA tokens), consistent with the standing Options-tab constraint. Run the in-browser pass once the working dir + backend are free.
- **Deferred — image generation in the story engine:** wiring `generate(...)` into scene rendering (character portraits / scene art) waits on the streaming story engine; a `POST /options/comfy/generate` endpoint can be added when a UI consumer needs it.


### Storyline World Primer + agentic creation (done; `feat/storyline-world-primer`; plan: `docs/plans/storyline-world-primer.md`)
The **agent process of building a storyline** + the frontend to match it — the World Primer slice of `Documents/Plans/1.storyline-context-model.md`. **RAG is intentionally out of scope.**
- **Phase 1 — data layer:** `world_primer` (Text, nullable) on `Storyline` — agent-facing runtime context, distinct from the human-facing `premise`. Model column + Base/Update/Read schema fields + create-CRUD wiring + a sample primer on the Embergate seed + roundtrip/default-null tests. Docs: `api-contract` storyline shape gains `worldPrimer`.
- **Phase 2 — authoring agent:** `services/llm.chat_complete` (generic completion primitive); `agents/storyline_agent.py` with `draft_storyline` (seed → title/genre/tagline/premise via tolerant JSON extraction) and `generate_world_primer` (seed+premise → agent-facing primer); resolves endpoint/model/key from `settings_store`; clear `400` when the LLM is unconfigured. Routes `POST /storylines/draft` + `POST /storylines/primer`. 9 agent/endpoint tests via `httpx.MockTransport` (no network). Docs: `api-contract` Authoring endpoints + shapes, `data-flow` authoring flow.
- **Phase 3 — frontend:** `worldPrimer` on the `Storyline` type/`Draft`/`STORYLINE_DRAFT`; `lib/api` `draftStoryline`/`generateWorldPrimer`; `useLibraryState` `draftStoryline()`/`generatePrimer()` (independent spinners + error handling); `StorylineModal` live seed box + enabled "Draft with Velora", editable **World Primer** field + "Generate primer" action. Full draft→primer→persist flow tested through the real hook (api mocked).
- **Phase 4 — context-file grounding (non-persistent):** `lib/readDocs.ts` (read `.txt`/`.md` client-side, concat under per-file headers, cap at 8k chars); `StorylineModal` drop zone now reads dropped/selected files into `draft._docFiles` (removable chips) and passes them as `docsOverview` to draft/primer. **Files are never uploaded, persisted, or indexed.** Helper unit tests + a modal test asserting a dropped file's text reaches the generation call.
- **Validation:** 61 backend tests (ruff + mypy clean) + 61 frontend tests (typecheck + lint + build clean).
- **Dev-DB gotcha (now auto-handled for nullable columns):** idempotent `create_all` adds tables but not columns, so a persistent dev DB used to need a manual `ALTER` per new column. The preflight now **reconciles additive nullable columns automatically** (`bootstrap._reconcile_additive_columns` → `ADD COLUMN` for any nullable model column missing from an existing table), so `world_primer` self-healed on the live `backend-db-1` on first run (`migrate (optional): added columns: storylines.world_primer`). **Non-nullable** additions on a populated table are still *reported* (not auto-added) and need a manual `ALTER`/migration — the standing **Alembic** follow-up remains.
- **Deferred — live in-browser a11y/responsive pass (320/375/768/1024):** a concurrent agent session held the working dir with a *stale* shared backend + dev-DB (no `world_primer` column or authoring routes) that couldn't be restarted without disrupting it. a11y was verified structurally instead (global `:focus-visible` covers the new controls; modal scrolls internally via `max-h-[90vh] overflow-auto`; `role="alert"` errors with a `md:hidden` mobile echo to avoid double-announce; native focusable controls; existing AA tokens). Run the in-browser pass once the dir is free (apply the ALTER, restart the backend on this branch, then exercise draft/primer/file-drop at each width).
- **Deferred — the RAG system (next plan):** document **persistence, structure-aware chunking, dense (`fastembed`/`nomic`) + BM25 hybrid + RRF, automatic/agentic retrieval at runtime, `start` vs. `dynamic` inclusion tiers, contextual-retrieval prefixes, reranking, derived lore graph / `query_kg`**. Also deferred: **runtime injection** of the World Primer into conversations (waits on the story-player↔backend wiring — the player is still on in-memory seed).


### Options menu — settings store + LLM proxy + `/options` page (complete; plan: `docs/plans/options-menu.md`)
Backend Phase 1 of the Options menu (`[Options Menu] (1/3)`): a global `app_settings` key/value model (one row per namespace — `llm`, `library`; no auth yet so config is global) + camelCase schemas in `schemas/settings.py`; `services/settings_store.py` (get-or-create defaults seeded from process `Settings`, **write-only API key** stored server-side and never returned in clear — reads expose only `hasApiKey` + a masked `…AB12` hint); `services/llm.py` (server-side httpx proxy for `GET /models` + a tiny `POST /chat/completions` test, dodging browser CORS to local model servers; upstream non-2xx → `502 upstream_error`, network/timeout → `502 bad_gateway`). Routes live under the **`/options`** prefix (`routes/options.py`) — *not* `/settings`, since the Setting entity already owns that. 9 new backend tests (httpx `MockTransport`, no network); 49 backend total green; ruff + mypy clean. Docs updated: `api-contract.md` (Options group + shapes), `structure.md` (`app_settings`), `data-flow.md` (Settings Flow).
**Phase 2 (`[Options Menu] (2/3)`):** header **Options ▾** dropdown (`OptionsMenu` — Settings Menu link → `/options` + a label-free Appearance theme selector; theme moved out of `AppHeader`) and the **`/options`** route (`features/options/OptionsView` — centered 66%-width panel, vertical ARIA tablist with ↑/↓/Home/End, keyed-remount lazy-init tabs). Tabs: `AppearanceTab` (theme swatches), `LibraryDefaultsTab` (default storyline + startup, via `PATCH /options/library`), `AboutTab` (read-only diagnostics + `GET /health`, no secrets), and a Phase-2 `LanguageModelsTab` (base URL / API key / model / params → `PATCH /options/llm`). `useOptionsSettings` loads `GET /options`. `lib/api.ts` extended (+`getSettings`/`updateLlmConfig`/`updateLibraryDefaults`/`fetchLlmModels`/`testLlmConnection`/`getHealth`). 11 new frontend tests; 44 frontend total green; typecheck + lint + build clean.
**Phase 3 (`[Options Menu] (3/3)`):** the Language Models tab now **discovers models** (`POST /options/llm/models` — "Fetch models" button + on-blur of the base URL → a `<select>`, falling back to a text input when none are reported) and runs a **connection test** (`POST /options/llm/test` → OK + latency + sample, or a structured error). 47 frontend tests green; typecheck + lint + build clean.
- **Deferred:** live preview a11y/responsive pass was constrained by a concurrent session sharing the working dir, so this was built and validated in an **isolated git worktree** (`/home/bdgr/Agents/Velora-options` on `feat/options-menu`); structure/keyboard verified via the suite + an earlier preview snapshot — a final in-browser pass at 320/375/768/1024 should run once the working dir is free. Encryption-at-rest for the key + per-owner scoping deferred to the auth phase.

### Storyline seal — customizable symbol + color (`feat/storyline-seal-symbol`)
Each storyline gets a **seal** — a simple shape glyph (`symbol`) plus a hex `symbolColor` — shown left of its name. **Backend:** new `symbol` / `symbol_color` columns on `Storyline` (defaults `◆` / `#C8862A`), camelCase schema fields (Base/Update/Read), create-CRUD wiring, Embergate seed; 1 new API test (default + custom roundtrip + PATCH); 51 backend tests green, ruff + mypy clean. **Frontend:** `symbol?`/`symbolColor?` on the `Storyline` type, a tiny `lib/seals.ts` (8 shapes + gold-first color palette + defaults), a **seal picker** (preview chip + symbol grid + color swatches, mirroring `CharacterForm`'s accent picker) in `StorylineModal`, dynamic seal render in `StorylineMenu` (active button + each dropdown row, default-◆ fallback), and draft/submit wiring in `useLibraryState`/`editor.ts`. 54 frontend tests green; typecheck + lint + build clean.
- **Dev-DB gotcha (applied):** the persistent Postgres dev DB needed `ALTER TABLE storylines ADD COLUMN IF NOT EXISTS symbol TEXT DEFAULT '◆'; ADD COLUMN IF NOT EXISTS symbol_color TEXT DEFAULT '#C8862A';` — idempotent `create_all` adds missing tables but does **not** ALTER existing ones. This was run against `backend-db-1` (existing rows backfilled to the gold ◆ default). Reinforces the standing **Alembic** follow-up.
- **Live preview verified:** created a "Starfall" world with a teal ★ seal end-to-end — the picker live-preview updated, the switcher rendered the seal (button + dropdown rows), and the API persisted `symbol`/`symbolColor` (verified at desktop + tablet; the picker is `flex-wrap` so it can't overflow narrower widths). Test storyline cleaned up afterward.

### Storyline creation modal — write-first pop-up (`feat/storyline-create-modal`; plan: `docs/plans/storyline-create-modal.md`)
Replaces the one-shot "Untitled Storyline" create with a write-first modal (done end-to-end). **Phase 1:** added a nullable `premise` (long `Text`) column to `Storyline` (model/schema/CRUD/seed) — 41 backend tests, ruff + mypy clean. **Phase 2:** the switcher's "New Storyline" now opens `StorylineModal` (title / genre / tagline + multi-paragraph **premise**); `useLibraryState.openCreateStoryline` + `submitStoryline` persist via `POST /storylines` then activate the world. Verified end-to-end via preview (create "Tidefall" → persisted with premise via API; modal a11y: `role=dialog`/`aria-modal`/labelled, focus-trapped, Esc closes). **Phase 3:** added the forward-looking **seams** — a drag-and-drop **context-files** drop zone + an agentic **"Draft with Velora"** panel, both visible but non-functional ("Coming soon"), in a two-column desktop rail that collapses to the mobile By-hand/Agentically toggle. 34 frontend tests green; a11y/responsive pass at 320/375/768/1024 (no overflow; desktop two-column 887px, mobile toggle swaps form ↔ seams).
- **Dev-DB gotcha:** the backend's idempotent `create_all` creates missing *tables* but does not ALTER existing ones, so the persistent Postgres dev DB needed a manual `ALTER TABLE storylines ADD COLUMN IF NOT EXISTS premise TEXT`. Reinforces the standing **Alembic** follow-up before the next schema change.
- **Deferred:** wire the seams (context-file ingest + agentic draft-into-fields); add storyline **edit/rename** (modal is create-only); mobile still can't reach "New Storyline" (switcher hidden `< md`).

### Storyline data layer — Postgres persistence + Library wiring (done; `feat/storyline-data-layer`)
Backend stood up end-to-end (plan: `docs/plans/storyline-data-layer.md`): core config/db/redis clients; SQLAlchemy models + camelCase Pydantic schemas for Storyline/Character/Setting/Scenario + the stat seam; CRUD routes under `/api` with the error envelope; per-storyline stat definitions + clamped character stat values; Embergate seed; `python app.py backend` preflight (docker compose up + checks + schema + seed) with `web/backend/docker-compose.yml` (Postgres on host **5544**); chat-scaffold `events`/`play_sessions` tables + NDJSON envelope types (no streaming). Frontend `lib/api.ts` client + `NEXT_PUBLIC_API_URL`; the **Library now reads/writes the backend** (await-then-apply, loading/error/Retry) and **persists** (verified end-to-end via preview: create → reload → survives; delete → gone). 38 backend tests + 33 frontend tests green.
- **Remaining:** populate `web/shared/contracts/` (currently the FE reuses `lib/types.ts` and BE owns Pydantic); wire the **Story player** to the backend; surface stats on the **scene pages**; add a `users`/auth owner column. (**Alembic** migrations landed — see Scaffolding below.)

### Frontend — Embergate UI (built from `docs/CharacterFrontpage/`)
- [x] Library at `/` — header (wordmark/storyline/search/theme/create), recent-scenario carousel, ARIA tabs, character/setting/scenario/branch cards, create/edit/delete editors (By-hand + faked Agentic), character profile, begin-scene → `/play/[id]`.
- [x] **Library redesign — storyline-scoped, open columns** (`redesign/storyline-library`): prominent **storyline switcher** (outlined button, large Cinzel small-caps + ◆ seal + rotating chevron; active world + counts + "New Storyline") swapping the whole working set; body is three **open columns** (Scenarios · Characters · Settings) side-by-side on desktop, collapsing to the 3-tab section switcher on `< lg`; selecting a scenario lights up its cast and brings its setting forward ("◆ In this scene" cue + `aria-current`, not color alone) **and scrolls the active setting into view**. The page is **self-contained** (`h-dvh`, no page scroll) with **per-column scrolling** + **sticky column headers** on a **solid `bg-page`** so the radial glow never shows behind scrolling cards. The old standalone "Storylines"/branch entity removed (Branch type + `Scenario.branches` retained as sub-data). State refactored to storyline-scoped in `useLibraryState`. Verified: typecheck/lint/Vitest (29) + build green; a11y/responsive pass at 320/375/768/1024 (no page or horizontal overflow; per-column scroll + mobile collapse confirmed via preview).
  - **Deferred:** the storyline switcher is hidden `< md` (mobile can't switch storylines yet) — surface it on mobile when multi-storyline ships; "New Storyline" creates an empty storyline but there's no storyline rename/editor yet.
- [x] **Creation modal — side-by-side agentic layout** (`EntityModal`): on desktop (`md+`) the By-hand form and the Agentic draft panel show at once (form left, agentic chat in a fixed right rail); the mode toggle collapses to mobile-only (`< md`), still tab-switching one column at a time. UI structure only — verified at 375/1280 via preview (desktop form 533px + agentic rail 325px side-by-side, toggle hidden; mobile single-column toggle swap). **Deferred:** the agentic draft is still faked (`generate()` 850ms stub) and does not yet stream/build live into the form fields — that's the next functional step.
- [x] Story player at `/play/[scenarioId]` — three-zone scene: cast rail + turn order, transcript beats (narrator/dialogue/action/player/check/branch_choices), composer (send/roll/choose), director rail (goal/tension/state/relationships), ❖ loader.
- [x] Three themes (Parchment/Ember/Slate) with persistence; Cinzel/EB Garamond/IBM Plex Mono via `next/font`.
- [x] A11y/responsive pass: keyboard (tabs arrow-keys, modal focus-trap, menu/Esc), `:focus-visible`, polite live region on the transcript, `sr-only` page `<h1>`, and **no horizontal overflow at 320/375/768/1024**; reduced-motion via Framer `MotionConfig` + CSS `motion-reduce`.
- [ ] **Deferred:** rails → mobile drawers (currently hidden < lg, transcript stays primary); the 3D page-flip nav transition; a formal automated WCAG contrast audit (tokens designed for AA); wiring the **Story player** to the backend (the Library is now backend-backed and persists; the Story player still uses in-memory seed).



### Scaffolding (next step after init)
- [x] Scaffold the Next.js app in `web/frontend/` (`create-next-app`: TS, Tailwind, App Router) + add Framer Motion. **Done:** Next.js 16 (Turbopack) · React 19 · Tailwind v4 (CSS-first `@theme`) · Framer Motion 12. Fonts via `next/font` (Cinzel / EB Garamond / IBM Plex Mono). Test runner: **Vitest + React Testing Library**, tests co-located as `*.test.tsx`.
- [x] Scaffold the FastAPI app in `web/backend/app/` and wire root `app.py` to it (factory + routers + CORS + error envelope + preflight).
- [x] Add backend deps (declared in `pyproject.toml`; `uv sync`). No new deps were needed for the data layer.
- [x] Set up PostgreSQL + Redis connections in `app/core/` (`config.py`, `db.py`, `redis.py`).
- [x] Create initial DB models (storylines, characters, settings, scenarios, events/play_sessions, stat definitions, stat values). **Deferred:** a `users` table (no auth yet).
- [x] **Migrations (Alembic)** — `web/backend/alembic/` (env.py wired to `Base.metadata` + `app.core.config` URL; `render_as_batch` for SQLite) + a drift-verified `baseline` migration capturing the full current schema. **Coexists** with `create_all` + the additive reconciler: preflight stamps an existing schema to `head` (or upgrades) on Postgres and skips SQLite (the test path). Best-effort (logged + in the preflight report, never blocks startup). Tests: `test_alembic.py` (upgrade builds the exact table set; baseline columns match `create_all`) + `test_bootstrap.py` (SQLite skip). Commands in `docs/workflow.md`.
- [~] Markdown **stat-guidance loader** done (`services/stat_guidance.py` + `app/content/stats/*.md`, injected into `character_agent`). **Remaining:** YAML config loaders in `app/content/`.

### Core domain & event system (next after scaffolding)
- [x] Lock the four core objects (Storyline / Character / Setting / Scenario) as Pydantic models + ORM (`app/models`, `app/schemas`). **Remaining:** move the shared types into `web/shared/contracts/` (FE currently reuses `lib/types.ts`).
- [~] Five-event NDJSON schema — the envelope **types** exist as a discriminated union (`app/events/envelope.py`); the validator (parse → validate → repair/retry) and the stream are not built yet.
- [ ] Stand up the NDJSON stream in full-event mode, then add delta streaming for visible messages.
- [~] Stat system — storyline stat schema, per-character values, and validator **clamping** are done at the data layer (`/api` stat endpoints). **Remaining:** the Stats panel UI (on scene pages) and `state_update`-carried stat changes (needs the stream).
- [x] Author guidance files for the first handful of stats and wire them into agent context. **Done:** `web/backend/app/content/stats/{health,suspicion,trust,patience}.md` authored; `services/stat_guidance.py` is a path-safe (traversal-rejecting), cached loader resolving paths against `Settings.content_dir`; `character_agent.propose_starting_stats` folds each definition's guidance (trimmed to 400 chars) into the stat-schema prompt. `build_agent` is **not** wired — it designs a stat schema from scratch (no `StatDefinition` rows / guidance paths exist at blueprint time).
- [ ] Extend stats to relationship/mood values (same machinery, relational target).

### Architecture decisions to finalize
- [ ] Auth mechanism (JWT vs. session cookie; provider) — currently "backend-owned, TBD".
- [ ] SSE vs. WebSocket for the event stream (or both) — define before building the story player.
- [x] API base prefix (`/api`) and CORS origins (`FRONTEND_ORIGIN`) decided and wired in `app/main.py`.
- [ ] LLM provider interface shape (OpenAI + local).
- [ ] Stat lifecycle across scenarios (reset / persist / partial carry-over) and how hidden stats render.
- [ ] Deployment target (containerized vs. split hosting).

### Character voice
- [x] **Situational voice adaptation** — characters now adapt their manner to the moment instead of rigidly replicating their voice samples (constant quips at a funeral, no fear when dying, etc.). Prompt-first fix across `prompt_registry._CHARACTER_OUTPUT_CONTRACT` (manner-adaptation rule + moment-first `<thinking>`), `character_turn_agent._build_user_prompt` (baseline-voice HEAD reframing + read-the-moment TAIL cue), and `reflection_agent._SYSTEM` (disposition carries emotional state across turns). Plan: `docs/plans/situational-voice-adaptation.md`.
- [ ] **(Deferred follow-up)** Shared per-turn **scene-appraisal** signal (one cheap LLM call, cached on `TurnContext`, feeding every speaker a common read of the scene's mood/stakes). Revisit only if playtesting shows the prompt-only adaptation above is insufficient; weighed against the local reasoning model's per-call latency/token cost.

### Deferred capabilities (design seams only for now)
- [ ] Dice-based resolution (optional later layer: `check_request` → `roll_result` → `consequence` → `state_update`).
- [ ] Vector DB for semantic memory / cross-scenario character & setting memory.

### Quality gates to enforce once code exists
- [x] pytest in `utils/tests/backend/` (38 tests across `data/` + `api/`, on in-memory SQLite; ruff + mypy clean).
- [x] Frontend test runner chosen: **Vitest + React Testing Library**, tests **co-located** beside components (`web/frontend/**/*.test.tsx`). Playwright e2e still optional/later.
- [ ] Accessibility + responsive pass per `accessibility-mobile` + `ada-compliance`.

## Retained / Removed Setup Files
- Removed: `initialize.md`, `read-yaml.py`, and the starter skill directories (`accessibility-mobile/`, `ada-compliance/`, `plan/`, `repo-structure/`, `ui-frontend/`, `website-architecture/`) — all migrated into `docs/skills/`.
- Retained: `.gitignore`, `.python-version` (runtime config).
