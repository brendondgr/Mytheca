# RESULTS — <EXP-ID> <title>

> Written after the run. Sections in this order; do not reorder or drop one.
> If the run failed, this file still gets written — see §3.4 of the contract.

## 1. Headline

Two sentences. The number, and whether the hypothesis survived.

## 2. Results table

**Generated, not typed.** Mean ± std across seeds, never a single run. If n differs
per cell because a run was excluded, show the n and point at the `ISSUES.md` entry.

<!-- generated from data/metrics.json -->

| Arm | <primary metric> | <secondary> | n |
| --- | --- | --- | --- |
| | | | |

## 3. Figures

Each figure embedded with a one-line caption naming the script that produced it.

Replace this block with the real embed once a figure exists — a live image link with an
unsubstituted `<slug>` is a broken link, and broken links are what `check_links.py` reports:

```markdown
![what the reader is looking at](figures/fig-my-slug.svg)
*Fig 1 — caption. Generated by `figures/make_figures.py` from `data/metrics.json`.*
```

## 4. Interpretation

What this means for the paper's argument. Which claim in `CLAIMS.md` moves, and to
what status. Resist upgrading a claim further than the evidence carries it.

## 5. Threats to validity

Confounds, leakage, dataset artifacts, sample size, cost of the comparison. **State
under-powering in plain words if the run is under-powered.** A threats section that
lists no real threat is not a threats section.

## 6. What surprised us

The section that becomes the discussion. If nothing surprised you, say that — it is
itself information about how well the hypothesis was specified.

## 7. Follow-ups

Each one is copied into `../../OPEN_QUESTIONS.md` in the same change.

- [ ]

## 8. Reproduction

The exact command that regenerates everything in this folder from scratch.

```bash
```
