# Claims Ledger

Every claim the paper will make, and the evidence that supports it.

Status: `unsupported` | `partial` | `supported` | `refuted` | `cut`

**Every claim below is `unsupported`.** That is not a placeholder state — it is the
accurate state of this project as of 2026-08-06. Mytheca has no evaluation of any
kind: no benchmark, no baseline, no ablation, no metric, no human study. Each claim
is asserted somewhere in the codebase (usually a docstring) as the justification for
a mechanism, and none has ever been tested.

This table is therefore the experiment backlog. It is meant to look uncomfortable.

**Source.** Transcribed from `mytheca-research-audit.md` §1.6, which derived these
from what the code does and what its docstrings assert — not from README marketing.
Each is stated as a falsifiable proposition of the form *doing X yields Y over Z
under conditions W*. IDs were renumbered `C1`→`C-001` for the contract's format; the
audit's original label is kept in Notes so §1.6 stays greppable.

| ID | Claim | Evidence | Artifact | Status | Notes |
|----|-------|----------|----------|--------|-------|
| C-001 | Voicing each character with an isolated per-speaker LLM call, rather than one prompt rendering all speakers, preserves greater voice distinctness across a multi-turn multi-party scene. | — | — | **unsupported** | Audit C1. **The architecture's load-bearing justification**, asserted verbatim in `character_turn_agent.py` ("so voices stay distinct"). Zero evidence. To establish: blinded human speaker-attribution accuracy on label-stripped transcripts, and/or Coverage/Uniformity/Complexity (arXiv:2604.24698), on matched scenes from (a) the full loop and (b) a single-prompt baseline, ≥20 scenes × ≥3 seeds. |
| C-002 | Requiring a visible in-voice `<thinking>` deliberation before speech produces more in-character spoken lines than speaking directly. | — | — | **unsupported** | Audit C2. Asserted in `character_turn_agent.py`; `TURN_EFFORT = MEDIUM` is set specifically for it. **Already ablated by someone else** — Drama Machine (arXiv:2408.01725) ran the Ego/Superego ablation, on 2 scenarios. To establish: Lanham-style faithfulness perturbation (arXiv:2307.13702) — corrupt the `<thinking>` block, measure whether the spoken line changes — plus an A/B with the block removed. |
| C-003 | Server-side clamping of LLM-proposed numeric stat changes against a declared per-world schema, with Markdown band guidance rendered into the prompt, yields better long-run character-state continuity than letting state live in free text. | — | — | **unsupported** | Audit C3. Mechanism evidence only: `validator.validate_stat` provably cannot emit an out-of-range or undefined stat (`test_validator.py`). Nothing measures continuity. ⚠️ **Scooped** — PANGeA (AIIDE 2024) reports 28%→98% narrative alignment for the comparable mechanism; MAGNET/ATLAS (arXiv:2607.00918) reports −50% hallucinations. To establish: continuity-error rate on long sessions against the ConStory-Bench taxonomy (arXiv:2603.05890). |
| C-004 | A model-free, conservative, skip-by-default retrieval gate loses no grounding quality relative to always-retrieve, while saving retrieval cost. | — | — | **unsupported** | Audit C4. Asserted in `retrieval_gate.py` ("a bad retrieval is worse than none"). To establish: three-arm comparison (never / gated / always) on turns with a known lore-grounded answer, reporting quality *and* tokens. The gate's trigger conditions are enumerable, so a targeted adversarial set is cheap. |
| C-005 | A ReAct per-beat planner produces better multi-party turn structure than a one-shot speaker-set decision. | [EXP-2026-08-001](experiments/EXP-2026-08-001-planner-vs-oneshot-director/) *(pre-registered `planned`; evidence pending, not obtained)* | — | **unsupported** | Audit C5. **Unusually strong provenance evidence:** the one-shot design was built, then explicitly replaced, and *both implementations are still in the repository and tested* (`director_agent.who_is_up` vs `planner_agent.next_beat`). Never compared. **The cheapest ablation available** — the baseline arm already exists. Related transferable evidence: Zhu et al. (arXiv:2505.22809) report removing the ReAct step cost >70% F1 in a closely related setting. |
| C-006 | A pre-emit single-line consistency check with one-shot regeneration reduces within-turn contradictions at acceptable latency, without breaking delta streaming. | — | — | **unsupported** | Audit C6. Mechanism only. Best-effort design means it silently passes on any failure, so **its true firing rate is not even logged as a metric**. To establish: contradiction rate guard-on vs. guard-off on scenes seeded with contradiction opportunities; guard precision/recall against human labels; added latency per turn. |
| C-007 | Carrying a per-character disposition across turns via an off-hot-path reflection step improves stance continuity relative to reconstructing stance from the transcript alone. | — | — | **unsupported** | Audit C7. Inherited from Generative Agents (UIST 2023), whose own contribution has never been cleanly ablated in downstream systems. To establish: A/B with `TURN_REFLECTION_ENABLED=false` — **the ablation arm is already a config change** — scored on stance-continuity across a scene boundary. |
| C-008 | Streaming a turn's completion makes the model's deliberation visible ~20x earlier than its first word of prose, on a model that thinks before it writes. | [EXP-2026-08-003](experiments/EXP-2026-08-003-live-turn-visibility/) | [latency_by_arm.png](experiments/EXP-2026-08-003-live-turn-visibility/figures/latency_by_arm.png) | **partial** | First reasoning token 0.55 ± 0.24 s vs first answer token 10.59 ± 3.56 s in the SAME streamed call (n=5, llama.cpp gemma-4-26B-it). Supported only for that within-call gap, which is immune to the run's arm-ordering confound. **Two sibling hypotheses were refuted:** a 512-token thinking budget did not reduce completion tokens (592 ± 108 -> 607 ± 25), and streaming did NOT collapse time-to-first-*prose* (10.59 s against an 11.82 s total). Scoped to one prompt, one upstream; says nothing about output quality. ~~EXP-2026-08-004 bounds it further: on the deployed skynet route the reasoning channel is absent entirely~~ — **withdrawn 2026-08-19**: that null result was an instrument artefact (the parser read only the `reasoning_content` spelling and missed vLLM's `reasoning`), not a property of the model. See [EXP-2026-08-004 §AMENDMENTS](experiments/EXP-2026-08-004-deployed-model-visibility/RESULTS.md). The claim's scope is again limited only by what has actually been measured. |
| C-009 | Mytheca's character prompt places per-turn volatile state ahead of the transcript, so an inference server's prefix cache can reuse only the static system message and every turn re-reads the whole conversation. | [EXP-2026-08-005](experiments/EXP-2026-08-005-conversation-scaling/) | [scaling.png](experiments/EXP-2026-08-005-conversation-scaling/figures/scaling.png) | **supported** | Measured over a real 10-turn scene at maxTurns=5, suggestionsCount=0, contextBeats=100: prompt tokens grew 1830 -> 4678 while cached tokens stayed pinned at exactly 800 (the system message), so the hit rate fell 44% -> 17% monotonically. Mechanism confirmed by direct probe: an identical prompt hits ~99% cached and an appended one ~98%, but changing one stat value that sits BEFORE the transcript drops it to ~46%. **Scope:** this establishes the cache is wasted, NOT that it is what makes turns slow — at these context sizes the dominant cost is the number of sequential LLM calls per turn. **Resolved 2026-08-20** by [EXP-2026-08-006](experiments/EXP-2026-08-006-turn-latency-overhaul/): the prompt was reordered to stable -> transcript -> volatile behind a block-anchored window, and the reusable prefix now climbs 72% -> 90% across a scene with the server's own hit counter at 62-77% (mean 65.0% +/- 12.7%) against this run's 25.5% +/- 10.3%. The claim as written describes the *former* layout and stays supported for it. |
| C-010 | ~~The latency a player experiences in Mytheca is dominated by the inference endpoint's own throughput variance, not by the app's turn structure.~~ | [EXP-2026-08-006](experiments/EXP-2026-08-006-turn-latency-overhaul/) | — | **withdrawn** | **Withdrawn 2026-08-20, the same day it was raised.** It rested on a probe showing 1.4-90.4 s for byte-identical 143-token output. The intended GPU was not connected for that probe — the operator identified this. Repeated on the right hardware: **1.37-4.22 s over 12 runs (mean 1.67 +/- 0.78)**, stable. The endpoint is not the bottleneck and the app's turn structure is: on healthy hardware the character beat was **35.5 s per beat** because the model deliberated twice (a hidden reasoning pass *and* the visible in-voice `<thinking>` block); removing the hidden pass took it to **2.4 s** and the median turn from **137.5 s to 35.1 s**. Kept as a withdrawn row rather than deleted: the failure was reaching for a conclusion that explained a surprising result without checking the machine under it. See ISSUES.md 1. |
| C-011 | Requiring a character to write a visible in-voice deliberation while the model also fills a hidden reasoning channel makes it reason through the same beat twice, and the second pass is the only one anyone sees. | [EXP-2026-08-006](experiments/EXP-2026-08-006-turn-latency-overhaul/) | [overhaul.png](experiments/EXP-2026-08-006-turn-latency-overhaul/figures/overhaul.png) | **supported** | Direct probe, 3 runs per arm on one character-shaped prompt: a 512-token thinking budget produced 826-2084 reasoning chars and 212-568 completion tokens in 2.19-5.82 s; a **0** budget produced **0** reasoning chars and 67-76 tokens in 0.92-1.04 s. In a real scene the character `thinking` step fell **35.5 s -> 2.4 s per beat** and the median turn **137.5 s -> 35.1 s**, with time-to-first-prose tightening from a 6-101 s spread to 6.7-10.8 s. **Scope:** this establishes the cost of the double pass, NOT that output quality is unchanged. The quality read is n=1, unblinded, by the author; a countable length limit was also needed before the visible thought actually shortened (median 460 -> 214 chars). Relates to C-002, which claims the visible `<thinking>` block earns its keep and remains unsupported. |
| C-012 | Applying frequency/presence penalties to a character generation degrades the sentence structure of its prose, because those penalties fall on the punctuation and function words prose is made of. | [EXP-2026-08-007](experiments/EXP-2026-08-007-prose-form/) | [penalties.png](experiments/EXP-2026-08-007-prose-form/figures/penalties.png) | **supported** | Three arms, n=10 each, one prompt set, only `frequency_penalty` and `presence_penalty` varying: sentences per 100 words **1.32 ± 1.16** at 0.40/0.30 against **11.42 ± 3.96** at 0.00/0.00, with **no overlap between the arms** (worst run off = 8.9 > best run on = 3.7). Passages containing any spoken line: 80% → 100%. Paragraph breaks 1.20 → 3.80. The 0.20/0.15 arm is bimodal, not intermediate. **Secondary:** the penalties, not length freedom, were what made passages ramble — 2183 ± 1771 characters with them on against 674 ± 471 with them off — which retracts the earlier working conclusion that an uncapped passage degrades the writing, and the 1200-token ceiling that had been imposed over the owner's instruction because of it. **Scope:** one model (`skynet`); says nothing about in-character drift, which is what the penalties were added for and is not measured here; says nothing about whether the writing is good, only that it is shaped like prose. |
| C-013 | Folding beats that have fallen out of the context window into a rolling summary preserves continuity a plain drop loses, without measurably costing prompt-cache reuse. | [EXP-2026-08-011](experiments/EXP-2026-08-011-context-compaction/) | [context_compaction.svg](experiments/EXP-2026-08-011-context-compaction/figures/context_compaction.svg) | **unsupported** | **Tested 2026-08-22 and the result is against it, on both halves.** Continuity: the compacted arm recovered 3 of the planted fact's 9 content tokens against the control's 6 and missed the probe threshold — the summary had kept *"owes 400 crowns"* and dropped both the creditor and the brass key, so the character answered faithfully from a record that no longer held the fact. Cache: the summary was rewritten 16 times where the design predicts at most 4, and the reusable prompt prefix fell from 80.3 % to 44.6 % — a **defect** in `maybe_compact`'s gate (it tests beats dropped *in total*, not since the last summary), so the bounded design the claim describes has still never been run (`ISSUES.md` §6). n = 1 scene per arm: this **cannot** be read as a rate, and it is not enough to move the claim to `refuted` either — it is one existence proof against it. `TURN_CONTEXT_COMPACTION` ships **off** and stays off. The control never dropped the plant, so the fair question was "does B match A while reading less?" (`ISSUES.md` §1); prose quality (H3) is **not measured** — a blinded read was pre-registered and not performed, rather than substituting a rubric invented afterwards. |
## The meta-claim

C-001, C-002, C-005 and C-007 are all instances of one proposition: **the multi-agent
scaffolding earns its cost.** That meta-claim is under active attack in the literature
from three independent directions — Wang et al. (ACL 2024, arXiv:2402.18272: a single
agent with a strong prompt rivals multi-agent discussion), the MAST taxonomy
(NeurIPS 2025 D&B, arXiv:2503.13657), and cross-component interference
(arXiv:2605.05716, where the best proper *subset* matched or beat a five-component
system in every setting tested, with Planning carrying a **negative** Shapley value).

It is the single most testable and most interesting thing about this repository, and
it is untested. See `paper/OUTLINE.md`.

## What is defensibly novel

Of everything Mytheca does, a good-faith search across three research areas found a
2023–2026 primary source for **eight** components. **Two** could not be located in
any verified paper (audit §3):

- **POV switching** — the player speaks *as* a cast member, the AI is locked out of
  that character, and branch proposals become first-person in-voice line suggestions.
- **Stat-arbitrated turn behavior** — numeric bounded state rendered as prose bands
  that condition generation and are updated via clamped model proposals.

Neither is currently framed as a claim, isolated, or measured. Neither appears in the
table above, because a claim has to be *stated* before it can be listed — writing
them is open work (`OPEN_QUESTIONS.md`). ⚠️ Both rest on *not finding* prior work,
which is weaker than finding it, and the audit flags the 2025 Wordplay accepted-paper
list as the most likely place for a missed scoop.

## Refuted / cut claims

None yet. When one lands here, it stays — that is what stops a settled question from
being re-litigated.
