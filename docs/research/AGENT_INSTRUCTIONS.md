# Agent Instructions — the research record contract

**This is the contract. If you are an agent about to run an experiment, a benchmark,
a baseline, an ablation, or any evaluation in this repository, read this file first
and follow it.** The numbered sections below are the instruction set as received and
are not to be edited to suit a task.

## Mytheca-specific deviations (the only ones)

Three places where this repository knowingly departs from the sections below. Each
was a decision, not an oversight, and each is recorded here so that a future reader
does not mistake it for drift.

| # | Section | Deviation | Why |
| --- | --- | --- | --- |
| 1 | §7.4 CI · §7.5 pre-commit | **Not implemented.** Enforcement is `make validate-research` + `make research-index`, run by hand. | Owner decision, 2026-08-06. Mytheca has never had a `.github/` directory; its validation gate is `uv run pytest` + `npm test` run manually (`docs/workflow.md`). Introducing GitHub Actions was judged a larger footprint than a retrofit warrants. **This is the one acceptance criterion in §10 that is deliberately unmet.** Tracked in `docs/checklist.md`. |
| 2 | §2 structure | `mytheca-research-audit.md` sits at `docs/research/` root, outside the listed tree. | It is a meta-document — a 904-line external audit of the project's research standing — not an experiment. Forcing it into `experiments/` would misrepresent it. It is the provenance source for every seeded ledger in this directory; see `README.md`. |
| 3 | §7.1/7.3 tooling location | Scripts live in `utils/scripts/research/`, not a root `scripts/`. | `utils/scripts/` is where this repository already keeps standalone tooling (`check_contrast.py`), per `docs/structure.md`. A root `scripts/` would be an invented sibling. |

A root `Makefile` is introduced by this retrofit as the entrypoint for the four
research targets. It does **not** replace `app.py`, which continues to own Docker
and both dev servers.

## The four commands

```bash
make new-experiment SLUG=<slug>   # scaffold a new experiment folder
make validate-research            # enforce the contract; non-zero exit on any violation
make research-index               # regenerate INDEX.md from every manifest
make figures                      # regenerate every promoted figure from recorded metrics
```

## Before you write a number anywhere

- It goes in `manifest.yaml` under `metrics.values` and in `data/metrics.json`.
- It does not go only in chat, only in a commit message, or only in a figure.
- If the run failed, the folder still exists, with `status: failed` and an honest
  `ISSUES.md`. Deleting it is how the same dead end gets walked into twice.

---

<!--
  INSTALLED CONTRACT — do not edit the numbered sections below to suit a task.
  They are the research-record instruction set as received. Mytheca-specific
  deviations are recorded in the preamble above them, and nowhere else.
-->

# Instruction Set: Establish `docs/research/` as the Single Research Record

**Audience:** an AI coding agent (Claude Code, Cursor, Codex, etc.) operating inside an existing research repository.
**Goal:** every experiment, baseline, ablation, failure, and figure that will ever appear in a paper is written into one predictable, highly structured location — automatically, at the time it happens, not reconstructed six months later.
**Constraint:** this is a *retrofit*, not a rewrite. Do not restructure source code, do not rename existing modules, do not rewrite the README wholesale.

---

## 0. Prompt to hand the agent

> Read `docs/research/AGENT_INSTRUCTIONS.md` (this file, once installed). Audit this repository against it. Produce a migration plan listing (a) files to create, (b) existing documentation to amend with minimal diffs, (c) existing results/logs/notebooks that must be back-filled into experiment folders, and (d) any conflicts with the current layout. Wait for approval before writing. After approval, execute the plan and open a single PR titled `chore(research): establish docs/research record`.

---

## 1. Non-negotiable principles

These govern every decision below. If an instruction conflicts with a principle, the principle wins.

1. **One canonical location.** `docs/research/` is the only place a research finding is allowed to live. Results in a Slack message, a notebook cell, a commit message, or a chat log do not exist.
2. **The unit of record is the experiment, not the file.** Each experiment is a directory with a fixed internal contract, not a loose markdown file dropped into a pile.
3. **Every number in the paper traces to an experiment ID.** No orphan numbers. A claim without a traceable experiment is a claim that gets cut from the paper.
4. **Figures are generated, never hand-placed.** Each figure has a script that produces it from recorded results. If a figure cannot be regenerated by a command, it does not go in the paper. This is the `showyourwork` philosophy — the article's figures, tables, and numbers are all built from checked-in scripts so the whole manuscript can be rebuilt from scratch.
5. **Failures are recorded with equal weight.** Negative results, crashed runs, and abandoned directions are first-class entries. They are what stops you re-running a dead end in month nine, and they populate the limitations section.
6. **Record at write-time, not at paper-time.** The pipeline writes the record; the human reviews it. Anything that depends on human diligence at the end will not happen.
7. **Append-only for completed experiments.** A finished experiment folder is immutable. Corrections go in a new experiment or an appended `AMENDMENTS` section, never a silent edit.

---

## 2. Target structure

Create exactly this. Do not invent sibling directories.

```
docs/research/
├── README.md                     # what this directory is + how to add an experiment
├── AGENT_INSTRUCTIONS.md         # this file — the contract agents must follow
├── INDEX.md                      # auto-generated table of every experiment
├── CLAIMS.md                     # claim → experiment → figure/table ledger
├── DECISIONS.md                  # dated ADR-style log of research decisions
├── OPEN_QUESTIONS.md             # unresolved threads, reviewer-bait, next steps
├── RELATED_WORK.md               # running bibliography + scoop watch
├── templates/
│   ├── experiment/               # copied wholesale to start a new experiment
│   │   ├── manifest.yaml
│   │   ├── PROTOCOL.md
│   │   ├── RESULTS.md
│   │   └── figures/.gitkeep
│   ├── DECISION.md
│   └── negative_result.md
├── experiments/
│   ├── EXP-2026-08-001-baseline-retrieval/
│   ├── EXP-2026-08-002-ablation-graph-depth/
│   └── ...
├── figures/                      # publication-ready figures ONLY (promoted from experiments)
│   ├── fig01-architecture.svg
│   ├── fig01-architecture.pdf
│   └── sources/                  # the script that generated each promoted figure
├── tables/                       # publication-ready tables (.tex / .md, generated)
├── datasets/
│   ├── DATASETS.md               # provenance, licence, version, access instructions
│   └── cards/                    # one datasheet per dataset
└── paper/
    ├── OUTLINE.md
    ├── venue-targets.md          # deadlines, page limits, checklist requirements
    └── checklists/               # NeurIPS-style / artifact-evaluation checklists
```

**Where results data actually lives.** Raw run outputs (checkpoints, logs, large CSVs) do **not** go in `docs/research/`. They go in `results/` or object storage, tracked by DVC or Git LFS, and the experiment folder stores a *pointer plus hash*. `docs/research/` must stay cloneable in seconds.

---

## 3. The experiment directory contract

Directory name: `EXP-<YYYY>-<MM>-<NNN>-<kebab-slug>`
The ID is permanent and is the only handle used in code, figures, commit messages, and paper drafts.

```
EXP-2026-08-001-baseline-retrieval/
├── manifest.yaml          # REQUIRED — machine-readable
├── PROTOCOL.md            # REQUIRED — what we did and why, written BEFORE running
├── RESULTS.md             # REQUIRED — what happened, written after
├── figures/               # REQUIRED if any figure exists
│   ├── fig-recall-at-k.svg
│   ├── fig-recall-at-k.pdf
│   └── make_figures.py    # regenerates every figure in this folder
├── tables/                # generated .md and .tex fragments
├── data/
│   ├── metrics.json       # canonical machine-readable metrics
│   ├── metrics.csv        # per-seed / per-run raw numbers
│   └── POINTERS.md        # paths + hashes for large artifacts stored elsewhere
├── env/
│   ├── requirements.lock  # or environment.yml / uv.lock / Dockerfile digest
│   └── hardware.md        # GPU/CPU, RAM, driver, wall-clock, cost
├── logs/                  # trimmed run logs or pointers to them
└── ISSUES.md              # bugs, anomalies, aborted runs, gotchas
```

### 3.1 `manifest.yaml` — required schema

This is the field's structural answer to the reproducibility crisis: hyperparameters, seeds, data version, and compute all recorded, because undocumented hyperparameters and cherry-picked seeds are exactly the documented failure modes.

```yaml
id: EXP-2026-08-001
slug: baseline-retrieval
title: Baseline retrieval performance on <dataset>
status: complete            # planned | running | complete | failed | superseded
date_started: 2026-08-06
date_completed: 2026-08-07
authors: [<name>]

purpose: >
  One paragraph. What question does this answer, and which paper claim
  does it support or refute?

hypothesis: >
  Stated before the run. If exploratory, write "exploratory — no prior hypothesis".

# --- Reproducibility block (all fields mandatory) ---
code:
  commit: a1b2c3d4
  branch: main
  entrypoint: "python -m src.eval.baseline --config configs/baseline.yaml"
  dirty: false              # were there uncommitted changes? if true, explain in ISSUES.md
config:
  path: configs/baseline.yaml
  hash: sha256:...
data:
  name: <dataset>
  version: v1.2
  split: test
  n_examples: 4821
  dvc_path: data/processed/<dataset>.dvc
  hash: md5:...
seeds: [0, 1, 2, 3, 4]
n_runs: 5
environment:
  python: "3.11.9"
  lockfile: env/requirements.lock
  container: "ghcr.io/<org>/<img>@sha256:..."
compute:
  hardware: "1x A100 80GB"
  wall_clock_hours: 2.4
  estimated_cost_usd: 12.40

# --- LLM / agentic block (omit if not applicable) ---
llm:
  model: "<provider>/<model>@<version-or-date>"
  temperature: 0.0
  top_p: 1.0
  max_tokens: 2048
  prompt_files: [prompts/extractor.md]
  prompt_hash: sha256:...
  tool_schema_hash: sha256:...
  total_input_tokens: 1842301
  total_output_tokens: 233104
  nondeterminism_note: >
    Provider-side nondeterminism remains even at temperature 0; results
    are reported as mean ± std over n_runs.

# --- Results block ---
metrics:
  primary: recall@10
  values:
    recall@10: {mean: 0.612, std: 0.008, n: 5}
    ndcg@10:   {mean: 0.441, std: 0.011, n: 5}
baselines_compared: [BM25, dense-e5]
statistical_test: "paired bootstrap, 10k resamples, p=0.03"

# --- Linkage ---
supports_claims: [C-003, C-004]
figures: [fig-recall-at-k]
supersedes: null
superseded_by: null
related: [EXP-2026-07-004]
```

### 3.2 `PROTOCOL.md` — written *before* the run

Sections, in order: **Question · Hypothesis · Setup · Data · Metrics (defined, not just named) · Baselines · Procedure (exact commands) · What would falsify this**.

Pre-registering the protocol is what separates an experiment from a fishing expedition. It also means the methods section is already half-written.

### 3.3 `RESULTS.md` — written *after* the run

Sections, in order:

1. **Headline** — two sentences. The number and whether the hypothesis survived.
2. **Results table** — generated, not typed. Mean ± std across seeds, never a single run.
3. **Figures** — embedded with a one-line caption each, referencing the generating script.
4. **Interpretation** — what it means for the paper's argument.
5. **Threats to validity** — confounds, leakage risks, dataset artifacts, cost of the comparison.
6. **What surprised us** — the section that becomes the discussion section.
7. **Follow-ups** — each one becomes an entry in `OPEN_QUESTIONS.md`.
8. **Reproduction** — the exact command that regenerates everything in this folder.

### 3.4 Failed and abandoned experiments

They stay. Set `status: failed` or `superseded`, fill `RESULTS.md` with what broke and why, and never delete the directory. A deleted failed experiment is a dead end you will walk into twice.

---

## 4. Figure standards

The single most common way research repositories rot is figures pasted from a notebook that nobody can regenerate after the axis label turns out to be wrong.

**Rules:**

- Every figure has a generating script committed beside it. One command regenerates all figures in an experiment.
- Output **both** `.svg` (vector, editable, diffable, web-embeddable) and `.pdf` (for LaTeX). Raster `.png` only for genuine bitmaps like heatmap screenshots or qualitative examples — and even then, keep the generator.
- Figures read metrics from `data/metrics.json` or `metrics.csv`. Never from hardcoded numbers in the plotting script.
- Filename = `fig-<slug>.<ext>`, matching the `figures:` list in `manifest.yaml`.
- Fonts embedded; no font smaller than 7pt at final print size; colour-blind-safe palette; no colour-only encoding.
- Architecture and system diagrams are authored as SVG (hand-written or via a diagram-as-code tool such as Mermaid, Graphviz, or TikZ) and committed with their source. Binary-only diagram exports are rejected.
- **Promotion:** a figure only moves to `docs/research/figures/` when it is destined for the paper. Promotion copies the figure *and* its generator into `figures/sources/`, and records the source experiment ID in a header comment.

Add a `make figures` (or `just figures`) target that regenerates every promoted figure from scratch. Run it in CI. If it fails, the figures are stale.

---

## 5. Cross-cutting ledgers

### `INDEX.md` — auto-generated, never hand-edited

A script walks `experiments/*/manifest.yaml` and emits a table: ID · date · title · status · primary metric · claims supported · link. Regenerate on pre-commit and in CI. Hand-maintained indexes go stale within three weeks.

### `CLAIMS.md` — the paper's spine

| ID | Claim (as it will appear in the paper) | Evidence (EXP IDs) | Artifact (fig/table) | Status |
|----|----------------------------------------|--------------------|----------------------|--------|
| C-001 | Federated KG construction improves entity coverage over single-agent extraction | EXP-2026-08-002, EXP-2026-08-005 | fig02, tab01 | supported |
| C-002 | ... | — | — | **unsupported — needs experiment** |

This table is the highest-value artifact in the whole directory. It tells you, at a glance, which claims you can actually make and which are currently vapour. Write claims *before* they are supported and mark them unsupported — that is your experiment backlog.

### `DECISIONS.md` — ADR-style, dated, append-only

One entry per research decision: **Context · Options considered · Decision · Consequences · Date**. Covers metric choices, baseline selection, dataset exclusions, scope cuts. Reviewers ask "why did you use X?" — this is where the honest answer already lives.

### `RELATED_WORK.md`

Running bibliography with a one-line "how it differs from us" per entry, plus a **scoop watch** section for papers that partially overlap your contribution. Update it whenever a relevant paper surfaces, not the week before submission.

### `OPEN_QUESTIONS.md`

Every follow-up from every `RESULTS.md`, plus known weaknesses. This becomes future work and pre-empts reviewer objections.

---

## 6. Documentation amendments (minimal diffs only)

Do **not** rewrite existing docs. Make these surgical edits:

**`README.md`** — add one short section near the top:

```markdown
## Research record

This is a research repository. All experiments, results, figures, and findings
live in [`docs/research/`](docs/research/). Nothing that will appear in a paper
is recorded anywhere else. Before running an experiment, read
[`docs/research/AGENT_INSTRUCTIONS.md`](docs/research/AGENT_INSTRUCTIONS.md).
```

**`CONTRIBUTING.md`** (create a stub if absent) — add:

```markdown
### Running an experiment
1. `make new-experiment SLUG=<slug>` — scaffolds the folder from templates.
2. Fill `PROTOCOL.md` **before** running anything.
3. Run via the recorded entrypoint so the manifest is auto-populated.
4. Fill `RESULTS.md`, regenerate figures, run `make validate-research`.
5. Commit the experiment folder in the same PR as any code change it depends on.
```

**Agent instruction file** (`CLAUDE.md`, `AGENTS.md`, `.cursorrules`, or equivalent) — add:

```markdown
## Research record (mandatory)
- Any experiment, benchmark, baseline, ablation, or evaluation run MUST be
  recorded under `docs/research/experiments/` following
  `docs/research/AGENT_INSTRUCTIONS.md`.
- Never report a metric in chat or a commit message without also writing it to
  the corresponding `manifest.yaml` and `RESULTS.md`.
- Never hand-edit a figure. Never hardcode a number in a plotting script.
- Failed and abandoned runs are recorded, not deleted.
- If asked to "just quickly check" a number, still create the experiment folder.
```

**Do not touch:** licence, citation file, install instructions, API docs, changelog.

---

## 7. Automation and enforcement

Convention without enforcement decays. Implement all four:

**7.1 Scaffolding.** `scripts/new_experiment.py` (+ `make new-experiment SLUG=...`): allocates the next ID, copies `templates/experiment/`, pre-fills date, git commit, branch, and dirty-tree status.

**7.2 Auto-capture at run time.** A small `research_record` helper (or existing tracker — MLflow, W&B, DVCLive) that, on every evaluation run, writes git commit, config hash, data version/hash, seeds, environment lockfile, hardware, wall-clock, and final metrics straight into the experiment's `manifest.yaml` and `data/metrics.json`. The tracker's job is capture; `docs/research/` is the durable, human-readable, git-tracked mirror. If you use MLflow or W&B, still export a static snapshot into the experiment folder — external dashboards die, repositories persist.

**7.3 Validation.** `scripts/validate_research.py` (+ `make validate-research`), failing on:
- missing `manifest.yaml`, `PROTOCOL.md`, or `RESULTS.md`
- manifest failing schema validation (use a JSON Schema or Pydantic model)
- `status: complete` with empty `metrics.values`
- a figure listed in the manifest that has no file, or a figure file not listed
- a figure without a generating script in the same folder
- any `data/` artifact pointer without a hash
- a `supports_claims` ID absent from `CLAIMS.md`
- a stale `INDEX.md`

**7.4 CI.** A GitHub Actions job on every PR: run the validator, regenerate `INDEX.md` and fail if the diff is non-empty, regenerate promoted figures and fail on mismatch. Optionally, a bot comment that lists any experiment touched by the PR.

**7.5 Pre-commit hook.** Regenerate `INDEX.md`, run the validator on changed experiment folders.

**7.6 `.gitattributes` / `.gitignore`.** SVG and small JSON/CSV in git. Checkpoints, large parquet, and raw logs to DVC or LFS. Nothing above ~10 MB enters `docs/research/`.

---

## 8. Publication alignment

Bake the venue requirements in from day one rather than scrambling at submission.

- **`paper/checklists/`** — commit the reproducibility checklist for each target venue and treat it as a live to-do. These checklists ask for datasets, hyperparameters and the ranges searched, number of runs and seeds, metric definitions, and compute infrastructure. Every one of those fields is already a manifest field, by design.
- **`paper/venue-targets.md`** — deadline, page limit, anonymisation policy, artifact-evaluation track, supplementary limits.
- **Artifact evaluation.** Most ACM/USENIX venues award three badges: *Available* (artifact deposited in a public archival repository), *Functional* (documented, consistent, complete, exercisable), and *Results Reproduced* (an independent committee re-obtains the main results within tolerance). Note that GitHub alone does not satisfy *Available* — an archival DOI from Zenodo, Figshare, or Dryad is required. Add `paper/ARTIFACT_APPENDIX.md` early; the appendix is typically ~2 pages and mostly assembles from `manifest.yaml` + `PROTOCOL.md`.
- **Research compendium framing.** The end state is a single container holding the article, the analysis, the data, and the environment — that is the standard the repository should be able to become with one archival push, not a restructuring project.

---

## 9. Migration procedure for an existing repository

Execute in order; open a plan for approval before step 3.

1. **Inventory.** Find every existing result: notebooks with output cells, `results/` dirs, `.csv`/`.json` metric dumps, plots in `assets/` or `images/`, benchmark numbers quoted in README or issues, evaluation numbers in commit messages, prior chat/report exports.
2. **Triage.** For each: does it support a claim you intend to make? If yes → back-fill. If no → leave in place and note it.
3. **Back-fill.** Create an experiment folder per recovered result. Mark unknown provenance honestly: `status: superseded`, `code.commit: unknown`, and an `ISSUES.md` line stating what could not be recovered. **Do not fabricate manifest fields.** A back-filled experiment with three known fields is more useful than one with ten invented ones.
4. **Re-run what matters.** Any result destined for the paper that cannot be traced to a commit and a seed should be re-run under the new protocol. Everything else stays as historical record.
5. **Install structure + templates + automation** (sections 2, 3, 7).
6. **Amend docs** (section 6).
7. **Seed `CLAIMS.md`** with the paper's intended claims, most marked unsupported. This immediately surfaces the real experiment backlog.
8. **Turn on CI** and let it fail loudly on the next PR.

---

## 10. Acceptance criteria

The retrofit is complete when all of the following hold:

- [ ] `docs/research/` exists with the full structure from section 2.
- [ ] `make new-experiment SLUG=x` scaffolds a valid, validator-passing experiment.
- [ ] `make validate-research` passes and fails correctly on a deliberately broken folder.
- [ ] `make figures` regenerates every promoted figure with no diff.
- [ ] `INDEX.md` is generated and current.
- [ ] `CLAIMS.md` lists every intended paper claim with its evidence status.
- [ ] At least one real experiment is fully recorded end to end as a worked example.
- [ ] All pre-existing results are either back-filled or explicitly noted as unrecoverable.
- [ ] README, CONTRIBUTING, and the agent instruction file point at `docs/research/`, with no other documentation rewritten.
- [ ] CI enforces validation on every PR.

---

## 11. Non-goals

- Restructuring `src/`, renaming modules, or changing the build system.
- Adopting a heavyweight workflow engine unless one is already in use.
- Migrating to a new experiment-tracking SaaS. Use what exists; mirror it into the repository.
- Rewriting the README's project description, install steps, or usage examples.
- Storing large binary artifacts inside `docs/research/`.

---

## Appendix A — `CLAIMS.md` starter

```markdown
# Claims Ledger

Every claim the paper will make, and the evidence that supports it.
Status: `unsupported` | `partial` | `supported` | `refuted` | `cut`

| ID | Claim | Evidence | Artifact | Status | Notes |
|----|-------|----------|----------|--------|-------|
| C-001 | | | | unsupported | |

## Refuted / cut claims
Keep them. They document what was tried and prevent re-litigating settled questions.
```

## Appendix B — `DECISIONS.md` entry template

```markdown
## D-004 — Primary metric is recall@10, not MRR
**Date:** 2026-08-06
**Context:** Downstream agent only consumes the top-10 retrieved subgraph.
**Options:** MRR (standard in IR); recall@10 (task-aligned); nDCG@10 (graded relevance).
**Decision:** recall@10 primary; nDCG@10 reported as secondary.
**Consequences:** Not directly comparable to <prior work> Table 3; must report both.
```

## Appendix C — `ISSUES.md` entry template

```markdown
## 2026-08-06 — Run 3 crashed at step 4200 (OOM)
**Impact:** seed 3 excluded from the mean; n=4 not 5 for this cell.
**Cause:** batch size 32 with the larger context window exceeds 80GB.
**Resolution:** re-ran at batch size 16 with gradient accumulation; noted in manifest.
**Paper implication:** must state n=4 for this configuration.
```
