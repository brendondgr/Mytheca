# Frontend local env. Copy to `.env.local` (gitignored) to override defaults.

# Base URL of the Mytheca FastAPI backend. Defaults to the local backend if unset.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3345/api
