"""Authored content: the built-in Story-Graph type catalogue and per-stat
Markdown guidance. Data, not code — consumed by the registry and the
stat_guidance loader.

Layout
------
graph_registry.py   — Story-Graph node/edge type definitions.
stats/              — Per-stat Markdown guidance files (health.md,
                      suspicion.md, trust.md, patience.md). Each file
                      explains the stat's meaning, range, and value bands
                      so agents can interpret and adjust values correctly.
"""
